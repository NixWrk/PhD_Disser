"""Reader-facing result sections for scientific notebook 33.09."""


def result_sections(md, code):
    md(r'''## 3. Лучшие наборы по устойчивости двух тканевых оценок

**Таблица 2. Лучшие наборы для каждого числа размеров.** Приведены максимальные коэффициенты усиления ошибок по 49 состояниям, наибольшее число обусловленности и допустимое σ для локального стандартного отклонения обеих тканевых оценок не более 5%. Последний столбец — вычисленное требование к сценарию измерений, а не характеристика прибора. Меньшие g и большее допустимое σ предпочтительнее.''')
    code('''assert len(best)==8 and list(best.k)==list(range(2,10))
assert len(pd.read_csv(out/'subset_scores.csv'))==502
assert np.all(np.diff(best.worst_score)<=1e-9)
def sizes(s): return str(s).replace('|', ', ')
table(pd.DataFrame({'Число размеров':best.k,'Размеры, мм':best.sizes_mm.map(sizes),
 'g₁, 1/Ом':best.worst_sigma_logrho1_unit_ohm_error,'g₂, 1/Ом':best.worst_sigma_logrho2_unit_ohm_error,
 'κ, максимум':best.worst_kappa,'σ для 5%, Ом':0.05/best.worst_score}))
pair=best[best.k==2].iloc[0];full=best[best.k==9].iloc[0]
paragraph(f"**Лучшая пара по заданному критерию — {sizes(pair.sizes_mm)} мм.** "
 f"Её худшее усиление ошибок в {num(pair.worst_score/full.worst_score,2)} раза выше, чем у полного набора. "
 "Лучшая тройка — 50, 130 и 140 мм; набор из четырёх — 50, 120, 130 и 140 мм. "
 "Это сравнение локальной чувствительности; нелинейная проверка ниже рассматривается отдельно.")
fig=go.Figure()
for tissue,col,color in [('Мягкие ткани ρ₁','worst_sigma_logrho1_unit_ohm_error','#a65b17'),('Лёгкое ρ₂','worst_sigma_logrho2_unit_ohm_error','#007fbd')]:
 fig.add_trace(go.Scatter(x=best.k,y=100*0.01*best[col],mode='lines+markers',name=tissue,line=dict(color=color,width=3),
  customdata=best.sizes_mm.map(sizes),hovertemplate='Число размеров: %{x}<br>Набор: %{customdata} мм<br>Отклонение: %{y:.3f}%<extra>%{fullData.name}</extra>'))
fig.update_xaxes(title='Число разных размеров сборок',dtick=1)
fig.update_yaxes(title='Локальное стандартное отклонение, %',type='log',tickmode='array',tickvals=[0.03,0.05,0.1,0.5,1,3,5],ticktext=['0,03','0,05','0,1','0,5','1','3','5'])
fig.update_layout(height=430,legend=dict(orientation='h',y=1.12,x=0));show(fig)
''')
    md(r'''**Рисунок 1. Изменение худшего локального стандартного отклонения тканевых оценок при увеличении числа размеров.** Для каждой точки взят лучший набор таблицы 2; принято одинаковое σ = 0,01 Ом без корреляции ошибок. Вертикальная шкала логарифмическая: равные расстояния по ней соответствуют одинаковому отношению ошибок. Ниже — лучше. Максимум для каждой ткани берётся по 49 состояниям и может достигаться при разных сопротивлениях.

Оценка лёгкого заметно менее устойчива, чем оценка мягких тканей. Дополнительные размеры уменьшают усиление ошибок, но выигрыш постепенно сокращается. В частности, переход от пяти размеров к девяти улучшает худший коэффициент всего примерно на 4,8%. При этом число обусловленности может расти: добавленные измерения усиливают уже хорошо определяемое направление сильнее, чем плохо определяемое. Поэтому κ нельзя использовать как единственный показатель точности.

Эти кривые не включают систематические смещения и межзаписные изменения состояния. Чтобы проверить ошибку самого восстановления, далее решается нелинейная обратная задача.

## 4. Восстановление известных сопротивлений и ошибка формы лёгкого

На 16 состояниях между узлами библиотеки проведены два теста. **Самопроверка модели:** цель рассчитана прямым FEM модели 4, а сопротивления восстанавливаются через её сплайн. **Проверка геометрического упрощения:** цель рассчитана прямым FEM исходной КТ-маски, а обратная задача использует ту же модель 4. Сопротивления цели известны, поэтому здесь можно вычислить ошибку параметров. Случайные возмущения не добавляются.

Каждая инверсия ограничена принятыми диапазонами и запускается из лучшего узла библиотеки и центра диапазона в логарифмических координатах. Это уменьшает зависимость от старта, но не доказывает глобальную единственность. Нулевая невязка двух измерений при двух неизвестных сама по себе не является проверкой; доказательную роль имеет сравнение с известными сопротивлениями.''')
    code('''iq=read('interpolation_qc.json')['validation'];pts=iq['points_summary']
summary=pd.read_csv(out/'summary_by_count.csv');inv=pd.read_csv(out/'inverse_validation.csv')
assert len(inv)==256 and inv.success.all()
paragraph(f"Максимальная ошибка интерполяции прямого импеданса — **{num(iq['Z_abs_error_ohm']['max_abs'],5)} Ом**. "
 f"Максимальная относительная ошибка вектора чувствительности — {num(100*max(p['J1_relative_l2_error'] for p in pts),3)}% для ρ₁ "
 f"и {num(100*max(p['J2_relative_l2_error'] for p in pts),3)}% для ρ₂. "
 "Вектор включает девять размеров, ошибка рассчитана по евклидовой норме. Последствия интерполяции для параметров показывает самопроверка в таблице 3.")
paragraph('**Таблица 3. Максимальные абсолютные ошибки восстановления сопротивлений на 16 дополнительных состояниях, %.** '
 'Самопроверка учитывает интерполяцию и инверсию; при КТ-цели добавляется замена формы лёгкого. Максимумы разных тестов нельзя просто вычесть для выделения одной причины.')
selftest=summary[summary.target=='self_model4'].set_index('k');cttest=summary[summary.target=='ct_reference'].set_index('k')
table(pd.DataFrame({'Число размеров':best.k,
 'Самопроверка, ρ₁':selftest.loc[best.k,'max_abs_rho1_error_pct'].to_numpy(),
 'Самопроверка, ρ₂':selftest.loc[best.k,'max_abs_rho2_error_pct'].to_numpy(),
 'КТ-цель, ρ₁':cttest.loc[best.k,'max_abs_rho1_error_pct'].to_numpy(),
 'КТ-цель, ρ₂':cttest.loc[best.k,'max_abs_rho2_error_pct'].to_numpy(),
 'Решений на границе, КТ-цель':cttest.loc[best.k,'bounds_hit_count'].to_numpy()}))
paragraph(f"**У пары 50 и 140 мм** максимальная ошибка при КТ-цели — "
 f"{num(cttest.loc[2,'max_abs_rho1_error_pct'])}% для ρ₁ и {num(cttest.loc[2,'max_abs_rho2_error_pct'])}% для ρ₂. "
 f"У девяти размеров — {num(cttest.loc[9,'max_abs_rho1_error_pct'])}% и {num(cttest.loc[9,'max_abs_rho2_error_pct'])}% соответственно. "
 "В этих синтетических тестах решения остаются внутри границ сопротивлений.")
''')
    md(r'''**Главное ограничение:** ошибка ρ₂ около 8,5–8,7% сохраняется даже без ошибки измерения и почти не уменьшается при добавлении сборок. В самопроверке она не превышает примерно 0,17%. Следовательно, интерполяция не объясняет основную величину обнаруженного расхождения. Набор, хороший по локальной чувствительности, может сохранять смещение из-за принятого упрощения формы лёгкого. Требование к полной ошибке ρ₂ не более 5% здесь не выполнено.

Самый неблагоприятный случай найден при ρ₁ около 1,765 и ρ₂ около 21,500 Ом·м: модель 4 восстанавливает ρ₂ около 19,6–19,7 Ом·м. Это конкретный результат двухтканного КТ-эксперимента. Он не означает, что такая же ошибка возникает при любых физиологических значениях или что исходная КТ-модель экспериментально валидирована.

Для четырёх наборов в этом неблагоприятном случае дополнительно рассчитаны прямые FEM-импедансы при восстановленных сопротивлениях. Такая проверка контролирует замену прямого решателя сплайном в найденной точке; она не является повторной полной оптимизацией с FEM на каждом шаге. Аналогично проверены реальные оценки для пары и полного набора. Результаты этой проверки приведены после экспериментальной таблицы.

Здесь нелинейно проверены восемь наборов, выбранных по локальному критерию. Наименьшая КТ-ошибка среди всех 502 наборов отдельно не оптимизировалась. Увеличение числа измерений уменьшает случайную составляющую в принятом сценарии, но не обязано исправлять систематическую ошибку модели.

## 5. Условное применение к экспериментальным вдоху и выдоху

Те же наборы применены к базовым уровням из 33.08. Истинные сопротивления у человека не измерены. Диаметр 5 мм является новым модельным условием, а геометрия вдоха применяется также для выдоха. Это проверка согласованности сценария с записями, а не доказательство точности тканевых оценок.

Для исключённых из подгонки размеров вычислена ошибка прогноза импеданса. Эта проверка выявляет решения, согласующиеся только с выбранными измерениями. Она остаётся проверкой внутри одного человека и общего последовательного эксперимента.''')
    code('''real=pd.read_csv(out/'real_fits.csv');assert len(real)==16 and real.success.all()
state_names={'inhale':'Вдох','exhale':'Выдох'}
fig=make_subplots(rows=1,cols=2,horizontal_spacing=0.12,subplot_titles=['Мягкие ткани','Лёгкое'])
for state,color in [('inhale','#007fbd'),('exhale','#ce552d')]:
 d=real[real.state==state].sort_values('k')
 for tissue in [1,2]:
  fig.add_trace(go.Scatter(x=d.k,y=d[f'rho{tissue}_hat'],mode='lines+markers',name=state_names[state],legendgroup=state,
   showlegend=tissue==1,line=dict(color=color,width=3,dash='solid' if state=='inhale' else 'dash'),
   marker=dict(symbol='circle' if state=='inhale' else 'diamond-open',size=9),
   hovertemplate='Число размеров: %{x}<br>ρ: %{y:.4f} Ом·м<extra>%{fullData.name}</extra>'),row=1,col=tissue)
for tissue in [1,2]:
 lo,hi=plan['bounds_ohm_m'][tissue-1]
 for bound in [lo,hi]: fig.add_hline(y=bound,line_dash='dot',line_color='#7e8793',row=1,col=tissue)
 fig.update_yaxes(title=('ρ₁' if tissue==1 else 'ρ₂')+', Ом·м',range=[lo-0.06*(hi-lo),hi+0.06*(hi-lo)],row=1,col=tissue)
fig.update_xaxes(title='Число разных размеров',dtick=1)
fig.update_layout(height=430,legend=dict(orientation='h',y=1.2,x=0));show(fig)
''')
    md(r'''**Рисунок 2. Зависимость восстановленных базовых сопротивлений от числа размеров в подгонке.** Синяя сплошная кривая — вдох, оранжевая штриховая — выдох. Для каждого числа взят набор таблицы 2. Серые пунктирные линии показывают границы поиска. Кривые ρ₂ совпадают на верхней границе; переключение легенды позволяет посмотреть их отдельно.

**Такое совпадение не подтверждает одинаковое сопротивление лёгкого на вдохе и выдохе.** В обоих состояниях модель упирается в ограничение. При этом оценка ρ₁ заметно зависит от состава измерений. Следовательно, текущий сценарий с контактом 5 мм ещё не даёт согласованного физиологического восстановления двух параметров из этих записей.

**Таблица 4. Подгонка и прогноз исключённых размеров.** Сопротивления приведены в Ом·м, среднеквадратические ошибки импеданса — в Ом. У полного набора нет исключённых размеров. Невязка включает все расхождения модели с данными и не используется как оценка σ из раздела 2.''')
    code('''selected=real[real.k.isin([2,3,4,9])].copy()
def edge_label(r):
 found=[]
 for j in [1,2]:
  if r[f'bounds_hit_rho{j}_lower']: found.append(f'ρ{j}: нижняя')
  if r[f'bounds_hit_rho{j}_upper']: found.append(f'ρ{j}: верхняя')
 return '; '.join(found) or 'внутри диапазона'
table(pd.DataFrame({'Размеров':selected.k,'Состояние':selected.state.map(state_names),
 'ρ₁':selected.rho1_hat,'ρ₂':selected.rho2_hat,'Ошибка подгонки, Ом':selected.fit_resid_rmse_ohm,
 'Прогноз исключённых, Ом':pd.to_numeric(selected.heldout_rmse_ohm,errors='coerce').map(lambda x:num(x) if pd.notna(x) else 'нет проверки'),
 'Граница поиска':selected.apply(edge_label,axis=1)}))
for state in ['inhale','exhale']:
 r=real[(real.state==state)&(real.k==9)].iloc[0]
 paragraph(f"Полный набор, {state_names[state].lower()}: **ρ₁ = {num(r.rho1_hat,4)}, ρ₂ = {num(r.rho2_hat,4)} Ом·м**, "
  f"невязка {num(r.fit_resid_rmse_ohm)} Ом; {edge_label(r)}.")
direct=read('direct_check_qc.json')
paragraph(f"Дополнительная проверка включает {direct['points']} пар сопротивлений и {direct['rows']} прямых FEM-импеданса. "
 f"Максимальное расхождение со сплайном — **{num(direct['max_spline_Z_error_ohm'],5)} Ом**. "
 f"Изменение среднеквадратической невязки выбранных размеров не превышает {num(direct['max_selected_rmse_change_ohm'],5)} Ом. "
 "Это контролирует вычислительное ускорение в выбранных точках и не является независимой проверкой истинных сопротивлений.")
''')
    md(r'''У пары 50 и 140 мм прогноз остальных размеров ошибается значительно сильнее, чем её собственная подгонка. Для вдоха ошибка прогноза около 14,4 Ом, для выдоха — 12,9 Ом. Даже полный набор имеет невязки около 7,1 и 6,4 Ом. Поэтому объявлять пару достаточной для текущих реальных записей нельзя.

Расхождение с 33.08 нельзя целиком приписать диаметру: здесь также уточнена дискретизация контактов. Фактическую контактную площадь эксперимента необходимо согласовать с модельной. Затем следует проверять оставшиеся допущения — прежде всего неизменную геометрию между вдохом и выдохом — при одном и том же наборе измерений. Само добавление сборок эти вопросы не решает.

''')
    md(r'''### 5.1. Кривые решений отдельных сборок

Общая подгонка скрывает, какие пары сопротивлений требуются каждому отдельному измерению. Для проверки согласованности строим зависимость ρ₁ от заданного ρ₂ отдельно для каждого размера L и каждого дыхательного состояния:

$$Z_L(\rho_{1,L}(\rho_2),\rho_2)=Z_L^{\mathrm{изм}},$$
где $Z_L$ — импеданс выбранной модели для сборки длиной L, Ом; $Z_L^{\mathrm{изм}}$ — её экспериментальный базовый уровень на вдохе либо выдохе, Ом; $\rho_{1,L}(\rho_2)$ — удельное сопротивление мягких тканей, которое при заданном сопротивлении лёгкого ρ₂ воспроизводит это единственное измерение, Ом·м. Индекс L обозначает отдельное решение уравнения; он не вводит разные физические сопротивления тканей под разными сборками.

Используется та же библиотека модели 4 с четырьмя конечными контактами диаметром 5 мм. Геометрия тела, форма лёгкого, h и монтаж фиксированы. Для 601 значения ρ₂ в принятом диапазоне одномерным поиском корня определяется ρ₁ внутри его рабочих границ. Расчёт идёт по кубическому сплайну ранее полученных FEM-импедансов, без новых решений FEM и без экстраполяции. Положительность производной по ρ₁ проверена численно на густой сетке; для всех построенных точек найден корень внутри границ. Это численная проверка выбранной ветви решений, а не общее доказательство единственности обратной задачи.

**Как читать график.** Каждая точка цветной кривой точно воспроизводит один базовый импеданс в пределах точности сплайна и поиска корня. Вертикальная линия при выбранном ρ₂ показывает, какое ρ₁ требует каждая сборка. Пересечение всех кривых означало бы одну пару сопротивлений, объясняющую все девять измерений. Пересечение только двух кривых согласует лишь эту пару измерений; остальные размеры должны проверяться отдельно.''')
    code('''sys.path.insert(0,str(pipe/'tools'))
from lateral_array_design_solution_curves import make_figure
curve_qc=read('solution_curves_qc.json')
assert curve_qc['new_FEM_solutions']==0 and curve_qc['finite_diameter_mm']==5
for name,digest in curve_qc['input_sha256'].items(): assert sha(out/name)==digest
for name,digest in curve_qc['output_sha256'].items(): assert sha(out/name)==digest
for name,digest in curve_qc['code_sha256'].items(): assert sha(pipe/'tools'/name)==digest
fig=make_figure(out)
display(HTML(fig.to_html(full_html=False,include_plotlyjs='inline',config={'responsive':True,'displaylogo':False})))
''')
    md(r'''**Рисунок 3. Зависимости ρ₁ от ρ₂, согласующие модель с отдельными сборками, на вдохе и выдохе.** Цвет обозначает размер в миллиметрах; шкалы обеих панелей одинаковы. Круги отмечают найденные пересечения пар, чёрный ромб — общую подгонку по девяти размерам из таблицы 4. Обе шкалы начинаются с нуля. Серым отмечена область вне рабочего диапазона, в которой кривые не рассчитывались; штриховые линии обозначают границы расчёта. Кнопка «Крупнее» увеличивает участок с кривыми, «От нуля» возвращает исходные шкалы. Начало оси в нуле не означает расчёт при ρ = 0: в данной модели проводимость равна 1/ρ, а библиотека построена только для положительных сопротивлений в указанных границах. Щелчок по легенде переключает выбранный размер на обеих панелях; наведение на круг показывает пару и её невязку на полном наборе.''')
    code('''for state in ['inhale','exhale']:
 s=curve_qc['states'][state]
 paragraph(f"**{state_names[state]}.** Найдено пересечений отдельных пар: {s['pair_crossings_found']}. "
  f"Минимальный обнаруженный размах ρ₁ между девятью кривыми при одном ρ₂ — "
  f"**{num(s['minimum_sampled_and_refined_rho1_spread'])} Ом·м**, "
  f"при ρ₂ = {num(s['rho2_at_minimum'])} Ом·м. "
  f"На всей рассчитанной сетке сборка 50 мм требует ρ₁ не выше {num(s['sampled_rho1_50_max'])}, "
  f"а сборка 70 мм — не ниже {num(s['sampled_rho1_70_min'])} Ом·м.")
paragraph(f"Максимальная невязка уравнения одной сборки в построенных точках — "
 f"{curve_qc['max_root_residual_ohm']:.2e} Ом. Это точность поиска корня сплайна; "
 "точность приближения к прямому FEM оценивается отдельно в разделе 4.")
''')
    md(r'''**Вывод:** в исследованном диапазоне кривые не сходятся к одной паре сопротивлений. В частности, решения сборок 50 и 70 мм разделены во всём рассчитанном диапазоне. Поэтому чёрный ромб обозначает компромисс с ненулевыми невязками, а его положение на верхней границе ρ₂ не подтверждает истинное сопротивление лёгкого. Небольшой наклон кривых показывает, что изменение ρ₂ в рабочем диапазоне слабо изменяет требуемое ρ₁. Различие наклонов полезно для разделения двух параметров только при согласованности измерений с общей моделью.

Указанный размах характеризует расхождение решений сборок; он не является ошибкой относительно истинного ρ₁ или доверительным интервалом. Пересечения найдены по смене знака разности двух кривых с уточнением корня; касания без смены знака исчерпывающе не проверялись. Без установленной погрешности каждого измерения нельзя заменить требование точного пересечения произвольной шириной полос. График показывает несогласованность данного сценария с совокупностью записей, но не определяет её причину: сохраняются допущения о контактной площади, двухтканном составе, общей геометрии дыхательных состояний и сопоставимости последовательных записей.

Выбор малого числа сборок по чувствительности пока остаётся проектным результатом. Перед оценкой его достаточности выделим кривые лучших наборов: это позволит проверить, сохраняется ли обнаруженное расхождение после сокращения числа измерений.

''')
    md(r'''### 5.2. Кривые для лучших наборов из двух–девяти сборок

Общий рисунок 3 показывает согласованность всего эксперимента. Для оценки сокращённой схемы нужно отдельно рассмотреть именно те сборки, которые вошли в лучший набор таблицы 2. Поэтому на следующем рисунке число размеров переключается от двух до девяти. Для каждого выбора показаны только его кривые, пересечения пар внутри набора и соответствующая общая подгонка на вдохе и выдохе.

Начальный вариант — **50 и 140 мм**. Далее доступны **50, 130 и 140 мм**; **50, 120, 130 и 140 мм**, а также лучшие наборы из пяти–девяти размеров. Их состав берётся непосредственно из таблицы результатов выбора, без ручного назначения. Кривые отдельных сборок сохраняются: отбор измерений меняет совместную подгонку, но не уравнение одной сборки и не её геометрию.

Здесь слово «лучший» относится к минимальному худшему усилению ошибок двух тканевых оценок по критерию раздела 2. Оно не означает минимальную невязку на экспериментальных записях или уже доказанную достаточность набора.''')
    code('''from lateral_array_design_solution_curves import make_optimal_figure
fig=make_optimal_figure(out)
display(HTML(fig.to_html(full_html=False,include_plotlyjs='inline',config={'responsive':True,'displaylogo':False})))
''')
    md(r'''**Рисунок 4. Решения отдельных сборок в лучших наборах из двух–девяти размеров.** В меню выбирается число размеров; состав набора указан в заголовке. Левая панель — вдох, правая — выдох. Цвет одного размера сохраняется при всех переключениях. Чёрный ромб показывает подгонку именно выбранного набора; его подсказка содержит невязку выбранных измерений. Круги относятся только к пересечениям двух кривых внутри текущего набора. По умолчанию обе оси начинаются с нуля; серый фон, рабочие границы и кнопки масштаба имеют тот же смысл, что на рисунке 3.

**Как сравнивать наборы.** Сначала следует проверить, встречаются ли все его кривые в одной точке, затем — где находится ромб подгонки и какова его невязка. Для этой проверки удобно включить «Крупнее». В рассматриваемом диапазоне уже кривые 50 и 140 мм не пересекаются. Поскольку оба размера входят во все выбранные наборы, ни один из них не даёт точного общего решения для текущих записей. Добавление сборок изменяет положение компромиссной оценки; по одному перемещению ромба нельзя судить о приближении к истинным сопротивлениям.

Таким образом, оптимальность по расчётной чувствительности и согласованность с реальными измерениями проверяются раздельно. Следующий раздел формулирует условную достаточность при заданных ошибках измерения и не отменяет обнаруженных расхождений.''')
    md(r'''## 6. Условно достаточное число сборок

**Таблица 5. Минимальное число размеров по локальному критерию для заданных σ и порога стандартного отклонения обеих тканевых оценок.** Условие проверено на 49 узлах. «Не достигнуто» означает, что даже девять размеров не удовлетворяют сценарию. Порог не является гарантированной максимальной ошибкой или 95%-ным интервалом.''')
    code('''minimum=pd.read_csv(out/'minimum_k_by_threshold.csv')
wide=minimum.pivot(index='sigma_ohm',columns='threshold_pct',values='minimum_k')
for c in wide.columns:
 wide[c]=pd.to_numeric(wide[c],errors='coerce').map(lambda x:str(int(x)) if pd.notna(x) else 'Не достигнуто')
wide.columns=[f'Порог {int(c)}%' for c in wide.columns];wide=wide.reset_index().rename(columns={'sigma_ohm':'σ, Ом'})
table(wide)
paragraph(f"По локальному критерию при σ = 0,01 Ом и пороге 5% проходят уже два размера — 50 и 140 мм. "
 "При σ = 0,05 Ом и пороге 20% минимально проходит тройка 50, 130 и 140 мм. "
 f"Условие 5% для полного набора требует σ не выше **{num(0.05/full.worst_score,5)} Ом** в самом трудном проверенном состоянии. "
 "Это расчёт случайной составляющей при заданных условиях, без найденного выше смещения геометрической модели.")
''')
    md(r'''**Почему это ещё не достаточность всей методики.** При требовании полной ошибки ρ₂ не более 5% положительный результат локального сценария не устраняет систематическое расхождение 8,5–8,7% в КТ-тесте. Истинная ковариация ошибок записей также не определена. Для крупных сценарных отклонений дополнительно требуется нелинейная проверка с возмущёнными измерениями: линейная формула не задаёт точную величину ошибки далеко от исходной точки.

**Практический итог:** пару 50/140 мм следует сохранить как основного кандидата, тройку 50/130/140 мм — как вариант с дополнительной информацией. Полный девятиразмерный ряд пока нужен для контроля. Утверждать сокращённый протокол преждевременно: сначала нужно согласовать конечные контакты с экспериментом и уменьшить либо учесть обнаруженную ошибку геометрического упрощения. Базовое ρ₁ при дыхании, изменение h и пульсовые параметры этим подбором не исключались.''')
