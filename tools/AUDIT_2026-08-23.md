# Критический аудит — tools

- Дата фиксации: 2026-08-23
- Исходная задача Codex: `01a02e5a-d65d-7100-9a15-dd51cda24dcf`
- Режим: read-only; во время аудита файлы и Git-история не изменялись.
- Базовая ветка на момент аудита: `Калмыков`.

Ниже сохранён итоговый текст независимого критического прохода. Это технический
аудит состояния папки, а не подтверждение научной валидности её результатов.

Аудит завершён строго read-only. Файлы, Git-история и планировщик не изменялись.

Целевая копия `D:\Аспа\Kalmykov_PhD` подтверждена: ветка `Калмыков`, HEAD `e47f769`, рабочее дерево чистое.

## Критические проблемы

1. Планировщик запускает не целевой репозиторий.

   Фактическая задача `Kalmykov research reporting` активна, запускается каждые 15 минут, но её action указывает на:

   `D:\Git_Code\Kardio_respiratory_dudes\Калмыков\tools\research_report.py`

   Рабочий каталог также старый. В старой копии:

   - HEAD: `07c52ad`;
   - 1 изменённый notebook;
   - 930 неотслеживаемых файлов;
   - `scheduled-run.json`: `outcome=deferred_dirty`, `git_clean=False`;
   - `archive_transaction_id` отсутствует.

   Поэтому код Task Scheduler `0` не означает публикацию архива. Целевая копия `D:\Аспа\Kalmykov_PhD` этой задачей не обслуживается.

   Связанные пути: [install_research_reporting_task.ps1](<D:\Аспа\Kalmykov_PhD\tools\install_research_reporting_task.ps1:24>), `D:\Git_Code\Kardio_respiratory_dudes\Калмыков\research\generated\scheduled-run.json`.

2. Зеркалирование имеет чрезмерно широкий охват.

   [research_report.py](<D:\Аспа\Kalmykov_PhD\tools\research_report.py:1223>) рекурсивно ищет все `.md` и `.html` во всём репозитории. В конфигурации исключены только отдельные каталоги; нет исключений для `.git-recovery`, `Colab Notebooks_rebuilt*`, `private`, `results`, `outputs`, `.venv-registration`.

   Наблюдаемые факты:

   - 120 доступных `.md/.html`, из них только 22 внутри `research/`;
   - Git показывает 65 047 ignored-путей;
   - среди ignored есть как минимум 77 потенциально зеркалируемых `.md/.html`, включая 41 в `.git-recovery` и 25 в rebuilt-снимках;
   - `.git-recovery` по корневым правилам нельзя удалять или публиковать без отдельной проверки.

   Это может отправить во внутреннее зеркало промежуточные, исторические, чужие и медицинские материалы.

3. Сохранённые notebook outputs фактически считаются публикуемыми.

   В канонической папке `Colab Notebooks` обнаружено 13 notebook-файлов:

   - все 13 содержат сохранённые outputs и execution counts;
   - 10 содержат графические/HTML/PDF outputs;
   - 12 содержат абсолютные локальные пути;
   - прямых маркеров паспорта, СНИЛС, email или `patient id` автоматический скан не обнаружил, но это не является подтверждением обезличивания.

   Экспорт прямо сохраняет вычисления без повторного запуска: [research_report.py](<D:\Аспа\Kalmykov_PhD\tools\research_report.py:976>). Существующий файл `Colab Notebooks\16_Эксперимент_РНЦХ_2026-07-15.html` находится вне `research/generated` и не содержит баннера конфиденциальности/provenance.

4. Маркировка `internal_restricted` не является контролем доступа.

   [research-report.json](<D:\Аспа\Kalmykov_PhD\research-report.json:11>) задаёт только строковую маркировку. Код не проверяет ACL или владельца Z:-архива. Манифест также записывает абсолютный `source_root`: [research_report.py](<D:\Аспа\Kalmykov_PhD\tools\research_report.py:1805>).

## Существенные проблемы

- Воспроизводимость неполная: проверяются `REPORT_META`, ссылки и существование Git-коммита, но не проверяются версии Python/nbconvert, входные данные, параметры notebook, единицы, QC, хеши внешних данных и соответствие файла указанному коммиту. Внешние абсолютные artifacts принимаются без проверки: [research_report.py](<D:\Аспа\Kalmykov_PhD\tools\research_report.py:315>).

- Зависимость `nbconvert` не закреплена. В коде она обязательна при `export_notebooks=true`, но отдельного `requirements.txt`, `pyproject.toml` или CI для `tools` нет: [research_report.py](<D:\Аспа\Kalmykov_PhD\tools\research_report.py:1081>).

- Тесты покрывают только ссылки и транзакционное восстановление: 9/9 прошли. Нет тестов конфигурации, notebook-export, ширины зеркала, планировщика, абсолютных путей, ACL и приватности. [test_research_report_transactions.py](<D:\Аспа\Kalmykov_PhD\tools\tests\test_research_report_transactions.py:37>).

- Установщик ждёт завершения первого запуска 60 секунд, тогда как задача допускает выполнение до 2 часов; при 13 notebook-файлах возможен ложный отказ установки: [install_research_reporting_task.ps1](<D:\Аспа\Kalmykov_PhD\tools\install_research_reporting_task.ps1:7>), [install_research_reporting_task.ps1](<D:\Аспа\Kalmykov_PhD\tools\install_research_reporting_task.ps1:77>).

- Git вызывается с `safe.directory=*`, что ослабляет проверку владения репозиторием: [research_report.py](<D:\Аспа\Kalmykov_PhD\tools\research_report.py:184>). При этом shell-инъекции в Python-коде не обнаружены: `subprocess.run` получает список аргументов, а не shell-строку.

- `validate` целевой копии даёт `14 records, 0 errors, 5 warnings`; все предупреждения относятся к ещё отсутствующим HTML в `research/generated`. Каталог `research/generated` сейчас отсутствует.

## Организационные замечания

Внутри `tools` нет случайного отдельного файла: все четыре файла образуют связный набор.

| Файл | Назначение | Оценка |
|---|---|---|
| `tools/research_report.py` | validate/build/export-html/sync/update | Основное ядро, но проектно-специфичное, не общая репозиторная библиотека |
| `tools/install_research_reporting_task.ps1` | регистрация Windows-задачи | Нужен только при осознанной фоновой публикации; текущая регистрация устарела |
| `tools/tests/test_research_report_transactions.py` | 9 unit-тестов | Нужен, но покрытие недостаточно |
| `tools/README.md` | краткая инструкция | Нужен, но не описывает фактический путь установленной задачи, ACL и зависимости |

`research_report.py` занимает около 2 189 строк. Возможный мёртвый интерфейс: `git_context()` возвращает `git_root`, а `git_audit()` принимает `context`, но эти данные дальше не используются. Это организационное замечание, не доказательство неисправности.

## Следующий безопасный шаг

Не запускать `install`, `update` или публикацию до явного решения, какая копия является канонической. Сначала сохранить старую рабочую копию без очистки и отдельно подтвердить:

1. переназначить или вывести из эксплуатации задачу, указывающую на `D:\Git_Code\...`;
2. ограничить зеркало allowlist-каталогами `research/` и нужными производными;
3. исключить `.git-recovery`, rebuilt-снимки, виртуальные окружения, `private`, `raw/results/outputs`;
4. проверить ACL Z:-архива и выполнить только read-only `sync --dry-run` из `D:\Аспа\Kalmykov_PhD`.
