# Порядок реализации

Дата: 2026-07-31.

## Принцип выбора

Сначала строится измерительный и валидационный фундамент. Модель выдоха не реализуется,
пока проект не умеет доказать на известных парах, что правильно читает данные, совмещает
анатомию и измеряет нужную величину по всей доступной поверхности лёгких.

## Этап 0. Зафиксировать предмет и данные

Зачем: исключить повторение опытов с разными определениями толщины и неверными фазами.

- использовать определения `MEASUREMENT_MODEL.md`;
- создать machine-readable manifest для каждого набора: источник, protocol class,
  breath state, pathology, FOV, spacing, labels, subject overlap;
- исключить NLST2023 из respiratory-pair cohort;
- разделить Lung250M-4B на breath-hold, 4DCT и longitudinal поднаборы;
- распаковать все 10 DIR-Lab COPDgene воспроизводимым способом.

Gate 0: один CLI-командный запуск перечисляет все случаи и не смешивает протоколы.

Статус: выполнен для COPDgene и LungCT — 40/40 полных пар, protocol/FOV/phase labels и
checksum исходных архивов сохранены в локальном manifest.

## Этап 1. Надёжный парный benchmark лёгких

Зачем: получить ground truth деформации, на котором позже сравниваются простая, статистическая
и механическая модели.

- автоматические маски лёгких/долей и рёбер;
- rigid alignment по позвоночнику с Dice не ниже 0.70;
- deformable registration с поддержкой sliding или регистрацией долей раздельно;
- landmark TRE по субъекту и регионам;
- Jacobian, folding, volume consistency и surface-distance QC;
- batch-отчёт по COPDgene, LungCT и выбранным breath-hold поднаборам Lung250M.

Gate 1: средний TRE на DIR-Lab не хуже 2 мм, нет folding, нет утечки между лёгкими и
окружающими тканями. Если gate не достигнут, локальная толщина не сравнивается.

Этот gate далее разделяется на Gate 1L для лёгких и Gate 1B для грудной стенки. Текущие
expert landmarks и lung surface QC оценивают 1L. Jacobian внутри тела необходим, но не
достаточен для 1B: правильность движения кожи, жира, мышц и рёбер пока независимо не
валидирована.

Статус: batch/QC завершён на 10 COPDgene и 3 LungCT с expert landmarks. Elastix и locked
ConvexAdam прошли Gate 1 у 0/13. ConvexAdam выбран на шести development LungCT без expert
landmarks и улучшил mean TRE относительно Elastix у 13/13; общая медиана 14.83→6.50 мм,
на COPDgene 16.96→9.12 мм, на LungCT 4.52→2.06 мм. Однако у всех 13 превышены mean-TRE
и FOV-surface p95 limits, а у 3/13 есть folding внутри тела/общего FOV. Ошибка максимальна
в нижних и средних отделах. Реализован FOV-aware mask/surface QC с исключением
искусственных границ обрезанного тома и региональный TRE. Парные анатомические эффекты
остаются заблокированы.

## Этап 2. Программа-минимум: профиль стенки

Зачем: определить, есть ли измеримый сигнал изменения мягких тканей по всей поверхности
лёгких, без предварительной привязки к электродам.

- сегментация тела, кожи, жира, мышцы, рёбер и лёгких;
- 3D-профили `кожа→лёгкое` с компонентами тканей;
- равномерное физическое сэмплирование всей доступной плевральной поверхности;
- отдельная маркировка путей через ребро и отбраковка путей через толщу лёгкого/FOV;
- subject-level bootstrap и sensitivity к порогам/сегментации.

Gate 2: synthetic phantom восстанавливается в пределах одного вокселя; на реальных данных
не менее 90% профилей имеют валидный endpoint и согласованную сумму компонентов; эффект
отделён от registration/segmentation uncertainty.

Статус: API и CLI реализованы, synthetic phantom проходит инварианты суммы компонентов и
покрытия шести направлений. На `copd1` валидны 51% и 53% кандидатов по фазам, поэтому
текущий строгий критерий 90% не достигнут. Парные дельты не созданы из-за Gate 1.

Разбор этого провала показал дефект знаменателя, а не данных: доминирующая причина
`crosses_lung_interior` (2639 из 6529) относится к медиастинальной, диафрагмальной и
междолевой поверхности, где прямой путь наружу бессмыслен по построению. Домен будет
переопределён на костальную часть плевры отдельной заморозкой, см. `D-042`. До этого
51%/53% не интерпретируются как качество измерителя.

Этап 2 также получил предшествующий шаг W1: бюджет неопределённости внутри одной фазы,
без регистрации и без сравнения фаз. Он отвечает на вопрос, различим ли эффект в
принципе, до вложений в соответствие стенки. Протокол заморожен в
`WALL_MEASUREMENT_UNCERTAINTY_W1.md`; выполненный результат — измеритель пригоден для
одного числа по субъекту, но не для 3D-карты, а раздельные жир/мышца упираются
исключительно в HU-сегментацию.

## Этап 3. Baseline перехода вдох→выдох

Зачем: сложная модель имеет смысл только если превосходит простой управляемый сценарий.

Реализовать сначала:

- B0: равномерное масштабирование до целевого `q`;
- B1: раздельные SI/AP/LR деформации верхних/нижних отделов;
- B2: параметры подъёма куполов диафрагмы, движения нижних рёбер и асимметрии лёгких;
- harmonic/RBF распространение на соседние структуры;
- ансамбль по `q` и типу дыхания.

Импедансная кривая задаёт относительную фазу/амплитуду и повторяемость, но не фиксирует `q`.

Gate 3: leave-one-subject/dataset-out оценка; baseline публикует не только среднюю ошибку,
но и покрытие истинной геометрии ансамблем.

## Этап 4. Статистическая и механическая модели

Зачем: проверить, дают ли они реальный выигрыш над B0–B2.

- статистические modes DVF только на истинных respiratory cohorts;
- Li–Porikli-подобный FEM с отдельными лёгкими, диафрагмальной нагрузкой и плевральным
  скольжением;
- одинаковые входы, сценарии и метрики для baseline/statistical/FEM;
- никакой калибровки по скрытому выдоху тестового субъекта.

Gate 4: модель выигрывает у B2 больше измерительного шума на TRE, поверхности в электродной
зоне и/или `ΔZ`.

## Этап 5. Применение к NIX/GEORG/YAROSLAV

Зачем: получить полезный результат при честно сохранённой неопределённости.

- сформировать 5–20 сценариев выдоха на человека;
- использовать impedance trace для выбора плато, относительной амплитуды и repeatability;
- деформировать тело, лёгкие, сердце, кости и электродные патчи согласованно;
- построить FEM для каждого сценария;
- представить диапазон `ΔZ`, а не одну кривую без uncertainty;
- проверить, меняет ли геометрическая неопределённость научный вывод исследования.

## Что делать непосредственно сейчас

1. **Выполнено:** диагностическое сохранение failed fields в отдельный локальный
   quarantine, который profile/measurement pipeline программно не может использовать.
2. **Выполнено:** synthetic representation gate — известные отдельные lung/body fields,
   правильное направление, нормальный контакт, tangential slip, региональный Jacobian и
   два отрицательных контроля. Результат находится в `05_sliding_phantom.ipynb`.
3. **Выполнено:** до expert test зафиксирована спецификация S1 и algorithmic synthetic
   gate в `SLIDING_REGISTRATION_S1.md`. Это ещё не является реализацией S1.
4. **Исторический S1.0 batch superseded:** `sliding-phantom-v2.0` дал PASS у 1/3, но
   последующий разбор обнаружил осевую сингулярность его тангенциального ground truth.
   Старые артефакты сохраняются для воспроизводимости, но не определяют следующий алгоритм.
5. **Выполнено:** axis-safe representation gate PASS; неизменённый S1.0 на frozen
   `sliding-phantom-v2.1-axis-safe` дал PASS у 2/3. Deep case провален по lung endpoint и
   slip; ошибка присутствует во всех трёх z-регионах. Real benchmark заблокирован.
6. **Выполнено:** S1.1 и config заморожены до batch как low-rank tangential model;
   suite v3 добавляет не запускавшийся ранее longitudinal pattern.
7. **Выполнено:** frozen S1.1 реализован и прошёл multipattern suite v3 у 4/4, включая
   заранее не запускавшийся longitudinal challenge. Параметры S1.1 больше не менять.
8. **Выполнено, FAIL:** S1.1 batch на шести development real pairs без expert landmarks
   дал `0/6 PASS`; пять полей имеют lung folding и провал keypoint gates, шестой optimizer
   не завершил frozen objective. Expert Gate 1L не открывался.
9. **Выполнено, failure diagnosis:** на пяти полях переход от initial ConvexAdam к
   normal projection увеличил медиану mean image-derived keypoint TRE `1.70→10.09 мм` и
   folding `0.19%→3.26%`; final six-mode S1.1 дал `7.64 мм` и `3.94%`. Mask Dice при
   этом часто улучшался, поэтому mask agreement не заменяет correspondence/topology.
10. **Выполнено, класс отвергнут:** frozen `sliding_s12_heuristic_screen_v1` на пяти
    сохранённых non-expert fields. Gaussian repair без coupling дал topology 5/5,
    contact 0/5; оба algebraic normal coupling дали contact 5/5, topology 0/5 и общий
    `0/5`. Подбор `sigma/scale/taper` прекращён.
11. **J1.0 и J1.1 выполнены; J1.2 preflight FAIL:** выбран joint piecewise-SVF с отдельными
    lung/body transforms, normal-contact constraint, разрешённым tangential
    discontinuity и сохранением локального correspondence. Замороженный численный gate
    scaling-and-squaring/round-trip/Jacobian прошёл 4/4 cases и 8/8 региональных строк,
    folding отсутствует. Curved representation gate прошёл positive 2/2 и controls 2/2;
    он показал, что fixed-normal equality не является критерием конечного контакта.
    CUDA runner с advected-surface loss и topology rejection реализован и прошёл identity
    smoke. Однако exact truth старого phantom провалил новый contact gate 0/3, поэтому
    optimizer search не запускался. Contact-valid v2 generator прошёл analytic contact
    3/3, но его сетки 1.10–1.25 мм провалили неизменённый raster gate 0/3
    (`0.753–0.848 мм`, coverage `92.2–94.9%`). Optimizer/challenge не запускались.
    Finer-grid v3 exact-truth preflight с теми же радиусами, motion, textures, search и
    порогами прошёл 3/3 (`0.640–0.719 мм`, coverage `97.3–98.3%`). Все checksum
    совпали. Frozen 3×3 development search затем дал `0/9`: contact/topology прошли,
    но estimated tangential slip составил лишь `0.003–0.007 мм` при truth
    `1.327–2.998 мм`; candidate не выбран. Held-out остаётся закрытым. Следующий шаг —
    отдельный identifiability/data-term screen на известных motion modes до новой
    algorithm version, а не добавление weights в просмотренный search.
    Screen truth/estimated/glued/scale-only/zero/inverse и ratio threshold `0.8`
    был заморожен до вычисления и выполнен. Full intensity оказался boundary-dominated,
    interior подтвердил верное направление 3/3; MIND различает counter-rotation, но не
    twist/shallow. Текущий шаг — новая frozen architecture с interior-valid mask и
    tangentially sensitive correspondence/initialization, не продолжение grid search.
12. После возобновлённого J1.2 отдельно улучшить initial correspondence для
    `LungCT_0005`, затем
    заморозить полноценную S1.2 спецификацию и запустить все шесть development subjects
    с нуля, включая `LungCT_0029`.
13. **Параллельный трек стенки, не ожидающий J1.2.** Gate 1 оценивает лёгкое; трудность
    лёгочной регистрации создают плевральное скольжение, экскурсия диафрагмы и паренхима
    без текстуры, ни одно из которых не входит в определение толщины стенки. Порядок:
    (a) **выполнено** W1 — бюджет неопределённости внутри одной фазы. Полная толщина
    `0.211 мм` и мягкие ткани вместе `0.186 мм` — `feasible`; жир `1.029` и мышца
    `1.104 мм` — `marginal` и ограничены только HU-окном; пространственный p95
    `2.626 мм`. По этим числам принято `D-044`: жир и мышца объединены в одну мягкую
    ткань, отчётная форма — распределение `Δh` по телу, а не медиана. `D-045` показал,
    что пространственный шум — выборочная ошибка медианы в неоднородных бинах, а не
    предел измерителя;
    (a2) **выполнено, no selection** W2 — скан «плотность × разрешение» в spine-anchored
    цилиндрической системе координат. Ни одна из девяти конфигураций не прошла
    одновременно шум `≤1.0 мм` и занятость `≥0.9`; ближайшая `24×8` при `2.5 мм` дала
    `1.030`/`1.096 мм` и `0.85`/`0.92`. Более грубое `12×4` оказалось хуже более мелкого
    `24×8`, что подтверждает `D-045`. Пустые бины структурно сосредоточены в подмышке и
    костодиафрагмальном синусе, поэтому `D-046` выносит (b) вперёд;
    (b) **следующий шаг** — переопределить домен Gate 2 на костальную поверхность
    плевры, см. `D-042` и `D-046`; без этого новый скан измерит те же структурно
    недостижимые бины, поэтому наращивать плотность раньше бессмысленно;
    (b2) заморозить W3 — повторный скан с более плотным сэмплированием и критерием
    занятости, учитывающим структурно недостижимые бины; пороги шума переносятся без
    изменений;
    (c) зафиксировать численные пороги Gate 1B — наружный контур, устойчивые костные
    ориентиры, рёберно-привязанное соответствие **в системе координат W2**,
    segmentation repeatability и валидность профилей в общем FOV, с порогом
    повторяемости из W1. До (c) 1B остаётся `NOT VALIDATED`, парные дельты
    заблокированы, карта `Δh` не вычисляется.
14. Параллельно улучшить outer-pleura/FOV QC профилей до заранее заданного покрытия, но не
   вычислять paired deltas.
15. После Gate 1L строить парные карты формы лёгких; только после 1L+1B — парные карты
   полной толщины и компонентов ткани.
16. По проверенным парным изменениям обучать отдельную single-CT модель, которая выдаёт
   ансамбль возможных выдохов, а не использует скрытый выдох тестового субъекта.
