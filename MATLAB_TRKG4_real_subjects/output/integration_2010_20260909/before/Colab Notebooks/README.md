# Colab Notebooks — канонический аналитический корпус

> **Статус на 28.08.2026:** структурная миграция серий `00–60` для
> экспериментов 2–3 выполнена; серия `70` изолирована и отложена. Строгие
> реальные расчёты остаются заблокированы ручным QC, калибровками, КТ/FEM и
> неопределённостью. Отдельный исследовательский контур выполнен на доступных
> данных и не создаёт принятых физиологических результатов. Работа остаётся
> разработкой методики, а не завершённой физической, экспериментальной или
> клинической валидацией.

## С чего начинать

1. [00.00 — карта проекта](00.00_Карта_проекта.md) — цель, три эксперимента,
   последовательность исследования и порядок чтения.
2. [00.01 — методологический контракт](00.01_Методологический_контракт.md) —
   цепочка от измерений к количественным показателям и критерии продолжения.
3. [00.02 — реестр доказательств и ограничений](00.02_Реестр_доказательств_и_ограничений.md) —
   статусы утверждений, допущения и открытые вопросы.
4. [00.03 — контракт исследовательского прогона](00.03_Контракт_исследовательского_прогона.md) —
   правила расчёта с кандидатными входами без подмены `accepted`.
5. [План миграции](ПЛАН_РЕОРГАНИЗАЦИИ_И_ПЕРЕИМЕНОВАНИЯ.md) — утверждённое
   соответствие старых и новых путей, входов и выходов.

Исходное состояние перед переименованием зафиксировано коммитом
`1ac7813654b59861e359cffe3ad40941baeab2d9` и машинным
[манифестом](MIGRATION_MANIFEST_2026-08-26.json).

## Три эксперимента

| Эксперимент | Площадка и прибор | Зарегистрированные сигналы | Активная серия | Ограничение переноса |
|---|---|---|---|---|
| 1 — `kandidatskaya` | МГТУ, РЕО32 | Каналы 5 и 9 крестом над областью сердца, канал 7 мягких тканей, канал 13 правого лёгкого, ЭКГ и стандартная ТРКГ | `70` | Другие электроды и модель; не переносить в эксперименты 2–3 |
| 2 | МГТУ, реокардиомонитор МГТУ | ЭКГ, ТТРКГ и несколько размеров боковых сборок | `10.00–10.02`, `11.01–11.02`, `12.01`, `33`, `40.01–40.04` | Записи размеров последовательны; межзаписная изменчивость, межканальное влияние и пригодность конкретных каналов учитываются адресно |
| 3 | РНЦХ, реокардиомонитор РНЦХ | ЭКГ, ТТРКГ и боковая сборка 140 мм по схеме МГТУ | `10.10–10.11`, `11.11–11.12`, `40.11–40.14` | Другой прибор и другая сессия; численные поправки МГТУ автоматически не переносить |

РЕО32 не является ревизией реокардиомонитора. ТТРКГ экспериментов 2–3 не
обозначает стандартную ТРКГ эксперимента 1.

## Карта серий

| Серия | Назначение | Текущий структурный статус |
|---|---|---|
| `00` | Карта проекта, методологический контракт, реестр доказательств, правила исследовательского прогона | Созданы новые канонические документы |
| `10` | Паспорта, происхождение данных, первичный файловый QC и приборные вопросы экспериментов 2–3 | `10.01` ограничен проверкой файлов до физиологической разметки; метрологические гипотезы отделены от QC |
| `11` | Единая разметка дыхания и ЭКГ | Созданы независимые ноутбуки для экспериментов 2 и 3; требуется прогон на разрешённых внешних данных и ручной QC |
| `12` | Сводный контроль сигналов после принятия файлов и физиологических интервалов | `12.01` создан переносом блоков из прежнего `10.01`; до отдельной ревизии и принятых входов не является источником численных результатов |
| `20` | DICOM, Inobitec, регистрация электродов и КТ/FEM | DICOM-QC отделён; 20.02–20.04 остаются заблокированными контрактами; 20.07 фиксирует статический прогон TotalSegmentator для костей, лёгких, тела, мышц, жира и сердца со статусом `pending_manual_review`; 20.10 регистрирует требующий пересчёта исторический FEM-кандидат; 20.11 отделяет локальный Якобиан inverse от прямой чувствительности; 20.12 задаёт индивидуальный критерий $L_{max}$ по ошибке КТ/FEM ↔ плоская модель, без численной границы |
| `21` | Автоматическая сегментация 4D-КТ и анализ динамики сердца | TotalSegmentator 2.18.0 проверен на 39 фазах Adam, Nix и Georg; маски всего сердца, камер и модельного миокарда имеют статус `pending_manual_review`. Выявлены неполнота миокарда, межмодельное рассогласование, межфазные скачки объёма и неподтверждённая шкала R–R; количественные функции сердца не рассчитываются |
| `30` | Прямая и обратная двуслойная модель | `30.01` — формула; `30.02` — статическая инверсия; `30.03` — динамика; `two_layer_model.py`/`30.04` — проверенное ядро; `30.05` — только доказуемые масштабные законы без субъектных выводов; `33.01–33.05` разделены и синтетически проверены |
| `31` | Чувствительность, неопределённость и идентифицируемость | `31.00` — договор; `31.01` — локальные производные; `31.02` — сценарный анализ Соболя; `31.03` — структурная идентифицируемость без экспериментального CRLB; `31.04` — раздельный реестр погрешностей без фиктивной общей $\sigma$ |
| `32` | Выбор размеров боковых сборок | `32.01` — условные критерии без принятого $L_{min}$; `32.02` — синтетический перебор 36/45 пар при известной $h$; `32.03` — перебор 466/968 наборов при неизвестной $h$; реальные пара и минимальный набор не выбраны без калибровки, ковариации, КТ-проверки и holdout |
| `33` | Статические и пульсовые оценки боковых сборок | Пять строгих задач разделены и имеют синтетические самопроверки; `33.06` отдельно прогнан на 19 реальных записях как `exploratory_hypothesis_not_validated`. Профиль `h` у обоих добровольцев не имеет локальных минимумов и достигает наименьшей ошибки при `h=1 мм`, на нижней границе вычислительной сетки; это отрицательный диагностический результат, поэтому анатомическая оценка `h` не получена. В соседнем `33.07` для каждого испытуемого совместно оцениваются четыре общих параметра: $h$, $\rho_1$, $\rho_{2,\mathrm{вдох}}$ и $\rho_{2,\mathrm{выдох}}$. Подбор ограничен предварительными рабочими диапазонами и проверен профилем по $h$, многократными стартами и исключением одной сборки. В основном сценарии оба минимума достигаются при `h=10 мм` на нижней границе; у Георга $\rho_{2,\mathrm{вдох}}=25$ Ом·м также находится на границе, а у Ника результат чувствителен к записи 50 мм. Поэтому устойчивое внутреннее решение не подтверждено. В дополнительном сценарии $h$ фиксируется по историческому КТ-референсу: 15 мм для Ника и 40 мм для Георга. У Ника получены внутренние оценки $\rho_1=4{,}749$, $\rho_{2,\mathrm{вдох}}=17{,}123$ и $\rho_{2,\mathrm{выдох}}=14{,}273$ Ом·м; у Георга обе оценки $\rho_2$ достигают верхней границы даже после её последовательного расширения. Этот результат показывает несогласованность КТ-референса, текущей двуслойной модели и записанных импедансов, но не опровергает КТ-толщину. Точное обращение отдельного $Z$ в $\rho_{2,i}$ сохранено только как диагностика межсборочной согласованности, а не как итоговая параметризация. В `33.01` воспроизведена историческая статическая подгонка при ручных КТ-значениях `h=15 мм` и `h=40 мм`; в `33.04` отдельно реализованы новая МНК-инверсия и совместимость со старым методом попарных пересечений с КТ/HU-подстановкой $\rho_2$. Воспроизведение прошло численную регрессионную проверку, но происхождение `h`, преобразование HU → $\rho_2$ и адресные приборные ограничения повторно не валидированы; `BASE/RHEO` при этом принимаются измеренными выходами в заявленных единицах |
| `34` | Проверка двуслойной ветки | Контракт, внутренняя проверка, КТ/FEM-референс, пульсовая проверка и матрица доказательности разделены; строгая реальная ветка блокируется непринятыми входами, `h`, неопределённостью и внешней проверкой модели |
| `40` | ТТРКГ и перенос тканевых вкладов | `40.01` и `40.11` формируют кандидатные ансамбли ТТРКГ экспериментов 2 и 3; `40.12` описывает межканальное влияние прибора РНЦХ. `40.02` сохраняет исторический тест монтажа на руках; его четырёхплоскостная схема отменена уточнением автора от 08.09.2026. `40.05` сравнивает исправленный смешанный монтаж и три поверхностных варианта при изменении проводимостей тканей (16 × 27 состояний). `40.03` отвечает за общий выбор координат электродов ТТРКГ на наружной поверхности тела; физический монтаж ещё требует авторской разметки и повторного FEM-контроля. `40.04/40.14` задают протокол чувствительности, а `40.22` сохраняет исследовательские сценарии переноса без FEM |
| `50` | Идентифицируемость источников ТТРКГ | Строгие приборные go/no-go ждут FEM. Сценарная проверка `40.22` показала ранг 1 для одного ТТРКГ-канала и не более 2 после добавления одной боковой сборки при трёх произвольных источниках |
| `60` | Контракты УО, ФВ и внешней валидации | Строгий DAG не пройден. `60.04` создал только таблицу зависимости сферы, условных УО и ФВ от неизвестного объёмного оператора; разрешённых статусов `60.01/60.02` не создано |
| `70` | Изолированная ветка эксперимента 1 на РЕО32 | Исторический код сохранён; ветка отложена и не входила в текущую переработку |

Файл-каркас имеет явный статус `структурный каркас` или метаданные
`skeleton_unimplemented`. Его нельзя запускать или цитировать как выполненный
расчёт.

## Основная последовательность для экспериментов 2–3

```text
10  паспорт, первичный файловый QC и приборные вопросы
 ├─► 11  дыхательная и ЭКГ-разметка
 │     └─► 12  сводный QC сигналов на принятых интервалах
 │            ├─► 30–32  двуслойная модель, определимость и выбор размеров
 │            └─► 33  оценки по боковым сборкам эксперимента 2
 │                   └─► 40  обработка ТТРКГ и перенос тканевых вкладов ─┐
 └─► 20  КТ / Inobitec / геометрия / FEM ───────────────────────────────┴─► 50
                                                                    └─► 60  УО/ФВ только после go/no-go и внешней проверки
```

Схема показывает зависимости аналитических документов, а не хронологию
регистрации. КТ на вдохе и ручная сегментация Inobitec выполнялись до
регистрации физиологических сигналов.

Серия `21` ответвляется от КТ-ветки `20` и проверяет возможность автоматического
получения динамической геометрии. Её выходы остаются кандидатными до сравнения
с ручной сегментацией Inobitec по объёмным и поверхностным метрикам. Поэтому
они не передаются в строгие контракты серий `50–60` как подтверждённые
анатомические входы.

КТ на этапе разработки нужна для реальной анатомии, ручной сегментации,
проверки геометрии и FEM. В целевой сокращённой методике индивидуальная КТ не
должна оставаться обязательным входом без отдельного обоснования.

## Данные, конфигурация и производные артефакты

- Первичные КТ, ЭКГ, РЕО, ТТРКГ и другие физиологические записи находятся вне
  Git и могут содержать медицинские или персональные данные.
- Локальные пути задаются через шаблоны в [config](config/README.md). Файлы
  `*.local.json` исключены из Git.
- Новые разметки серии `11` записываются во внешний `derived_root`, раздельно
  для дыхания и ЭКГ. Статус `accepted` устанавливается только после ручного QC.
  Временных меток дыхательных команд нет ни в одном эксперименте: интервалы
  восстанавливаются по памяти автора и форме сигнала и остаются экспертной
  ретроспективной разметкой даже после принятия.
- Исторические субъектно-связанные `timestamps/*.json` и `params/*.json`
  исключены из индекса Git и добавлены в `.gitignore`. Локальные копии не
  удалены; их происхождение сохраняется в истории Git. Канонические новые
  артефакты создаются только во внешнем `derived_root` по схемам из `schemas/`.
- Сохранённые notebook outputs не считать результатом без входных хешей,
  единиц, калибровки, версии кода и статуса проверки.

## HTML-версии ноутбуков

HTML создаётся как читательская версия научного ноутбука: в нём сохраняются
текст, формулы, таблицы, графики и расчётные результаты, но исходный код
обязательно исключается из HTML-разметки. Простое визуальное скрытие кода не
допускается. Если пересчёт отдельно не запрошен, экспорт выполняется без
запуска ячеек. Каноническая команда и порядок проверки зафиксированы в
[локальных правилах](AGENTS.md#html-экспорт-ноутбуков).

## Исследовательский прогон 28.08.2026

Отдельный контур по правилам `00.03` не меняет статусы входов и пишет результаты
только во внешний `derived_root`:

- `exp02/exploratory/33.06_side_arrays_exploratory.json` — 19 записей боковых
  сборок, профиль `h`, условная статическая подгонка и пульсовая инверсия;
- `exp02/exploratory/33.07_joint_bounded_two_layer_solution.json` — совместная
  ограниченная оценка общих $h$, $\rho_1$, $\rho_{2,\mathrm{вдох}}$ и
  $\rho_{2,\mathrm{выдох}}$ для каждого испытуемого, профили по $h$, проверка
  исключением одной сборки и вспомогательная диагностика межсборочного разброса
  точно обращённых $\rho_{2,i}$.
- `cross_experiment/exploratory/40.22_ttrkg_transfer_identifiability_exploratory.json`
  — ТТРКГ экспериментов 2–3, тест переключения и сценарии переноса/ранга;
- `cross_experiment/exploratory/60.04_equivalent_sphere_sv_ef_scenarios.json`
  — 1890 алгебраических сценариев, из которых ни один не является строгой
  оценкой УО или ФВ.

В Git хранятся код, описание контракта, обезличенные агрегированные таблицы и
графики последнего исследовательского прогона в `33.06`, `33.07`, `40.22` и
`60.04`. Сырые
сигналы, идентификаторы, хэши и абсолютные локальные пути в notebook outputs не
сохраняются. Полные внешние артефакты могут быть воспроизведены только при
доступе к локальным медицинским данным и исключённым из Git конфигурациям.

## Архив и происхождение

[archive/legacy](archive/legacy/README.md) содержит перемещённые исторические
файлы и санифицированные указатели на объединённые прототипы. Полные приватные
снимки смешанных файлов остаются локально вне Git. Активные расчёты не должны
читать архивные файлы.
Архивные числа и выводы не переносятся автоматически в новую доказательную
цепочку.

Исторический [аудит от 23.08.2026](AUDIT_2026-08-23.md) не переписывается под
новое состояние. Технический результат текущей миграции зафиксирован в
[аудите от 27.08.2026](AUDIT_2026-08-27.md). Подробный разбор старых файлов сохранён в
[карте ноутбуков](КАРТА_НОУТБУКОВ_И_ЗАВИСИМОСТЕЙ.md).

## Служебные документы

| Путь | Назначение |
|---|---|
| [AGENTS.md](AGENTS.md) | Локальные правила научного статуса, нумерации, приватности и литературы |
| [ПРАВИЛА_ОПИСАНИЯ_РАСЧЁТОВ.md](ПРАВИЛА_ОПИСАНИЯ_РАСЧЁТОВ.md) | Требования к входам, единицам, допущениям, неопределённости и проверкам |
| [TODO.md](TODO.md) | Действующий backlog внешних входов, реальных прогонов и проверок |
| [КАРТА_НОУТБУКОВ_И_ЗАВИСИМОСТЕЙ.md](КАРТА_НОУТБУКОВ_И_ЗАВИСИМОСТЕЙ.md) | Историческая содержательная карта старого корпуса |
| [ПЛАН_РЕОРГАНИЗАЦИИ_И_ПЕРЕИМЕНОВАНИЯ.md](ПЛАН_РЕОРГАНИЗАЦИИ_И_ПЕРЕИМЕНОВАНИЯ.md) | Утверждённый контракт миграции `старое → новое` |

COMSOL-файлы, оставшиеся непосредственно в этой папке, являются историческими
probe/runner-артефактами. Канонические активные результаты FEM для реальных
испытуемых искать в `MATLAB_TRKG4_real_subjects/` и проверять по конкретной
карточке запуска; само наличие кода не подтверждает воспроизводимость.


Параметрическое сравнение контактов `40.02` продолжено в
[40.05 — методика оценки чувствительности: проверка предположений и настройка расчёта](40.05_Чувствительность_ТТРКГ_к_проводимости_тканей.ipynb).
По уточнению автора от 08.09.2026 плоскостными могут быть только наружные
токовые электроды; внутренние в этой схеме являются манжетами 5 мм.
Четырёхплоскостная ветка старого расчёта не соответствует уточнённой
постановке. Новая серия сравнивает 16 монтажей на полной сетке из
27 сочетаний проводимостей мягкотканного фона, цельной сердечной области
и лёгких при фиксированной КТ-геометрии; статус — вычислительный эксперимент
разработки методики.
