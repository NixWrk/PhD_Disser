"""Scientific narrative for the expanded-range calculation in notebook 33.09."""


def result_sections(md, code):
    md(r'''### 5.1. Повторный расчёт в расширенном диапазоне

Во всех ограниченных подгонках рисунка 2 значение ρ₂ достигло верхней границы. Поэтому нужно проверить, обусловлено ли отсутствие общего решения именно этой границей. Для этого выполнен **новый прямой расчёт CEM** при **ρ₁ от 0,1 до 20 Ом·м и ρ₂ от 0,1 до 100 Ом·м**. Изменены сопротивления; геометрия модели 4, монтаж C01, контакты диаметром 5 мм и экспериментальные наблюдения сохранены. Объёмные и контактные матрицы повторно использованы, поскольку они описывают ту же дискретную геометрию; линейные системы с новой проводимостью решены заново.

Расширение является **диагностическим вычислительным опытом**. Оно позволяет увидеть продолжение кривых решений и проверить влияние границ поиска, но не расширяет физиологически обоснованную область. Литературные интервалы раздела 1 сохраняются. Даже попадание оценки в эти интервалы означает лишь совместимость с принятыми границами, а не независимое подтверждение истинных свойств тканей.

Основная новая библиотека содержит **13 × 17 = 221 сочетание сопротивлений**, размещённых равномерно по логарифмам. В каждом состоянии рассчитаны девять размеров. Ещё 16 сочетаний между узлами служат для проверки интерполяции; две прежние точки повторены для проверки совпадения нового запуска с исходным решателем. Значение **ρ = 0 не рассчитывается**: проводимость в этой постановке равна 1/ρ. Нулевая отметка используется только как начало шкал; нижняя расчётная граница равна 0,1 Ом·м.

Для построения кривых между новыми узлами интерполируется нормированный отклик Z/ρ₁ по логарифмам обоих сопротивлений, затем восстанавливается Z. Нормирование выделяет основной масштаб зависимости импеданса от сопротивления; контактное сопротивление остаётся фиксированным и учитывается в исходных решениях CEM. Сплайн не используется за пределами новой библиотеки. Его точность проверяется на 16 дополнительных состояниях, на прежних 49 узлах, а затем прямым FEM в найденных оценках и контрольных точках кривых. Для каждого проверяемого импеданса заранее принято численное условие |ΔZ| ≤ 0,02 Ом + 0,001|Z_FEM|. Это допуск на вычислительное приближение, а не измеренный шум прибора и не погрешность тканевых оценок. Эта проверка не заменяет исследование сходимости объёмной сетки.

Подгонка повторена для тех же лучших наборов из двух–девяти размеров. **Состав наборов сохранён из таблицы 2:** он выбран по чувствительности в литературном диапазоне. Оптимизация состава сборок в расширенной диагностической области не выполнялась. При поиске сопротивлений использованы несколько начальных приближений; это уменьшает зависимость от старта, но не является доказательством глобальной единственности.''')
    code('''interpolation_range=json.loads((range_out/'interpolation_qc.json').read_text(encoding='utf-8'))
direct_range=json.loads((range_out/'direct_check_qc.json').read_text(encoding='utf-8'))
assert interpolation_range['passed'] and direct_range['complete'] and direct_range['passed']
paragraph(f"**Численная проверка расширения.** Максимальное расхождение сплайна с FEM на 16 новых проверочных состояниях — "
 f"{num(interpolation_range['new_validation']['Z']['max_abs_error_ohm'],5)} Ом, "
 f"на 49 прежних состояниях — {num(interpolation_range['old_base_grid_additional_check']['Z']['max_abs_error_ohm'],5)} Ом. "
 f"Число дополнительно рассчитанных импедансов — {direct_range['rows']}: "
 f"{direct_range['fit_points']} найденных пар сопротивлений, каждая проверена по девяти размерам, и {direct_range['curve_points']} точек отдельных кривых. "
 f"Максимальное расхождение с FEM здесь — {num(direct_range['max_spline_vs_fem_Z_error_ohm'],5)} Ом; "
 f"заданный численный допуск выполнен. Максимальное изменение среднеквадратической невязки (RMSE) выбранных измерений при проверке прямым FEM составляет {num(direct_range['max_selected_rmse_change_ohm'],8)} Ом. "
 "Это проверка интерполяции в рассчитанных точках, а не гарантия для каждой точки непрерывной области.")
''')
    md(r'''**Таблица 4а. Изменение подгонки после расширения диапазона сопротивлений.** Приведены пара, тройка, четвёрка и полный набор. RMSE характеризует невязку тех измерений, которые вошли в подгонку; ошибка прогноза рассчитана по остальным размерам. Столбец «В литературной области» проверяет одновременно оба сопротивления. Значения вне неё не принимаются как физиологический результат.''')
    code('''extended=pd.read_csv(range_out/'real_fits.csv')
assert len(extended)==16 and extended.success.all()
small=extended[extended.k.isin([2,3,4,9])].copy()
old=real.set_index(['state','k'])
table(pd.DataFrame({'Размеров':small.k,'Состояние':small.state.map(state_names),
 'ρ₁, Ом·м':small.rho1_hat,'ρ₂, Ом·м':small.rho2_hat,
 'RMSE прежде, Ом':[old.loc[(r.state,r.k),'fit_resid_rmse_ohm'] for r in small.itertuples()],
 'RMSE после, Ом':small.fit_resid_rmse_ohm,
 'Прогноз исключённых, Ом':small.heldout_rmse_ohm.map(lambda x:num(x) if pd.notna(x) else 'нет проверки'),
 'В литературной области':small.within_literature_bounds.map({True:'да',False:'нет'}),
 'Граница расширенного поиска':small.apply(edge_label,axis=1)}))
for state in ['inhale','exhale']:
 r=extended[(extended.state==state)&(extended.k==9)].iloc[0]
 paragraph(f"**Полный набор, {state_names[state].lower()}:** ρ₁ = {num(r.rho1_hat,4)}, "
  f"ρ₂ = {num(r.rho2_hat,4)} Ом·м; RMSE = {num(r.fit_resid_rmse_ohm,4)} Ом. "
  f"Положение относительно расширенных границ: {edge_label(r)}. "
  f"Совместимость с литературными границами обоих параметров: {'да' if r.within_literature_bounds else 'нет'}.")
''')
    code('''above_lung=int((extended.rho2_hat>range_plan['literature_bounds_ohm_m'][1][1]).sum())
upper_lung=int(extended.bounds_hit_rho2_upper.sum())
paragraph(f"**Результат расширения:** в {above_lung} из {len(extended)} подгонок ρ₂ оказалось выше литературной верхней границы; "
 f"{upper_lung} оценок достигли новой границы {num(range_plan['bounds_ohm_m'][1][1],0)} Ом·м. "
 "Таким образом, снятие прежнего ограничения не дало физиологически приемлемого восстановления в принятом сценарии. "
 "Новая внутренняя точка минимума на выдохе также находится вне литературного диапазона.")
''')
    md(r'''Уменьшение RMSE при расширении допустимой области ожидаемо: оптимизатор получает больше возможных решений. Само по себе оно не доказывает улучшение оценки тканей. Для пары два уравнения иногда могут согласоваться точно при двух неизвестных, однако остальные размеры остаются независимой проверкой такого согласования. Чтобы увидеть, какие измерения могут иметь общие параметры, рассмотрим кривые решений каждой сборки.

При фиксированном ρ₂ для каждого размера L решается уравнение

$$Z_L\bigl(\rho_{1,L}(\rho_2),\rho_2\bigr)=Z_{L,\mathrm{набл}},$$

где $Z_L$ — рассчитанный передаточный импеданс сборки размера L, Ом; $Z_{L,\mathrm{набл}}$ — её наблюдаемый базовый импеданс, Ом; $\rho_{1,L}(\rho_2)$ — требуемое удельное сопротивление мягких тканей при заданном удельном сопротивлении лёгкого, Ом·м.

Каждая точка кривой — пара сопротивлений, воспроизводящая одно наблюдение в принятой модели с точностью поиска корня интерполированного отклика. График построен на логарифмической сетке из 601 значения ρ₂. При каждом из них интервал положительного ρ₁ проверяется по 401 узлу с последующим уточнением найденных смен знака. Поиск остаётся внутри нового диапазона. Участки без найденного корня не соединяются через границу. Численная проверка ищет ветви и пересечения; она не доказывает отсутствие изолированных касаний между узлами. **Для точного общего решения все выбранные кривые должны проходить через одну точку.** Пересечение только двух кривых ещё не означает согласования остальных измерений.''')
    code('''sys.path.insert(0,str(pipe/'tools'))
from lateral_resistivity_range_plots import make_figure, make_optimal_figure
fig=make_figure(range_out)
display(HTML(fig.to_html(full_html=False,include_plotlyjs='inline',config={'responsive':True,'displaylogo':False})))
''')
    md(r'''**Рисунок 3. Кривые решений девяти сборок после нового расчёта в расширенном диапазоне.** Слева показан вдох, справа — выдох. По горизонтали отложено ρ₂ лёгкого, по вертикали — требуемое ρ₁ мягких тканей; единицы — Ом·м. Цвет обозначает размер сборки и сохраняется на обеих панелях. Светло-зелёный прямоугольник со штриховой границей показывает литературную рабочую область; серый пунктир ограничивает новый расчётный диапазон. Круги отмечают найденные пересечения пар, чёрный ромб — совместную подгонку всех девяти наблюдений. Нулевая отметка осей не является рассчитанным значением сопротивления.

Сначала следует проверить, сходятся ли все кривые, затем — попадает ли предполагаемое решение в зелёную область. Кнопка «Кривые крупнее» увеличивает вертикальный масштаб, «Литературная область» возвращает прежние границы просмотра, «Весь диапазон» показывает полную новую область. Наведение раскрывает координаты, размеры пересекающихся сборок и невязку подгонки. Щелчок по легенде переключает один размер на обеих панелях.

Различие наклонов означает, что сборки по-разному связывают изменения двух сопротивлений; это полезно для их разделения. Однако чувствительность сама по себе не устраняет несовпадение кривых. При ненулевой общей невязке ромб является компромиссной оценкой. Без установленной погрешности отдельных наблюдений нельзя объявить произвольную близость кривых достаточным совпадением или построить обоснованный доверительный интервал.''')
    code('''curve_table=pd.read_csv(range_out/'solution_curves.csv')
crossings=pd.read_csv(range_out/'solution_curve_intersections.csv')
for state in ['inhale','exhale']:
 p=crossings[crossings.state==state]
 a,b=range_plan['literature_bounds_ohm_m']
 inside=p.rho1.between(*a)&p.rho2.between(*b)
 paragraph(f"**{state_names[state]}:** найдено {len(p)} пересечений пар, "
  f"из них {int(inside.sum())} внутри литературной области. "
  "Это число попарных пересечений, а не число общих решений всех девяти измерений.")
paragraph(f"Максимальная невязка поиска корня интерполированного уравнения: "
 f"{curve_table.residual_ohm.abs().max():.2e} Ом. Расхождение с прямым FEM проверено отдельно; "
 "эта малая невязка не является оценкой точности физических параметров.")
''')
    md(r'''### 5.2. Расширенные кривые для лучших наборов из двух–девяти сборок

Общий график показывает согласованность всего опыта. Следующий график выделяет наборы, предложенные для сокращённого протокола: **50 и 140 мм**; **50, 130 и 140 мм**; **50, 120, 130 и 140 мм**, затем лучшие наборы из пяти–девяти размеров. Индивидуальные кривые не меняются при переключении: меняются состав измерений, учитываемые пересечения и совместная подгонка. Слово «лучший» относится к критерию чувствительности раздела 2 в прежнем литературном диапазоне, а не к минимальной ошибке реальных наблюдений.''')
    code('''fig=make_optimal_figure(range_out)
display(HTML(fig.to_html(full_html=False,include_plotlyjs='inline',config={'responsive':True,'displaylogo':False})))
''')
    md(r'''**Рисунок 4. Кривые решений лучших наборов в расширенном диапазоне.** Меню справа выбирает число размеров от двух до девяти; их состав указан в заголовке. Начальное представление — пара 50/140 мм. Панели, цвета, границы и кнопки масштаба имеют тот же смысл, что на рисунке 3. Чёрный ромб показывает подгонку именно выбранного набора. Круги относятся только к парам внутри него. Отдельные интерактивные страницы для каждого набора сохранены рядом с расчётными таблицами.

Нужно последовательно проверить три условия: согласуются ли кривые выбранных сборок; лежит ли оценка в литературной области; воспроизводит ли она исключённые измерения. Положительный ответ только на первое условие ещё не делает сокращённый протокол пригодным для восстановления тканевых свойств. Расширение диапазона проверяет роль прежней границы поиска, но сохраняет ограничения контактов, двухтканного состава и единой геометрии для вдоха и выдоха.

Сопоставим теперь численно кривые пары, которая входит во все выбранные наборы.''')

    code('''for state in ['inhale','exhale']:
 pair=crossings[(crossings.state==state)&(crossings.L1_mm==50)&(crossings.L2_mm==140)]
 assert pair.empty
 spread=range_curves_qc['minimum_spreads_by_state_and_k'][state]['2']
 r=extended[(extended.state==state)&(extended.k==2)].iloc[0]
 paragraph(f"**Пара 50/140 мм, {state_names[state].lower()}:** пересечение кривых не найдено. "
  f"Минимальный разрыв по ρ₁ на 601 проверенном значении ρ₂ — {num(spread['minimum_rho1_spread_ohm_m'],4)} Ом·м "
  f"при ρ₂ = {num(spread['rho2_at_minimum'],1)} Ом·м. "
  f"RMSE собственной подгонки — {num(r.fit_resid_rmse_ohm,3)} Ом, "
  f"прогноза остальных размеров — {num(r.heldout_rmse_ohm,3)} Ом.")
''')
    md(r'''**Вывод по кривым:** общего решения не найдено и после расширения диапазона. Пара 50/140 мм входит во все выбранные наборы, поэтому её несогласованность сохраняется и при добавлении размеров. На выдохе кривые этой пары сближаются при больших ρ₂, но её прогноз остальных измерений остаётся существенно хуже собственной подгонки. Это пример того, почему близость двух кривых ещё не подтверждает достаточность двух сборок.

Приведённый разрыв является разностью двух модельных решений на сетке, а не ошибкой истинного ρ₁ и не доверительным интервалом. Численная проверка не исключает изолированных касаний между проверенными значениями. Для вывода о физиологически допустимых сопротивлениях прежние литературные границы сохраняются; диагностические оценки вне них не принимаются как свойства тканей человека. Следующий раздел возвращается к критерию числа сборок в литературной области; расширенные диагностические значения в этот критерий не подставляются.''')
