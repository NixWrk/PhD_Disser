# Единый MATLAB/Python-pipeline для notebook-отчётов

## Назначение

Папка `MATLAB_TRKG4_real_subjects/` является единственным активным источником
MATLAB-расчётов для notebook-файлов проекта. Notebook-файлы не должны
воспроизводить FEM-решатель внутри Python: они читают явно именованные
артефакты из `output/`, проверяют контракт и оформляют расчёт как отчёт.

Текущий статус всей цепочки — разработка методики, а не завершённая физическая
или клиническая валидация.

## Точка входа

Из корня папки MATLAB:

```matlab
report = run_pipeline('Profile', 'preflight');
report = run_pipeline('Profile', 'inverse_report');
```

Профиль `inverse_report` последовательно выполняет:

1. проверку MATLAB/EIDORS/Gmsh и обязательных входов;
2. модульные тесты;
3. расчёт двух валидных боковых электродных систем на одной сетке;
4. обратную задачу по многоразмерной серии на вдохе;
5. контроль пересечения площадок именно найденного кандидата.

Исторические PEM/CEM-сканы вынесены в отдельный явный профиль
`historical_pose_screening`; они не требуются для основного отчёта.

По умолчанию результаты пересчитываются. Использование уже существующих
файлов разрешается только явно:

```matlab
report = run_pipeline('Profile', 'inverse_report', ...
    'ReuseExistingResults', true);
```

При каждом запуске создаются CSV- и JSON-манифесты стадий. Они фиксируют
исполнителя расчёта, статус, длительность и ожидаемые артефакты, но не заменяют
provenance входных медицинских данных и commit SHA.

## Карта MATLAB → ipynb

| Потребитель | Производитель | Основные артефакты |
|---|---|---|
| `../Colab Notebooks/20.10_КТ_FEM_локализация_боковых_матриц_МГТУ.ipynb` | `run_trkg4_electrode_systems` | `nik_trkg4_right_rib_050mm_full_v5_1mm_local2mm.mat`, сводка боковых систем |
| тот же отчёт | `run_trkg4_inverse_inhale` | comparison, fit summary, depth curve, electrode/grid geometry, Jacobian, parameter sensitivity, history |
| необязательный исторический раздел | `run_trkg4_fast_pem_scan` | PEM all/top/validation/summary |
| необязательный исторический раздел | `run_trkg4_fast_cem_refine` | CEM candidates/summary |
| тот же отчёт | `check_trkg4_patch_overlap_v5` | `nik_trkg4_patch_overlap_<tag>.csv` и JSON происхождения |
| `Colab Notebooks/40.02_...ipynb` | `run_trkg4_arm_electrode_tests` | сводка монтажа на руках, патчи, контракт, MAT и PNG |
| `Colab Notebooks/40.03_...ipynb` | `build_surface_landmark_picker.py` и `trkg4_load_surface_landmarks` | интерактивный HTML, контракт координат и геометрическая проверка перед FEM |
| `Colab Notebooks/40.05_...ipynb` | `run_pipeline('Profile','arm_conductivity_matrix')` | 16 × 27 состояний, геометрия контактов, производные, численный QC и паспорт в `output/exploratory/arm_sigma_20260908` |

Генератор `notebooks/build_trkg4_inverse_report.py` создаёт структуру
отчётного ipynb, но не выполняет MATLAB-расчёт. Основной набор выбирается
параметром `ResultTag`; исторические сравнения необязательны. Их отсутствие
не должно препятствовать чтению основного результата.

## Контракт после аудита 08.09.2026

Исправленный запуск задаёт единый тег на всех основных стадиях:

```matlab
accepted_start_points = readmatrix( ...
    'notebooks/audit_fixes_2026_09_08/admissible_starts.csv');
report = run_pipeline('Profile', 'inverse_report', ...
    'ResultTag', 'auditfix_20260908', ...
    'InitialPoints', accepted_start_points, ...
    'MaxInverseEvaluations', 30, 'ComputeJacobian', true);
```

`accepted_start_points` — матрица N×5 с параметрами
`[rho_soft, rho_lungs, u_mm, v_mm, phi_deg]`; допустимость площадок проверяется
для всех девяти размеров до первого FEM-решения. Пример имени тега не означает,
что такой расчёт уже завершён. Для повторного запуска используется тот же тег;
для сохранения исторических результатов следует выбрать новый.

Генератор и исполнение ноутбука читают `TRKG4_RESULT_TAG`. По умолчанию и MATLAB,
и notebook используют `auditfix_20260908`. Наличие старого тега
не подтверждает прохождения добавленных проверок: старые файлы без контракта
показываются только с явным историческим статусом.

У результата всегда перезаписываются comparison, fit summary, depth curve,
electrode/grid geometry, history, Jacobian, parameter sensitivity и JSON
диагностики. При `ComputeJacobian=false` чувствительность содержит NaN и статус
`not_computed`, поэтому старые оценки не переживают новый расчёт. Числа из
крупношаговых секущих не интерпретируются как информация Фишера о позе.
Вычисленная диагностика включает проверку на половинном шаге; шумовая модель
не считается установленной, CRLB не выдаётся.

Последним публикуется `nik_trkg4_inverse_inhale_contract_<tag>.json`.
Контракт фиксирует параметры, выбор входного ряда, реализацию контактов,
единицы, прохождение геометрического QC, SHA-256 артефактов и исходных файлов.
Частично перезаписанный набор не пройдёт проверку контрольных сумм.
`ReuseExistingResults=true` для стадии inverse проверяет совпадение контракта,
артефактов и входов, а не только наличие файлов. Контракт является технической
проверкой происхождения; он не подтверждает физическую валидацию модели.

Контроль раздельности сохраняется как `nik_trkg4_patch_overlap_<tag>.csv` и
сопровождается JSON с хэшами таблицы геометрии и общей сетки. Раздельность
площадок не заменяет контроль площади, положения центроида и сходимости сетки.

Глубина до ближайшей поверхности лёгкого может включать кость. Она хранится
как геометрическая характеристика; её нельзя автоматически объявлять
эффективной толщиной мягких тканей классической двуслойной модели.

## Исторический контроль сдвига обратного решения от 01.09.2026

Следующие числа относятся к состоянию до аудита 08.09.2026. Дополнительный
контроль фактического центроида и площади площадок отвергает прежний итоговый
кандидат; близость его RMSE не подтверждает допустимость геометрии. Актуальный
исправленный расчёт выбирается тегом `auditfix_20260908`, а статус его проверок
фиксируется в `notebooks/FIXES_2026-09-08.md`.

После перепрогона от 01.09.2026 точка обратной задачи заметно сместилась.
Причины проверены отдельными расчётами, в которых по одному сохранялись или
заменялись входной размерный ряд, параметры и алгоритм контактных площадок.

Установлено следующее.

1. В исходных CSV значения импеданса не изменились. Для Ника исправлена только
   субъектная атрибуция одинаковых записей: независимой считается запись 90 мм,
   а поздняя копия 100 мм исключается.
2. При фиксированной прежней точке замена 100 на 90 мм изменила RMSE с 7,763
   до 7,683 Ом и полный критерий с 22,662 до 23,180. Этого недостаточно, чтобы
   объяснить пространственный уход решения.
3. При тех же параметрах, прежнем размерном ряде и неизменной объёмной сетке
   текущий алгоритм построения контактных площадок изменил FEM-кривую на
   5,420 Ом RMS; максимальное отличие 11,802 Ом получено для сборки 50 мм.
   Архивный RMSE 3,593 Ом относится к прежней дискретизации контакта и не
   воспроизводится простым возвратом старых параметров.
4. Для проверки восстановлены два исторических режима. Режим `legacy`
   повторяет самый ранний алгоритм, который независимо наращивал каждое пятно
   и допускал общие узлы электродов. В прежней точке он дал RMSE 21,015 Ом:
   результат 50-мм сборки увеличился до 148,142 Ом. Режим `legacy_fast`
   функционально воспроизводит более поздние правила: ближайшие грани без
   разбиения поверхности между электродами, запрет повторного использования
   узлов и остановка по площади явно выбранных граней. Его исходный файл в
   доступной истории не сохранился, поэтому режим является реконструкцией.
5. Реконструкция `legacy_fast` совпала с архивной геометрией для 34 из 36
   электродных пятен. Остались два краевых расхождения для `V_minus` при 50 и
   70 мм: максимальное смещение центроида составило 0,619 мм, а отличие
   площади — 1,732 мм². При фиксированных архивных параметрах этот режим дал
   RMSE 3,997 Ом и `J=13,114`. Семь из девяти значений импеданса совпали с
   архивом точно; для 50 мм получено 94,034 вместо 79,108 Ом, для 70 мм —
   89,811 вместо 88,785 Ом. Следовательно, работоспособная функциональная
   версия прежнего алгоритма восстановлена, но точное происхождение двух
   архивных пятен и RMSE 3,593 Ом остаётся неустановленным.
6. Проверка MAT-файлов показала: `nodes`, `elems`, `boundary`, `elem_sigma`,
   `tissue_id` и `tissue_names` двух файлов v5 совпадают поэлементно. Различие
   хэшей контейнеров связано с другими полями и метаданными, а не с изменением
   объёмной прямой модели.
7. Выполнен возобновляемый прямой поиск с несколькими начальными областями и
   одинаковым верхним бюджетом 100 FEM-оценок. `legacy_fast` достиг заданного
   разрешения шага после 70 оценок: `J=10,5237`, RMSE 3,952 Ом,
   $(u,v,\varphi)=(5,656; 26,230; -10°)$. Центр совпал с архивной точкой;
   изменилась главным образом $\rho_2$ до 21,977 Ом·м. Текущий `disjoint`
   использовал 100 оценок: `J=11,0160`, RMSE 4,068 Ом,
   $(u,v,\varphi)=(5,880; 26,655; -6,875°)$ и $\rho_2=24,707$ Ом·м.
   Его центр отстоит от архивного на 0,480 мм в касательной плоскости.
8. Бюджет 25 точек оказался недостаточным. К этому моменту `legacy_fast` уже
   дал `J=10,5285`, но для `disjoint` было найдено только `J=15,3326`;
   итоговая область появилась позднее. Три точки `disjoint` оказались
   геометрически недопустимыми: связную площадку требуемой площади нельзя было
   построить без пересечения с соседним электродом. Они сохранены в истории со
   штрафом и не использованы как конечные решения.
9. Резервный `fmincon` не подходит для самостоятельного поиска позы. Выбор
   конечного набора поверхностных граней делает зависимость от координат и
   угла кусочно-постоянной. Микроскопический шаг конечных разностей не видит
   смены граней: угол сохраняет стартовое значение, а движение по координате
   может определяться главным образом штрафом за CT-глубину.
10. CEM-кандидат, рассчитанный при фиксированных `rho` из неудачного локального
   прогона, сохраняется только как диагностический. Его нельзя передавать как
   итоговую локализацию без совместной повторной оценки всех параметров.

`run_trkg4_inverse_inhale` всегда сохраняет отдельную историю вычисленных
точек. При остановке по лимиту в итоговый файл должна попадать точка с
наименьшим конечным значением критерия среди фактически рассчитанных, а не
последняя принятая итерация решателя. Для неразрушающих контрольных прогонов
предусмотрены отдельные `data_selection` и `result_tag`; они не изменяют
канонические файлы v5.

Режимы `legacy` и `legacy_fast` предназначены только для проверки
происхождения архивных чисел. Они не заменяют текущий режим `disjoint`, в
котором электродные пятна связны, разделены по области ближайшего центра и
контролируются по фактической площади поверхности, используемой полной
электродной моделью. Близость RMSE исторического варианта сама по себе не
подтверждает физическую корректность его контактной геометрии.

Расширенный поиск устранил ложный большой пространственный сдвиг и локализовал
обе новые точки рядом с архивной областью. Это локальная вычислительная
устойчивость в пределах проверенных стартов и шага, а не доказательство
единственности или физической истинности решения.

До аудита 08.09.2026 основными входами `TRKG4_inverse_inhale_results.ipynb`
были файлы с тегом `disjoint_restartable_manifest90`:
сводка подгонки, кривая импеданса, профиль глубины, геометрия электродов,
геометрия сетки, Якобиан и локальная чувствительность. Файлы с тегом
`v5_1mm_local2mm`, архивный снимок до перепрогона и старый PEM/CEM-скрининг
используются только в сравнительных разделах. В 3D-графиках итоговая
координата `disjoint` показывается вместе со старой архивной координатой, но
архивная точка не участвует в новом расчёте.

Полный сравнительный раздел 18 удалён из читательской версии отчёта после
перевода основных разделов на `disjoint`: он повторял результаты разделов 10
и 14 и мог восприниматься как второй способ расчёта. Старые `legacy_fast`,
`v5_1mm_local2mm`, `ablation_*`, PEM/CEM и снимок
`output/repro_check_20260901_before/` сохранены локально для аудита
происхождения результата, но не считаются равноправными итоговыми оценками.

DICOM не является входом MATLAB/FEM-конвейера. Для наложения HU в отчёте
локально задаётся `KALMYKOV_DICOM_DIR`; без этой переменной отчёт продолжает
работу по STL и расчётным CSV. Абсолютный путь к медицинским данным в коде
не хранится.

## Возобновляемый исправленный поиск

```powershell
python tools/run_restartable_inverse_search.py --mode disjoint `
  --tag auditfix_20260908_repeat --budget 100 --data-selection manifest `
  --initial-points notebooks/audit_fixes_2026_09_08/admissible_starts.csv
```

Выполненный набор `auditfix_20260908` сохранён для отчёта. После его завершения
исправлен механизм контрольных точек Python-драйвера; использованный в расчёте
код сохранён в `notebooks/audit_fixes_2026_09_08/source_used_auditfix_20260908.zip`.
MATLAB-модель не менялась. Из-за проверки версии кода для нового запуска в команде
указан отдельный тег. Существующая история старого тега не переносится в новую версию.

`--budget` задаёт общее число оценок, включая геометрически отклонённые.
При первом расчёте нового тега используется `--no-resume`; при продолжении
этот флаг убирается. Повторное использование тега с `--no-resume` перезаписывает
его историю. Драйвер фиксирует исходники, входы и стартовые точки по SHA-256;
после изменения модели продолжать старый поиск нельзя. Технический сбой
MATLAB останавливает прогон, а допустимая геометрическая ошибка сохраняется
отдельным статусом `invalid_geometry` и не конкурирует с рассчитанными точками.
Итоговый контракт публикуется после повторного FEM и диагностики секущих.
Контрольная точка `.restartable_<tag>_state.json` атомарно сохраняет историю,
текущий шаг и незаконченный перебор параметров. При продолжении завершённого
поиска прежняя публикация сначала переводится в состояние выполняющегося запуска;
после завершения публикуется новый согласованный контракт.

## Параметрический монтаж на руках

Профиль запускается отдельно:

```matlab
report = run_pipeline('Profile', 'arm_electrode_report', ...
    'ArmPrebuiltMeshFile', 'output/accepted_arm_parameter_mesh.msh', ...
    'ArmMeshTargetSizeMm', 1);
```

Файл сетки обязателен. Для этого профиля принят
`output/accepted_arm_parameter_mesh.msh`: 752 827 узлов и 4 358 825
тетраэдров, целевой шаг 1 мм в рабочих объёмах обеих рук. Pipeline не
подменяет сетку молча и не запускает профиль без явно указанного файла.
Критерии приёмки и команды воспроизведения приведены в
[`ARM_PARAMETER_MESH_ACCEPTANCE.md`](ARM_PARAMETER_MESH_ACCEPTANCE.md).

Диагностический равномерный 1-мм прогон от 31.08.2026 построил сетку на
5 601 998 узлов и 33 866 614 тетраэдров с объёмом 20,1663 л. Геометрический
объём сохранился с расхождением около 0,022%, но были найдены вырожденные и
инвертированный элементы, а размер сетки примерно в 47 раз превысил прежний.
Эта сетка отвергнута и не используется в расчётах.

### Поверхностные координаты электродов ТТРКГ

Общее задание координат и ограничения метода описаны в ноутбуке
`Colab Notebooks/40.03_Поверхностные_координаты_электродов_ТТРКГ.ipynb`.
Частный конечно-элементный тест электродов на руках остаётся в `40.02`.

Система `surface_path_v1` использует всю внешнюю поверхность тела. Она
предназначена для четырёхэлектродного монтажа ТТРКГ и не используется для
боковых сборок мягких тканей, РЕО32 или стандартной ТРКГ. Общий механизм
принимает четыре произвольные координаты, проецирует их на границу FEM-модели и
строит контактные площадки. Он уже применяется к монтажам на руках и на правом
боку.

Интерактивный файл `output/nik_surface_landmark_picker.html` построен
скриптом `tools/build_surface_landmark_picker.py`. Основной режим
позволяет выбрать четыре именованные точки на любом участке внешней
поверхности. Дополнительный симметричный режим использует два независимо
заданных пути для сторон I+/V+ и I-/V-. Равными задаются расстояния от
соответствующих анатомических ориентиров до внутренних электродов и расстояния
между внутренними и внешними электродами. Зеркальное отражение XYZ не
используется.

Интерфейс создаёт `ttrkg_surface_montage.json` по схеме
`trkg4_ttrkg_surface_v3`. Свободно выбранные центры обрабатываются
существующим общим механизмом проекции на поверхность. Для симметричного режима
точные центры повторно вычисляет `trkg4_surface_montage_centres` на
исходной расчётной поверхности. Предварительные точки HTML служат только для
визуального контроля.

Скелет, лёгкие и сердце представлены независимыми справочными слоями.
Облегчённая до 12 000 треугольников поверхность и визуальная светотень не
изменяют исходную STL, принятую объёмную сетку или сохранённые результаты FEM.

Программные тесты проверяют генератор HTML, контракт MATLAB-загрузчика,
вычисление центров по поверхностным путям и сохранение прежнего контракта монтажа на руках.
Автоматизированная браузерная проверка подтвердила оба режима: свободный выбор
четырёх точек и последовательность «две направляющие → два расстояния → четыре
центра → JSON». Эти проверки подтверждают программную согласованность, но не
правильность выбранных анатомических ориентиров.

Частный профиль параметрического монтажа на руках сохраняет отдельные
ограничения. На принятой сетке автоматический перенос прежнего начала отсчёта
дал расхождение центров до 7,85 мм. Прежняя внутренняя плоскость присоединения
искусственной руки не задаёт однозначную точку на коже. Поэтому перед заменой
действующего осевого монтажа на руках необходимо принять опорную границу на каждой
руке, проверить пути, построить электродные патчи и повторить проверку
взаимности. Этот отрицательный результат относится к частному профилю монтажа на руках и не
ограничивает общий механизм привязки к внешней поверхности тела.

## Подготовка и QC геометрии

Историческая операция равномерного remesh перенесена в общий скрипт:

```powershell
python tools/remesh_stl_uniform.py input.stl output.stl `
  --cell-size-mm 1 --report output/remesh_report.json
```

MeshFix отключён по умолчанию. Он включается только явно и его результат
отклоняется при превышении заданного изменения объёма:

```powershell
python tools/remesh_stl_uniform.py input.stl output.stl `
  --cell-size-mm 2 --repair meshfix --max-volume-change-percent 0.5
```

Проверка объёмной сетки:

```powershell
python tools/check_msh_quality.py output/model.msh `
  --report output/model_mesh_qc.json
```

Без `--allow-defects` команда возвращает ошибку при наличии инвертированных
или вырожденных тетраэдров.

## Активный код и история

- `src/` — точки входа и логика текущей TRKG4-постановки;
- `vendor_stl_eidors/` — используемые проверенные EIDORS/STL-helper-функции;
- `tools/` — активная подготовка и QC STL/MSH;
- `tests/` — модульные тесты;
- `archive/MATLAB_investigation_NIX/` — ранний прототип и старые sweep-скрипты.

Архив не является вторым способом получить актуальные числа. Его старые
16-электродные кольцевые постановки, единицы и sweep-отчёты сохраняются только
для происхождения решений и критического аудита.


### Матрица проводимостей для монтажа на руках — 40.05

Уточнение автора от 08.09.2026 отменяет четыре плоскостных электрода.
Действующий смешанный вариант содержит две наружные токовые плоскости
I+/I− и две внутренние поверхностные манжеты V+/V− шириной 5 мм.
Сохранённая четырёхплоскостная ветка `40.02` остаётся историческим результатом
прежней постановки; её числа не переименовываются в исправленный вариант.

```matlab
run_pipeline('Profile','arm_conductivity_matrix');
```

Матрица содержит четыре типа контакта, два внутренних расстояния,
два межэлектродных разноса и три уровня каждой из трёх проводимостей:
16 монтажей × 27 тканевых состояний. Конфигурация уровней, частота 50 кГц,
опорные параметры и ссылки на IT’IS зафиксированы в
`src/trkg4_arm_conductivity_design.m`. Сердце целиком моделируется как
эффективная смесь крови и миокарда по явному решению автора. Геометрия КТ
не меняется; диапазоны задают сценарии тканевых свойств, а не измеренные
пульсовые амплитуды.

Подготовленные объёмные матрицы сохраняются в `prepared_fem.mat`.
Для каждого монтажа сохраняются `*_results.csv`, `*_patches.csv`, а для
четырёх контактных типов в положении 20/80 мм — `*_derivative_qc.csv`.
`identity.json` связывает результат с хэшами сетки, STL, кода и EIDORS;
изменение этих входов блокирует продолжение в той же папке результатов.
`PrepareOnly`, `MontageIndices` и `CaseIndices` позволяют подготовить модель
отдельно или рассчитать подмножество. Полнота серии проверяется ноутбуком.

Генератор `tools/build_arm_conductivity_notebook.py` создаёт канонический
`Colab Notebooks/40.05_Чувствительность_ТТРКГ_к_проводимости_тканей.ipynb`.
После расчётов ноутбук выполняется и экспортируется в HTML без входного кода
по правилам `Colab Notebooks/AGENTS.md`.

## Полный перебор центров и поворотов

Для полного CEM-перебора по запросу автора от 08.09.2026 используется тот же вход:

```matlab
report = run_pipeline('Profile', 'full_pose_scan', ...
    'SourceTag', 'auditfix_20260908', 'ResultTag', 'fullscan_20260908_v2');
```

Постановка: u и v от −120 до +120 мм с шагом 1 мм; φ от −20° до +20°
с шагом 1°. Все 58081 центра проецируются на треугольную поверхность,
в каждом учитывается 41 угол: всего 2381321 поза. Сопротивления фиксированы
по SourceTag; девять размеров и значения на вдохе берутся из субъектного
манифеста. Отсеивания по h, PEM-ранжирования и выбора только top-K нет.
Недопустимая геометрия имеет отдельный статус и NaN вместо импеданса/ошибки.
Цвет карты определяется RMSE в Омах относительно эксперимента, без штрафов J;
меньшая ошибка соответствует более зелёному цвету.

Результаты и промежуточные файлы сохраняются в
`output/full_pose_scan_<ResultTag>/`. `run_manifest.json` фиксирует входы
и код запуска. `geometry/` и `solved/` хранят проверяемые контрольные точки;
`progress.json` показывает фактическую стадию. Финальные `summary.json`,
`manifest.json` и `result_contract.json` публикуются после проверки полноты.
Наличие частичных CSV или checkpoint-файлов не означает завершения.

Рабочие матрицы Green вычисляются для точного объединения узлов групп
поз и освобождаются после сохранения всех результатов группы. Это ограничение
памяти; состав центров, углов, размеров и уравнения CEM не сокращаются.

Генератор отчёта:

```text
python notebooks/build_trkg4_full_scan_report.py --input-dir output/full_pose_scan_fullscan_20260908_v2
```

Он проверяет SHA256, наличие каждого центра и угла, статусы и RMSE/MAE
по девяти сохранённым импедансам. Создаются локальные интерактивные HTML
и PNG. Перебор представляет результат математической модели; полнота сетки
не подтверждает экспериментальную валидацию или точность реального положения.


## Две ткани и кривые границы: пример 11.3

Отдельный профиль сохраняет исходную сетку и контакты, но присваивает лёгочной
STL-маске ρ₂, всему остальному внутри тела — ρ₁, включая поддиафрагмальную область.
Сопротивления и поза фиксированы по выбранному исходному результату; повторной
подгонки нет. Историческая четырёхтканная модель не перезаписывается.

Из корня MATLAB_TRKG4_real_subjects:

```text
python tools/compare_curved_two_tissue.py matlab-export --tag auditfix_20260908
python tools/compare_curved_two_tissue.py prepare --tag auditfix_20260908
python tools/compare_curved_two_tissue.py matlab-solve --tag auditfix_20260908
python tools/compare_curved_two_tissue.py finish --tag auditfix_20260908
python notebooks/build_trkg4_inverse_report.py
```

Первые и третьи команды вызывают канонический MATLAB-вход
`run_pipeline('Profile','two_tissue_comparison','ResultTag',tag,'Stage',stage)`.
Python использует numpy, scipy, pandas, matplotlib и vtk, а извлечение 11.3 —
зависимости существующего ноутбука. MATLAB решает 18 прямых CEM-задач и четыре
обратных по взаимности. Все девять геометрий должны пройти действующий QC.

В `output/two_tissue_<tag>/` записываются центры и объёмы элементов, две маски,
профиль сечения, geometry.json, fem.csv, fem_contract.json, comparison.csv,
metrics.csv, comparison_summary.json и два PNG. Контракты проверяют хэши входов
и результатов; раздел 11.3.1 дополнительно сверяет параметры с выбранным отчётом.

`closed` — замкнутая лёгочная STL-маска; `extended` — продолжение после первого
входа вдоль inward сечения до границы тела. Последнее удаляет все дальние выходы,
не только купол диафрагмы. Аналитические стеки с конечным лёгким имеют два
сопротивления, но три или больше пространственных слоёв. Усреднение глубин или
локальных ядер обозначено как эвристика. Основная метрика сравнения — расхождение
с двухтканным FEM; невязка с экспериментом приводится отдельно. Сходимость FEM
по сгущению сетки и физическая валидация этой сокращённой модели не установлены.

Проверки: `python -m pytest tests/test_curved_two_tissue.py tests/test_inverse_report_contract.py -q`.
После генерации требуется полное выполнение ноутбука; само наличие ipynb не
означает успешного выполнения.


Матрицы контактов для 40.05 экспортируются после сравнения с EIDORS
(`*_contact.mat`, расхождение < 10⁻¹²). Полные системы в двойной точности
решаются Intel oneMKL PARDISO через `tools/solve_arm_conductivity_matrix.py`;
проверяется невязка исходной системы, а не только масштабированного оператора.
Зависимости: NumPy, SciPy, pandas, h5py, PyPardiso и oneMKL. Они могут быть
установлены в рабочее окружение либо локально в папку результата
`python_solver_deps`. `TRKG4_PYTHON` задаёт Python для MATLAB-точки входа.
`ExportOnly=true` завершает запуск после экспорта контактов; `Solver=matlab_direct`
сохраняет референсный путь через EIDORS `left_divide`.
Для независимого запуска подмножеств используются аргументы Python `--montages`
и `--cases` со списками номеров через запятую. Один монтаж одновременно должен
обрабатываться только одним процессом. `python_identity.json` фиксирует версии
библиотек, хэш runtime и связь с MATLAB-паспортом. Подготовка тканевых матриц
сохранена отдельно с исходным паспортом и архивом использованного кода.

Однократный длительный запуск со всей цепочкой завершения:

```text
python notebooks/full_scan_2026_09_08/run_full_scan_job.py --matlab <matlab-executable> --tag fullscan_20260908_v2 --green-limit 10000
```

Он последовательно выполняет полный MATLAB-перебор, проверяет лучшую точку
прямым EIDORS на всех девяти размерах и по взаимному измерению, затем создаёт
отчёт. `job_status.json` различает вычисление, построение отчёта, завершение
и ошибку; итоговая карта — `report/full_scan.html`.
При прерывании запуск с тем же кодом, сеткой и параметрами использует
сохранённые проверенные блоки. Активный `job.lock` предотвращает второй
одновременный запуск. Нативный прототип построения площадок не используется;
рабочая геометрия выполняется проверенными MATLAB exact helpers.


Во время серии 40.05 рабочая `vendor_stl_eidors/electrode_faces_by_area.m`
получила независимое исправление размерности индексирования для частного
случая одной грани. Численные системы к этому моменту были подготовлены и
экспортированы; идущий Python-перебор использует эти фиксированные матрицы.
`source_used_matrix.zip` содержит точный код построения с хэшами из
`identity.json`. Ноутбук проверяет архив, а отличие текущего рабочего файла
показывает явно. Рабочая версия не откатывается. Новые расчёты изменённым
кодом должны использовать новый каталог результата; для строгого
воспроизведения используется зафиксированная версия в отдельной копии.


### Подробная методическая редакция отчёта о чувствительности

Ноутбук `40.05` оформлен как методический вычислительный эксперимент: проверка
предположений, исследование зависимостей и верификация расчёта перед переходом
к другим электродным сборкам. Он не устанавливает оптимальный монтаж.
Генератор `tools/build_arm_conductivity_notebook.py` сохраняет подробную
постановку прямой задачи, определения производных, численный контроль,
результаты и общий порядок исследования новых сборок.

Исходная матрица 432 FEM-состояний сохраняется. Дополнительные сводки
линейности, конечных взаимодействий, геометрических сравнений и локальной
обусловленности формируются из неё в подкаталоге `notebook_analysis`
результатов серии. Предыдущая редакция отчёта и генератора сохранена
в `report_history` вместе с контрольными суммами.

Для обновления отчёта выполнить генератор, затем все ячейки ноутбука и
экспорт HTML без входного кода по правилам основной папки. Это не запускает
FEM заново. Расчётный профиль остаётся специализированным под текущие
монтажи; новая произвольная сборка требует отдельной спецификации и QC.

## Научная редакция отчёта о боковых сборках от 09.09.2026

Полный читательский текст поддерживается в
`notebooks/TRKG4_inverse_inhale_scientific.md`. Генератор
`notebooks/build_trkg4_inverse_report.py` переносит его во все текстовые ячейки
основного ноутбука и добавляет разделы о семействах упрощённых моделей,
критериях сравнения и первичных методических источниках. Маркеры замены
проверяются по полному набору Markdown-ячеек; изменение структуры требует
явного согласования текста с вычислительными разделами.

Для обновления только научного описания уже выполненного отчёта из корня
`MATLAB_TRKG4_real_subjects` используется:

```text
python notebooks/update_scientific_report.py --preview
python notebooks/update_scientific_report.py
```

Первая команда записывает проверочную версию в
`output/scientific_report_20260909/review.ipynb` и `review.html`; вторая обновляет
канонические `../Colab Notebooks/20.10_КТ_FEM_локализация_боковых_матриц_МГТУ.ipynb` и `.html`. Ячейки не исполняются,
MATLAB не вызывается. Скрипт требует ранее выполненный ноутбук, проверяет
совпадение вычислительной структуры всех кодовых ячеек и допускает только
объявленные изменения текста вывода и его Markdown-отступов. Сохранённые
численные таблицы и изображения копируются без изменения. При несовпадении
вычислений перенос результатов блокируется: новый расчёт выполняется отдельно.

HTML содержит научный текст, таблицы и рисунки без входного кода. Код остаётся
доступен в ipynb. Скрипт записывает `revision_verification.json` с контрольными
суммами и `reader_visible_text.txt` для полной вычитки. Исходная выполненная
редакция сохранена в `output/scientific_report_20260909/before/`.

Прямой запуск `build_trkg4_inverse_report.py` по-прежнему создаёт невыполненную
структуру ноутбука. Для редакторского обновления с сохранением результатов
следует использовать `update_scientific_report.py`. Редакция не выполняет новую
оптимизацию и не объявляет найденным оптимальное упрощение; она задаёт
проверяемую программу последующих вычислительных исследований.


## Канонический этап 20.10 и перенос от 09.09.2026

Полный отчёт находится в основной папке:

- [20.10: ноутбук](../../Colab%20Notebooks/20.10_КТ_FEM_локализация_боковых_матриц_МГТУ.ipynb)
- [20.10: читательский HTML](../../Colab%20Notebooks/20.10_КТ_FEM_локализация_боковых_матриц_МГТУ.html)
- [20.10: паспорт и границы этапа](../../Colab%20Notebooks/20.10_КТ_FEM_локализация_боковых_матриц_МГТУ.md)

Генератор `notebooks/build_trkg4_inverse_report.py` по умолчанию пишет в этот
канонический путь. `TRKG4_REPORT_TARGET` оставляет возможность изолированного
тестового вывода. `notebooks/update_scientific_report.py` обновляет его текст
и HTML с сохранением вычисленных выходов; `--preview` пишет отдельную
проверочную копию. Запуск ячеек при таком обновлении не выполняется.

Поиск расчётного корня поддерживает запуск из корня репозитория,
`Colab Notebooks`, корня MATLAB-ветки и её папки `notebooks`. Таблицы, сетки,
маски и MATLAB-ядро остаются в `MATLAB_TRKG4_real_subjects`; исходные данные
не переносятся вслед за читательским отчётом. Python-подготовка двухтканного
сравнения читает код только канонического 20.10.

Старые `notebooks/TRKG4_inverse_inhale_results.ipynb` и `.html` являются
указателями без второй вычислительной версии. Исторические аудиты сохраняют
старые пути как сведения происхождения. Полные локальные версии 20.10
содержат индивидуальную анатомию и исключены из Git; безопасный паспорт
и исходники генерации остаются доступными для версионирования.

При изменении пути потребителя изменился хеш скрипта сравнения. Точная версия,
которой получены сохранённые результаты, помещена в `source_used` рядом с ними
под именем с исходным SHA-256. Отчёт проверяет совпадение текущего производителя
либо этой явно обозначенной сохранённой версии с исходным хешем. Сохранённый
код используется для проверки происхождения, а не исполняется как резервный
решатель. Контракты и численные таблицы прежнего расчёта не переписываются.

Связь этапов: 20.10 владеет индивидуальным расчётом; 20.11 проверяет
чувствительности; 20.12 — пригодность и стоимость упрощений, включая Lmax;
серия 32 выбирает размеры после определения применимого оператора.
