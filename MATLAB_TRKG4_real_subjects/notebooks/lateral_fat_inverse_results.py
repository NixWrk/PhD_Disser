"""Russian scientific narrative for the fixed-CT inverse task with fat."""


def result_sections(md,code):
    md(r'''## 5.6. Восстановление сопротивлений при отдельном описании жира

Прямой расчёт раздела 5.5 уменьшил расхождение с наблюдениями, но использовал заранее заданные сопротивления. Теперь проверяется, позволяет ли та же пятикомпонентная модель восстановить общие для всех девяти сборок сопротивления остаточного мягкотканного фона и лёгкого. Положение C01, геометрия КТ на вдохе и конечные контакты диаметром 5 мм сохраняются. Новая локализация не выполняется.

Обозначения уточняются: **ρ₁** относится к оставшемуся мягкотканному фону после выделения жира, костей, сердца и лёгких; **ρ₂** — к лёгкому; **ρ₃** — к объединённым подкожному и внутреннему жиру. Поэтому новое ρ₁ не имеет в точности того же физического смысла, что эффективное сопротивление всего внелёгочного объёма в двухтканной модели. Свойства костей и сердца по-прежнему заданы литературным сценарием.

**Рисунок 8. Полная сегментация доступного объёма КТ и прежнее положение конечных электродов.** Голубым показаны лёгкие, светлым — кости, красным — сердце, жёлтым — объединённый жир. Полупрозрачная серая наружная оболочка показывает границу расчётного тела: остаточный мягкотканный фон занимает его объём за вычетом выделенных тканей. Оболочка не является отдельной новой маской кожи. Продолжения рук унаследованы из прежней FEM-геометрии.

Список слева меняет прозрачность тканей, справа — размер единственной отображаемой сборки. Все девять размеров имеют собственные четыре контактные площадки; одновременно установленный набор из 36 электродов здесь не моделируется. Фактическая площадь площадок составляет 19,6356–19,7213 мм² при номинальном диаметре 5 мм. Площадки изображены точными треугольниками из CEM, а дополнительные цветные маркеры обозначают их центры и облегчают поиск. Линия между центрами помогает проследить порядок I+, V+, V−, I−; она не задаёт проводящий элемент.

Рисунок позволяет проверить взаимное положение тканей и сборки, в частности расположение жира вблизи контактов. Для лёгких, костей, сердца и жира показаны поверхности исходных объёмных масок во всём полученном поле КТ; электрическая задача использует только их часть внутри расчётного тела. Поверхности упрощены только для быстрого отображения. Эта визуализация не заменяет проверку сегментации по срезам и не показывает распределение чувствительности. Численный расчёт использует прежние объёмные доли материалов и 4096 точек интегрирования на элемент.''')
    code('''from lateral_fat_3d import figure as anatomy_figure
def show_fat_result(fig):
    display(HTML(fig.to_html(full_html=False,include_plotlyjs='inline',config={'responsive':True,'displaylogo':False})))
show_fat_result(anatomy_figure())
''')
    md(r'''### 5.6.1. Два неизвестных сопротивления и проверка роли третьего

Наличие отдельной жировой области требует отдельной проводимости в прямой модели, но не требует автоматически определять её из тех же измерений. В основной обратной задаче ρ₃ фиксируется по [IT’IS V5.0](https://itis.swiss/virtual-population/tissue-properties/downloads/database-v5-0): **23,1086 Ом·м при 50 кГц** и **23,0406 Ом·м при 100 кГц**. Частотные значения рассчитаны по [дисперсионной модели Gabriel](https://itis.swiss/virtual-population/tissue-properties/database/dielectric-properties). Это литературное условие, а не индивидуальное измерение жира добровольца.

Для каждого дыхательного состояния независимо решается задача

$$\min_{\rho_1,\rho_2}\sum_{i=1}^{9}
\left[Z_i(\rho_1,\rho_2;\rho_3,\rho_{\rm кость},\rho_{\rm сердце})-Z_i^{\rm наблюд}\right]^2,$$

где $Z_i$ — передаточный импеданс i-й сборки из пятикомпонентной FEM/CEM-модели, Ом; $Z_i^{\rm наблюд}$ — базовый уровень из экспериментальной таблицы, Ом; $\rho_1,\rho_2,\rho_3,\rho_{\rm кость},\rho_{\rm сердце}$ — удельные сопротивления соответствующих областей, Ом·м. Все девять размеров имеют одинаковый вес в выбранном вычислительном критерии. Равенство весов не устанавливает одинаковый шум наблюдений.

**Литературные интервалы не ограничивают обратный поиск.** Они используются только после расчёта для оценки полученных значений: ориентир для ρ₁ — 1,3774–10 Ом·м; для ρ₂ — от табличного deflated выбранной частоты до 24,0741 Ом·м. Эти ориентиры не являются строгими индивидуальными физиологическими границами. Попадание в них не доказывает истинности оценки; выход за них требует объяснения и не скрывается ограничением оптимизатора.

По решению автора вычислительная область каждого свободного сопротивления составляет **0,1–100 Ом·м**, без автоматического расширения. Верхняя граница 100 Ом·м намеренно выходит за принятые литературные ориентиры для мягких тканей и лёгкого; она ограничивает объём диагностического поиска, а не задаёт физиологическую норму. Нижняя граница положительна, поскольку нулевое удельное сопротивление не является допустимым конечным параметром этой модели. Оптимизация логарифмов обеспечивает положительность. Если минимум достигает края, соответствующее значение отмечается как граничный численный кандидат, а не как независимо восстановленное сопротивление.

Дополнительно строится **профиль невязки по ρ₂ от 0,1 до 100 Ом·м** при 50 кГц и литературном ρ₃: для каждого заданного ρ₂ заново подбирается ρ₁ по всем девяти наблюдениям. Эта проверка отделяет найденный численный кандидат от ситуации, когда очень разные большие ρ₂ дают практически одинаковое согласие с экспериментом.

На каждом шаге оптимизации решается прямая FEM/CEM-задача для всех девяти размеров. Интерполяционная библиотека двухтканной модели не используется. Оптимизируются логарифмы сопротивлений; производные вычисляются через прямое и взаимное поля. Для каждой основной двух- или трёхпараметрической подгонки применяются два разнесённых численных начальных приближения; литературные значения неизвестных параметров не задают поисковые ограничения. Их совпадение проверяет зависимость от этих стартов, но не доказывает глобальную единственность.

**Влияние выбора ρ₃** проверяется при 50 кГц. Сначала ρ₃ поочерёдно задаётся равным 75%, 100% и 125% литературного значения, а ρ₁ и ρ₂ каждый раз восстанавливаются заново. Затем все три сопротивления подбираются совместно в той же области 0,1–100 Ом·м, без ограничения ρ₃ интервалом ±25%. Интервал ±25% задан для анализа чувствительности и **не объявляется литературно установленным физиологическим диапазоном жира**. Эта проверка показывает зависимость оценок от принятого параметра; она не определяет индивидуальную неопределённость ρ₃. Три параметра подбираются отдельно для каждого дыхательного состояния. Различие найденных ρ₃ между состояниями не интерпретируется как установленное дыхательное изменение жира.

КТ на вдохе используется также для выдоха. Равенство ρ₁ между состояниями не навязывается. Полученная разность параметров поэтому остаётся условной: она может компенсировать как тканевые изменения, так и несоответствие неизменной геометрии реальному дыханию. Частоты 50 и 100 кГц вновь задают два расчётных сценария с одними наблюдениями; отдельные экспериментальные записи на этих частотах здесь не вводятся.''')
    code('''from lateral_fat_inverse_analysis import OUT as INV_OUT, figure as inverse_figure, profile_figure
inv_qc=json.loads((INV_OUT/'inverse_qc.json').read_text(encoding='utf-8'))
assert inv_qc['complete'] and inv_qc['quadrature_all_within_tolerance']
for n,h in {**inv_qc['inputs_sha256'],**inv_qc['outputs_sha256']}.items(): assert sha(INV_OUT/n)==h,n
inv_fits=pd.read_csv(INV_OUT/'fits.csv')
inv_real=inv_fits[inv_fits.synthetic==0].copy()
inv_base=inv_real[(inv_real.fat_factor==1)&(inv_real.free_parameters==2)].copy()
search_status={'interior_stationary_point':'Внутренний кандидат','user_numeric_boundary_reached':'Достигнута заданная граница'}
''')
    md(r'''### 5.6.2. Результат восстановления по экспериментальным данным

**Таблица 4и. Численные кандидаты по девяти сборкам при литературном ρ₃ в области 0,1–100 Ом·м.** RMSE — среднеквадратичная невязка девяти импедансов, а не ошибка относительно истинных тканевых сопротивлений. Литературные признаки оценивают результат после поиска. Достижение вычислительной границы или выход на плато не дают установленной конечной оценки соответствующего параметра.''')
    code('''rows=[]
for r in inv_base.sort_values(['frequency_hz','state']).itertuples():
    bound=', '.join(name for name,hit in [('ρ₁',r.bound_rho1),('ρ₂',r.bound_rho2)] if hit) or 'нет'
    rows.append([r.frequency_hz/1000,'Вдох' if r.state=='inhale' else 'Выдох',r.rho1,r.rho2,r.rho3_fat,r.rmse_ohm,'да' if r.rho1_in_literature and r.rho2_in_literature else 'нет',bound,search_status[r.stop_reason]])
table(pd.DataFrame(rows,columns=['Частота, кГц','Состояние','ρ₁, Ом·м','ρ₂, Ом·м','ρ₃ задано, Ом·м','RMSE, Ом','ρ₁ и ρ₂ в литературной области','На вычислительной границе','Статус поиска']))
for state,label in [('inhale','Вдох'),('exhale','Выдох')]:
    r=inv_base[(inv_base.frequency_hz==50000)&(inv_base.state==state)].iloc[0]
    paragraph(f"**{label}, 50 кГц, численный кандидат:** ρ₁ = {num(r.rho1,4)}, ρ₂ = {num(r.rho2,4)} Ом·м при заданном ρ₃ = {num(r.rho3_fat,4)} Ом·м. Невязка — {num(r.rmse_ohm,3)} Ом. Статус: {search_status[r.stop_reason]}.")
paragraph(f"Вычислительная граница ρ₂ достигнута в {int(inv_base.bound_rho2.sum())} из {len(inv_base)} основных подгонок. Близость к вычислительному пределу нельзя интерпретировать как независимо установленное сопротивление лёгкого или отсутствие его дыхательного изменения.")
''')
    code("""outside=int((~(inv_base.rho1_in_literature & inv_base.rho2_in_literature)).sum())
paragraph(f"Литературные ориентиры не участвовали в ограничении поиска. После расчёта {outside} из {len(inv_base)} основных численных кандидатов оказались вне области одновременного соответствия прежним ориентирам ρ₁ и ρ₂. Граничные кандидаты дополнительно требуют проверки профиля невязки ниже; их большие значения не принимаются как установленные индивидуальные свойства.")
""")
    md(r'''**Рисунок 9. Согласование пятикомпонентной модели с измерениями после восстановления сопротивлений.** Верхние панели показывают эксперимент и найденный прогноз, нижние — разность со знаком «расчёт минус эксперимент». Слева расположен вдох, справа — выдох. Список выбирает частоту и способ задания ρ₃. При наведении доступны найденные параметры и общая невязка.

На верхних панелях следует оценивать совпадение всего размерного ряда, а на нижних — размеры и знак оставшихся ошибок. Уменьшение общей невязки не гарантирует правильных тканевых параметров. Отрезки соединяют дискретные размеры; доверительные интервалы отсутствуют, поскольку модель совокупной ошибки измерений не установлена. В каждом варианте контакты и геометрия одинаковы.''')
    code('show_fat_result(inverse_figure())')
    md(r'''**Таблица 4к. Влияние заданного ρ₃ и пробная трёхпараметрическая подгонка, 50 кГц.** Строки ±25% показывают повторное восстановление двух параметров при изменённом сопротивлении жира. Строка совместной подгонки дополнительно освобождает ρ₃ в области 0,1–100 Ом·м; интервал ±25% в этом поиске не используется. Её нельзя считать индивидуальным измерением жира только по уменьшению RMSE.''')
    code('''rows=[]
for r in inv_real[inv_real.frequency_hz==50000].sort_values(['state','free_parameters','fat_factor']).itertuples():
    mode='Подбор трёх ρ' if r.free_parameters==3 else f'ρ₃ = {100*r.fat_factor:.0f}% литературного'
    bound=', '.join(name for name,hit in [('ρ₁',r.bound_rho1),('ρ₂',r.bound_rho2),('ρ₃',r.bound_rho3)] if hit) or 'нет'
    rows.append(['Вдох' if r.state=='inhale' else 'Выдох',mode,r.rho1,r.rho2,r.rho3_fat,r.rmse_ohm,bound])
table(pd.DataFrame(rows,columns=['Состояние','Постановка','ρ₁, Ом·м','ρ₂, Ом·м','ρ₃, Ом·м','RMSE, Ом','На вычислительной границе']))
for state,label in [('inhale','вдоха'),('exhale','выдоха')]:
    a=inv_real[(inv_real.frequency_hz==50000)&(inv_real.state==state)&(inv_real.free_parameters==2)]
    r=inv_base[(inv_base.frequency_hz==50000)&(inv_base.state==state)].iloc[0]
    changes=[100*max(abs(a['rho'+str(k)]/r['rho'+str(k)]-1)) for k in [1,2]]
    lung_note='Во всех этих сценариях ρ₂ оставалось на верхней границе 100 Ом·м; это не означает нечувствительности или точного определения сопротивления лёгкого.' if a.bound_rho2.all() else f'Численный кандидат ρ₂ изменился максимум на {num(changes[1],2)}%; для граничных решений это отношение не характеризует точность.'
    paragraph(f"Для {label} изменение заданного ρ₃ на ±25% изменило оценку ρ₁ максимум на {num(changes[0],2)}% относительно литературного ρ₃. {lung_note}")
''')
    code("""for state,label in [('inhale','вдоха'),('exhale','выдоха')]:
    r=inv_real[(inv_real.frequency_hz==50000)&(inv_real.state==state)&(inv_real.free_parameters==3)].iloc[0]
    b=inv_base[(inv_base.frequency_hz==50000)&(inv_base.state==state)].iloc[0]
    edge=', '.join(name for name,hit in [('ρ₁',r.bound_rho1),('ρ₂',r.bound_rho2),('ρ₃',r.bound_rho3)] if hit)
    status=f'На вычислительной границе: {edge}.' if edge else 'Все три численных кандидата находятся внутри вычислительной области.'
    compatibility='Кандидаты ρ₁ и ρ₂ одновременно соответствуют принятым литературным ориентирам.' if r.rho1_in_literature and r.rho2_in_literature else 'Кандидаты ρ₁ и ρ₂ не соответствуют одновременно принятым литературным ориентирам; это оценка уже найденного результата.'
    paragraph(f"Совместная подгонка трёх параметров для {label}: ρ₁ = {num(r.rho1,3)}, ρ₂ = {num(r.rho2,3)}, ρ₃ = {num(r.rho3_fat,3)} Ом·м. RMSE уменьшилась с {num(b.rmse_ohm,3)} до {num(r.rmse_ohm,3)} Ом. {status} {compatibility} Выбранное сопротивление жира составляет {num(100*r.rho3_fat/b.rho3_fat,1)}% литературного значения; меньшая невязка сама по себе не подтверждает индивидуальные свойства ткани.")
""")
    md(r'''**Рисунок 10. Профиль невязки по сопротивлению лёгкого при свободной повторной подгонке ρ₁, 50 кГц.** Горизонтальная ось логарифмическая и охватывает ρ₂ от 0,1 до 100 Ом·м; вертикальная показывает RMSE девяти импедансов. Зелёная полоса обозначает литературный ориентир и не ограничивает вычисления. ρ₃ фиксировано по литературе. Каждая точка получена новой одномерной подгонкой ρ₁ в области 0,1–100 Ом·м с начальным значением 4 Ом·м. Для этого вспомогательного профиля второй старт не применяется; глобальность каждого одномерного минимума отдельно не доказана.

Выраженный минимум означает, что данный набор наблюдений различает соседние значения ρ₂ в пределах этой модели. Почти горизонтальный участок при больших ρ₂ означает, что дальнейшее увеличение сопротивления мало меняет качество подгонки: точное большое число может определяться остановкой оптимизатора, а не информацией измерений. Кнопка «Минимум крупнее» позволяет рассмотреть эту область. Без установленной модели ошибки измерений профиль не задаёт доверительного интервала. Отрезки соединяют проверенные значения; глобальное отсутствие других минимумов между ними не доказано.''')
    code('show_fat_result(profile_figure())')
    code("""profiles=pd.read_csv(INV_OUT/'profile.csv')
for state,label in [('inhale','Вдох'),('exhale','Выдох')]:
    g=profiles[profiles.state==state].set_index('rho2')
    best_rho=float(g.rmse_ohm.idxmin())
    paragraph(f"{label}: среди восьми проверенных значений наименьшая невязка получена при ρ₂ = {num(best_rho,1)} Ом·м. При переходе от 20 к 100 Ом·м RMSE изменяется с {num(g.loc[20,'rmse_ohm'],3)} до {num(g.loc[100,'rmse_ohm'],3)} Ом. Это результат численного профиля при заданном жире, а не доверительный интервал сопротивления лёгкого.")
""")
    md(r'''### 5.6.3. Различимость трёх сопротивлений и границы вывода

Для проверки третьего неизвестного используются столбцы матрицы $J_{ij}=\partial Z_i/\partial\ln\rho_j$, где i обозначает сборку, j — мягкотканный фон, лёгкое или жир; элементы J имеют единицы Ом. Сравниваются две матрицы в **одной и той же точке** основной подгонки: первые два столбца при известном ρ₃ и все три при свободном ρ₃.

Число обусловленности κ — отношение наибольшего сингулярного числа J к наименьшему. Дополнительно рассчитывается коэффициент $g_2=\sqrt{[(J^T J)^{-1}]_{22}}$, где $g_2$ измеряется в 1/Ом и характеризует локальное усиление ошибок при оценке логарифма ρ₂. При одинаковых некоррелированных ошибках с заданным стандартным отклонением σ приближённое стандартное отклонение lnρ₂ равно σg₂. Такой шум не установлен для наших записей: g₂ здесь служит сравнением информативности, а не готовой погрешностью результата. Для решений на границе это диагностика локальной задачи без активных ограничений.

**Таблица 4л. Изменение локальной устойчивости при освобождении сопротивления жира, 50 кГц.** Рост g₂ означает, что часть информации о лёгком становится труднее отделить от неизвестного жира. Полный численный ранг трёх столбцов сам по себе не обеспечивает нужную точность и не устраняет расхождение модели с экспериментом.''')
    code('''diag=pd.read_csv(INV_OUT/'identifiability.csv').set_index('case_id');rows=[]
for r in inv_base[inv_base.frequency_hz==50000].itertuples():
    d=diag.loc[r.case_id]
    rows.append(['Вдох' if r.state=='inhale' else 'Выдох',d.condition_two,d.condition_three,d.rho2_gain_fixed_fat_per_ohm,d.rho2_gain_free_fat_per_ohm,d.rho2_gain_ratio])
table(pd.DataFrame(rows,columns=['Состояние','κ, два ρ','κ, три ρ','g₂, ρ₃ известно, 1/Ом','g₂, ρ₃ свободно, 1/Ом','Усиление g₂, раз']))
err=100*max(abs(x) for x in inv_qc['synthetic_relative_error'])
paragraph(f"**Вычислительная самопроверка.** В дополнительном синтетическом опыте заданы известные ρ₁ = 4, ρ₂ = 12 Ом·м и литературное ρ₃ при 50 кГц. Три параметра восстановлены той же моделью без шума с максимальной относительной ошибкой {err:.2e}%. Этот опыт проверяет алгоритм в одной внутренней точке; он не подтверждает истинность оценок из записей человека.")
paragraph(f"В итоговом расчёте с верхним пределом 100 Ом·м выполнено {inv_qc['total_CEM_solves']} прямых CEM-решений для подгонок и численных проверок. Прерванные предварительные запуски в это число не включены и сохранены отдельно. Максимальное расхождение результатов двух исходных приближений по логарифмам сопротивлений — {inv_qc['max_two_start_log_difference']:.2e}. Аналитические производные трёх параметров проверены центральной конечной разностью в отдельной точке ρ₁ = 3, ρ₂ = 10, ρ₃ = 23,108608 Ом·м с шагом 0,0001 по логарифму сопротивления; наибольшая относительная ошибка столбца — {max(inv_qc['derivative_relative_errors']):.2e}. Уточнение интегрирования от 1024 до 4096 точек в найденных состояниях изменяет Z не более чем на {num(inv_qc['quadrature_max_difference_ohm'],4)} Ом; принятый численный допуск выполнен. Общая сходимость FEM-сетки этим не установлена.")
''')
    code("""profile_status='выполнен во всех 16 точках' if inv_qc['profile_quadrature_all_within_tolerance'] else 'выполнен не во всех точках; отмеченные в таблице profile.csv точки требуют дополнительного численного контроля'
paragraph(f"Для вспомогательного профиля допуск интегрирования {profile_status}. Максимальное изменение Z между 1024 и 4096 точками — {num(inv_qc['profile_max_quadrature_difference_ohm'],4)} Ом. Этот контроль также не задаёт погрешность восстановленного ρ₂.")
""")
    md(r'''**Основной результат простыми словами.** При литературном сопротивлении жира модель подобрала сопротивление оставшихся мягких тканей около 4 Ом·м, но сопротивление лёгкого во всех четырёх основных подгонках достигло заданного потолка 100 Ом·м. При этом весь размерный ряд по-прежнему не согласован с экспериментом: невязка составляет около 10–11 Ом. При фиксированном литературном сопротивлении жира физиологически обоснованная совместная оценка ρ₁ и ρ₂ не получена. Равенство граничных значений ρ₂ на вдохе и выдохе нельзя трактовать как отсутствие дыхательного изменения лёгкого.

Освобождение сопротивления жира также не дало обоснованной физиологической тройки: невязка уменьшилась до 4,92 Ом на вдохе и 4,75 Ом на выдохе, но ρ₂ перешло на нижний предел 0,1 Ом·м, ρ₁ выросло до 17–22 Ом·м, а ρ₃ снизилось примерно до 2,1 Ом·м. Эти значения резко отличаются от принятых литературных ориентиров. Расчёт показывает возможность улучшить прогноз Z за счёт неубедительных тканевых параметров; конкретную причину расхождения модели и измерений он не устанавливает.

**Практический вывод.** Литературное ρ₃ можно использовать как явно заданное условие двухпараметрического восстановления. Считать его точно известным свойством данного человека оснований нет. Добавление третьего неизвестного допустимо как исследовательская проверка, но требует оценки устойчивости и подтверждения, что найденное значение не компенсирует другие ошибки модели или измерений. Литературные интервалы служат только оценке результата. Вычислительные границы, остановка на плато и небольшая невязка не заменяют проверки идентифицируемости.

**Что ещё не получено.** КТ определяет геометрию, но не электрические свойства тканей. Этот опыт не даёт независимой проверки истинных сопротивлений и не подтверждает достаточность двух сборок. До интерпретации дыхательных изменений необходимо получить согласованное восстановление базовых параметров и проверить сохранение выводов при допустимых свойствах жира. Новый поиск положения и пульсовая задача здесь не выполнялись.

[Расчётные таблицы, проверки и команды повторения](../MATLAB_TRKG4_real_subjects/output/exploratory/lateral_fat_inverse_20260916/README.md) сохраняют происхождение входов и условия каждого варианта.''')
