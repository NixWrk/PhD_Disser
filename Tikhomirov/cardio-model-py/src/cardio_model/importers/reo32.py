"""Импорт сигналов с импедансометра REO-32.

Порт `Kernel/history/ReoImporter.m` + Reo32-helpers из `utils/MyFunction.m`.
Реализация — `Фаза 6` (#45). Реальных файлов REO-32 нет — валидация
будет на синтетике, помечается как «требует валидации».
"""
