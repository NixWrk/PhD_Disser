"""Sequential tissue restoration, section 5.4 of notebook 33.09."""


def result_sections(md,code):
    md(r'''## 5.4. Влияние замены костей и сердца мягкими тканями

Изменение частоты и небольшое уточнение формы лёгкого не устранили расхождение с экспериментом. Следующая проверка касается состава тканей. Сравниваются **три модели при фиксированном C01**: общий фон и лёгкое; те же области с отдельными костями; затем — с отдельными костями и сердцем. Используется исходная КТ-маска лёгкого, без дополнительного упрощения её формы.

Внешняя поверхность, лёгкое, сетка, четыре конечных контакта диаметром 5 мм и контактное сопротивление одинаковы. Сопротивления неизменяемых материалов сохраняются. Положение и параметры по эксперименту не подбираются. Поэтому разность прогнозов характеризует конкретную замену свойств органа.

**Таблица 4в. Литературные сопротивления материалов.** Значения вычислены как обратные действительной проводимости из той же сохранённой версии [IT’IS V5.0](https://itis.swiss/virtual-population/tissue-properties/downloads/database-v5-0), что в разделе 5.3, с использованием [дисперсионной модели Gabriel](https://itis.swiss/virtual-population/tissue-properties/database/dielectric-properties). Это входы сценария, а не измеренные свойства добровольца.''')
    code('''sys.path.insert(0,str(pipe/'tools'))
import lateral_tissue_ablation as tissue_study
tissue_out=tissue_study.OUT
tissue_qc=json.loads((tissue_out/'qc.json').read_text(encoding='utf8'))
assert tissue_qc['complete'] and tissue_qc['analysis_script_sha256']==sha(pipe/'tools/lateral_tissue_ablation.py')
for name,digest in {**tissue_qc['input_sha256'],**tissue_qc['output_sha256']}.items(): assert sha(tissue_out/name)==digest
tissue_plan=json.loads((tissue_out/'plan.json').read_text(encoding='utf8'))
for item in tissue_plan['sources']: assert sha(pipe/item['path'])==item['sha256']
tissue_execution=json.loads((tissue_out/'execution.json').read_text(encoding='utf8'))
assert tissue_execution['identity']['worker_sha256']==sha(pipe/'src/run_lateral_tissue_ablation.m')
labels={'Muscle':'Мышца: оставшийся фон','Lung (Inflated)':'Лёгкое: inflated','Lung (Deflated)':'Лёгкое: deflated',
        'Bone (Cortical)':'Кость: кортикальная','Heart Muscle':'Сердце: сердечная мышца'}
tv=pd.read_csv(tissue_out/'literature_values.csv').pivot(index='tissue',columns='frequency_hz',values='rho_ohm_m').loc[list(labels)]
tv.index=tv.index.map(labels);tv=tv.reset_index();tv.columns=['Материал','ρ при 50 кГц, Ом·м','ρ при 100 кГц, Ом·м'];table(tv)
''')
    md(r'''Маска кости целиком получает свойства кортикальной кости, без разделения губчатого вещества и костного мозга. Маска сердца целиком получает свойства сердечной мышцы, без выделения крови. Эти назначения соответствуют типам материалов прежней КТ/FEM-модели и остаются приближениями. Проверка сердца относится именно к такому описанию органа.

Старая метка кости пересекается с текущей замкнутой маской лёгкого в девяти исходных элементах общим объёмом 0,540 мл. В них сохранена принадлежность лёгкому, чтобы не изменить референс раздела 5.3. В остальных элементах восстановлены исходные метки органов. Все узлы, тетраэдры и граничные треугольники уточнённой сетки совпадают с предыдущим расчётом.

Для двух частот, двух табличных состояний лёгкого, трёх моделей и девяти размеров выполнено **108 прямых FEM/CEM-решений**. Ещё 27 решений проверяют совпадение моделей при равенстве сопротивлений органов и фона; девять — производные по четырём материалам. Глобальная пространственная сходимость этим не устанавливается.

Последовательные эффекты определяются разностями:

$$\Delta Z_{\mathrm{кость},i}=Z_{1,i}-Z_{0,i},\qquad
\Delta Z_{\mathrm{сердце}\mid\mathrm{кость},i}=Z_{2,i}-Z_{1,i},$$

где i — номер размера сборки; $Z_{0,i}$ — импеданс двухтканной модели, $Z_{1,i}$ — модели с костями, $Z_{2,i}$ — модели с костями и сердцем, Ом. Положительная разность означает увеличение прогноза после выделения органа. Эти величины не являются независимыми долями общего импеданса: при другой последовательности промежуточные разности могут измениться.''')
    code('''tissue_fig=tissue_study.figure()
display(HTML(tissue_fig.to_html(full_html=False,include_plotlyjs='inline',config={'displaylogo':False,'responsive':True})))
''')
    md(r'''**Рисунок 6. Последовательное восстановление электрических свойств костей и сердца.** Верхние панели сопоставляют три расчётные модели с экспериментом: слева — вдох, справа — условное соответствие категории deflated выдоху. Нижние показывают изменения после выделения каждого органа. Частота переключается в списке; экспериментальные точки остаются теми же, поскольку новая запись на другой частоте не проводилась.

Верхние панели позволяют оценить, приближает ли уточнение тканей расчёт к наблюдениям. Нижние раскрывают различия, почти невидимые на масштабе абсолютного импеданса. Для подробного просмотра сердца можно скрыть кривую изменения от костей нажатием на её название. При наведении показаны изменение в омах и процент от предыдущей модели. Отрезки соединяют дискретные размеры. Геометрия вдоха используется в обоих электрических состояниях; категория deflated не установлена как эквивалент выдоха добровольца.''')
    code('''tissue_inc=pd.read_csv(tissue_out/'increments.csv')
tissue_metrics=pd.read_csv(tissue_out/'metrics.csv')
for f in [50000,100000]:
 g=tissue_inc[tissue_inc.frequency_hz==f]
 paragraph(f"**При {f//1000} кГц** выделение костей изменяет импеданс на **{num(g.bone_delta_ohm.min(),3)}…{num(g.bone_delta_ohm.max(),3)} Ом** "
  f"({num(g.bone_delta_pct.min(),2)}…{num(g.bone_delta_pct.max(),2)}%). "
  f"Дополнительное выделение сердца — на **{num(g.heart_after_bone_delta_ohm.min(),4)}…{num(g.heart_after_bone_delta_ohm.max(),4)} Ом** "
  f"({num(g.heart_after_bone_delta_pct.min(),3)}…{num(g.heart_after_bone_delta_pct.max(),3)}%). Диапазоны охватывают девять размеров и два табличных состояния лёгкого.")
paragraph('**Таблица 4г. Среднеквадратичное расхождение с экспериментом, Ом.** Меньшее число означает более близкий прогноз для тех же девяти измерений.')
tm=tissue_metrics.pivot(index=['frequency_hz','state'],columns='model',values='rmse_ohm').reset_index()
tm=tm[['frequency_hz','state','two_tissue','bones','bones_heart']]
tm['frequency_hz']=tm.frequency_hz/1000;tm['state']=tm.state.map({'inhale':'Вдох','exhale':'Условный выдох'})
tm.columns=['Частота, кГц','Состояние','Фон + лёгкое','+ кости','+ кости + сердце'];table(tm)
paragraph(f"Повторный двухтканный расчёт воспроизводит исходную КТ-модель раздела 5.3 с максимальным расхождением {tissue_qc['prior_reference_max_difference_ohm']:.2e} Ом. "
 f"При равенстве свойств органов и фона различие трёх реализаций не превышает {tissue_qc['equal_material_max_difference_ohm']:.2e} Ом. "
 f"Максимальная относительная невязка системы составляет {tissue_qc['max_system_relative_residual']:.2e}; ошибка взаимности — {tissue_qc['max_reciprocity_abs_ohm']:.2e} Ом.")
''')
    md(r'''**Вывод по прямой задаче.** В проверенных условиях кости влияют на импеданс сильнее сердца, однако оба изменения значительно меньше расхождения с экспериментом. Их объединение с фоном не объясняет основную часть занижения прогноза при выбранных литературных сопротивлениях. Малый эффект сердца поддерживает его упрощение именно для этих базовых расчётов; допустимость для точного восстановления ρ₂ требует дополнительной проверки.

Для обратной задачи рассчитано локальное смещение, которым сокращённая модель могла бы компенсировать изменение импедансов:

$$\delta\boldsymbol\ell=\underset{\mathbf x}{\operatorname{argmin}}\,\|J\mathbf x-\Delta\mathbf Z\|_2^2,$$

где $\delta\boldsymbol\ell$ — изменения логарифмов ρ₁ и ρ₂; J — матрица чувствительности сокращённой модели в исходной литературной точке, Ом; $\Delta\mathbf Z$ — разность более подробного и сокращённого прогнозов для девяти размеров, Ом; $\|\cdot\|_2$ — евклидова норма. Малые $\delta\ell_j$ приблизительно равны относительным изменениям сопротивлений. Это линеаризация без нелинейной подгонки и без экспериментального шума. Большое значение показывает недостаточность локального приближения; его нельзя выдавать за найденное сопротивление.''')
    code('''tissue_bias=pd.read_csv(tissue_out/'local_inverse_diagnostic.csv')
for label,text in [('omit_bone','костей'),('omit_heart_after_bone','сердца при сохранённых костях')]:
 g=tissue_bias[tissue_bias.comparison==label]
 paragraph(f"При замене **{text}** фоном диагностическое смещение δlnρ₂ составляет "
  f"**{num(g.delta_logrho2.min(),4)}…{num(g.delta_logrho2.max(),4)}**, а δlnρ₁ — "
  f"**{num(g.delta_logrho1.min(),4)}…{num(g.delta_logrho1.max(),4)}** по четырём литературным состояниям. "
  "Это оценка направления и масштаба возможного смещения, а не результат полного восстановления.")
''')
    md(r'''Большое локальное смещение для костей требует нелинейной проверки. Поэтому при **50 кГц** выполнены шесть синтетических подгонок: для двух табличных состояний лёгкого последовательно проверены замена костей фоном, замена сердца фоном при сохранённых костях и замена обоих органов. Входные Z получены более подробной моделью с известными сопротивлениями; затем сокращённая модель подбирает общую пару ρ₁, ρ₂ по девяти размерам. На каждой итерации выполняется прямая FEM/CEM-задача, без интерполяционной библиотеки.

Для каждой подгонки использованы два начальных приближения. Диапазон ρ₁ равен 1,377–10 Ом·м; диапазон ρ₂ — 3,817–24,074 Ом·м и охватывает оба выбранных табличных состояния. Это границы вычислительного сценария, не индивидуальные нормы. Положение, контакты и свойства сохранённых органов фиксированы; шум отсутствует. Поэтому ошибки сопротивлений характеризуют именно проверяемое упрощение в идеализированных условиях. **Полученные оценки не являются сопротивлениями добровольца из экспериментальных записей.**

**Таблица 4д. Нелинейная ошибка восстановления после замены органов фоном, 50 кГц.** Ошибка каждого сопротивления вычислена относительно известного входного значения в процентах; положительный знак означает завышение. Последний столбец показывает остаточное среднеквадратичное расхождение девяти импедансов. Малое расхождение Z при большой ошибке ρ₂ означает, что сокращённая модель компенсирует отсутствие органа изменением параметра лёгкого.''')
    code('''import lateral_tissue_inverse_analysis as tissue_inverse
inverse_check=json.loads((tissue_out/'synthetic_inverse_validation.json').read_text(encoding='utf8'))
assert inverse_check['complete'] and inverse_check['analysis_source_sha256']==sha(pipe/'tools/lateral_tissue_inverse_analysis.py')
for name,digest in inverse_check['inputs_sha256'].items(): assert sha(tissue_out/name)==digest
inverse_qc=json.loads((tissue_out/'synthetic_inverse_qc.json').read_text(encoding='utf8'))
assert inverse_qc['identity']['worker_sha256']==sha(pipe/'src/run_lateral_tissue_inverse_probe.m')
ti=pd.read_csv(tissue_out/'synthetic_inverse.csv')
tb=ti[['state','comparison','error_rho1_pct','error_rho2_pct','fit_rmse_ohm']].copy()
tb['state']=tb.state.map({'inhale':'Вдох','exhale':'Условный выдох'})
tb['comparison']=tb.comparison.map({'omit_bone':'Кости → фон','omit_heart_after_bone':'Сердце → фон; кости сохранены','omit_both':'Кости и сердце → фон'})
tb.columns=['Состояние','Упрощение','Ошибка ρ₁, %','Ошибка ρ₂, %','Невязка Z, Ом'];table(tb)
for key,label in [('omit_bone','Замена костей фоном'),('omit_heart_after_bone','Замена сердца фоном при сохранённых костях')]:
 g=ti[ti.comparison==key]
 paragraph(f"**{label}** даёт ошибку ρ₂ от **{num(g.error_rho2_pct.min(),2)} до {num(g.error_rho2_pct.max(),2)}%**, "
  f"при остаточной невязке Z {num(g.fit_rmse_ohm.min(),4)}–{num(g.fit_rmse_ohm.max(),4)} Ом. "
  f"Максимальная абсолютная ошибка ρ₁ — {num(g.error_rho1_pct.abs().max(),3)}%.")
paragraph(f"Все шесть подгонок завершились успешно. Максимальное различие решений из двух начальных приближений по логарифмам параметров — {inverse_check['max_two_start_log_difference']:.2e}. "
 f"Число решений на границе диапазона — {int(ti.bound_hit.sum())} из шести. "
 f"Для нелинейной проверки выполнено ещё {inverse_qc['CEM_solves']} прямых CEM-решений. Два совпавших старта не являются доказательством глобального минимума.")
''')
    md(r'''В этих расчётах изменение Z от костей в целом растёт с размером сборки. Сокращённая модель воспроизводит близкую размерную зависимость, повышая сопротивление лёгкого. Малый остаток при неверном ρ₂ показывает, что отсутствие костей может быть компенсировано тканевым параметром и почти не обнаруживаться по одной величине невязки.

**Вывод для обратной задачи.** В проверенных синтетических состояниях замена костей фоном вызывает существенное завышение сопротивления лёгкого, хотя импедансы согласуются очень близко. Поэтому малое изменение базового Z не обосновывает исключение костей при восстановлении ρ₂. Эффект сердца заметно меньше; решение о его исключении следует принимать относительно заданной допустимой ошибки сопротивлений. Проверка не распространяется автоматически на другие тканевые параметры и положения сборок. Наборы размеров в разделах 3–4 выбраны для двухтканной модели; их оптимальность после выделения костей требует повторной проверки.''')
    md(r'''**Граница вывода.** Решение о допустимости замены органа фоном должно учитывать ошибки Z и обоих восстанавливаемых сопротивлений. Этот опыт не устанавливает допустимое упрощение для всего физиологического диапазона, других людей, иных мест монтажа или сердца с отдельными кровяными полостями. Улучшение совпадения с наблюдениями не доказывает истинность входных сопротивлений. Полные численные разности и локальная диагностика сохранены в [таблицах этого сравнения](../MATLAB_TRKG4_real_subjects/output/exploratory/lateral_tissue_ablation_20260916/README.md).

**Следующая постановка локализации.** После отдельной проверки жира положение и сопротивления следует подбирать совместно по всему вектору базовых измерений:

$$\min_{\boldsymbol\theta,\rho_1,\rho_2}\ \sum_{i=1}^{9}
\left[Z_i^{\mathrm{FEM}}(\boldsymbol\theta,\rho_1,\rho_2)-Z_i^{\mathrm{эксп}}\right]^2,$$

где $\boldsymbol\theta$ — общий центр и ориентация монтажа на КТ; ρ₁ и ρ₂ — неизвестные сопротивления фона и лёгкого, Ом·м; $Z_i^{\mathrm{эксп}}$ — базовый импеданс i-й сборки, Ом. Свойства отдельно выделенных органов задаются литературным сценарием; размеры и конечные площади контактов — условиями эксперимента. Равные веса определяют вычислительный критерий и не означают доказанной одинаковой дисперсии ошибок.

Для одного дыхательного состояния ищется **общая пара ρ₁ и ρ₂ для всех сборок**. Собственная пара под каждый отдельный Z не определяется одним числом. Практически для каждого допустимого положения можно сначала минимизировать невязку по сопротивлениям в обоснованных границах, затем сравнить полученные минимумы положений. Сопротивления не фиксируются значениями прежней подгонки. Для вдоха и выдоха задаются отдельные пары при явном сценарии геометрии и связи ρ₁ между состояниями. Это следующий этап; в настоящем сравнении C01 не переоптимизировалось. При известной КТ толщина h вычисляется для каждого кандидата положения; дополнительную независимую h в эту постановку автоматически не вводят.''')

