from pathlib import Path
import nbformat

PROJECT = Path(__file__).resolve().parents[3]
COLAB = PROJECT / 'Colab Notebooks'
NAME = '20.10_КТ_FEM_локализация_боковых_матриц_МГТУ'
LINK = f'[{NAME.split("_")[0]} — полный научный отчёт]({NAME}.ipynb)'


def edit(path, old, new):
    s = path.read_text(encoding='utf-8')
    assert s.count(old) == 1, (path, old[:80], s.count(old))
    path.write_text(s.replace(old, new), encoding='utf-8')


(COLAB / (NAME + '.md')).write_text(f'''# 20.10 КТ/FEM-локализация и сравнение моделей боковых сборок МГТУ

> **Статус на 09.09.2026:** полный выполненный отчёт включён в основную
> последовательность. Сохранены проверенный численный кандидат исходной
> обратной задачи и сравнение двухтканных моделей. Физическая валидация позы,
> оптимальное упрощение и допустимый диапазон размеров не установлены.

{LINK} · [HTML для чтения]({NAME}.html) ·
[Карта проекта](00.00_Карта_проекта.md).

## Место в последовательности

Этап принимает индивидуальную геометрию серии `20.01–20.04` и агрегированные
импедансы эксперимента 2 с установленным происхождением записей и разметки.
Файловый QC принадлежит `10.01`, дыхательная разметка — `11.01`, а проверка
уровней на выбранных интервалах — `12.01`. Наличие локальных агрегатов ещё не
означает принятия всех контрактов этой строгой последовательности.

Внутри полного отчёта сначала описываются геометрия и обратная задача,
затем — аналитические модели, сравнение с двухтканным FEM и программа выбора
упрощения. Для чтения формул предварительно полезны
[30.01 — прямая двуслойная модель](30.01_Прямая_двуслойная_модель_боковой_сборки.md)
и [30.04 — вычислительное ядро](30.04_Вычислительное_ядро_двуслойной_модели.ipynb).
Это перекрёстная зависимость теоретического и анатомического блоков; номер
20.10 задаёт владельца индивидуального расчёта.

| Следующий этап | Что получает из 20.10 | Что должен установить самостоятельно |
|---|---|---|
| [20.11 — чувствительность](20.11_КТ_FEM_чувствительность_боковых_сборок.ipynb) | Геометрию, контакты, фиксируемого кандидата и диагностику конечных секущих | Производные, нелинейность, влияние контакта и геометрии, сеточную сходимость |
| [20.12 — применимость упрощений](20.12_Критерий_Lmax_плоскослоистой_модели.ipynb) | Сравнение десяти вариантов и программу разделов 18–19 полного отчёта | Проверенное семейство упрощений, ошибку восстановленных параметров, стоимость и допустимые размеры |
| [32 — выбор размеров](32.02_Выбор_пары_при_известной_h.ipynb) | После 20.11/20.12 — пригодный оператор и область его применимости | Информативную пару или набор; наличие локального кандидата этого не решает |
| [34.03 — КТ-референс](34.03_КТ_как_референс_разработки.md) | Прослеживаемый индивидуальный вычислительный пример | Независимую проверку анатомии и применимости тканевых оценок |

## Что выполнено

Исправленная обратная задача использует девять размеров: для данного
добровольца сохранена независимая запись 90 мм, а поздняя копия 100 мм
исключена. Сохранены контракт с контрольными суммами, геометрический контроль
контактов, история поиска и диагностика секущих с уменьшением шага.
Оценки Крамера–Рао для позы не выдаются. Старые ограничения, связанные с
обратным выбором 90/100 мм и отсутствием контрактов, относятся к состоянию
до исправлений; их история сохранена в отчёте и локальном снимке миграции.

Исходный кандидат получен в четырёхматериальной модели с двумя подбираемыми
сопротивлениями. Отдельное сравнение раздела 11.3.1 по решению автора содержит
ровно два материала: лёгкое и все остальные ткани, включая поддиафрагмальную
область. Трёхслойность означает последовательность «мягкие ткани — лёгкое —
мягкие ткани». Значения сопротивлений перенесены из исходного кандидата без
повторной подгонки. Такое сравнение не является новой двухтканной инверсией.

Для этого примера добавление конечной толщины лёгкого через выбранное
усреднение не улучшило согласие с двухтканным FEM. Это ограничение проверенной
эвристики. Оно не устанавливает оптимальное упрощение и не исключает методов
с пространственно согласованными кривыми границами.

## Входы и происхождение

- Индивидуальная КТ на вдохе и ручная сегментация Inobitec задают происхождение
  геометрии. В сохранённом отчёте DICOM не загружался заново; электрические
  расчёты используют подготовленные STL, сетку и тканевые области.
- Геометрия опорной сборки задаёт исходные центр и ось. Общая поза остальных
  размеров является модельной параметризацией; её нельзя возвращать в `20.02`
  как независимо измеренную регистрацию электродов.
- Агрегированные импедансы и дыхательные состояния относятся к эксперименту 2,
  реокардиомонитору МГТУ. Приборные значения принимаются в заявленных единицах
  по решению автора; адресные флаги конкретных записей сохраняют свою роль.
- Частота расчёта, площадь и контактные параметры, свойства и правила назначения
  материалов, версия сетки и источники результатов описаны в полном отчёте.
  Проверка хешей подтверждает целостность записанного набора, но не заменяет
  независимую метрологическую или анатомическую проверку.

## Что остаётся открытым

Не завершены сходимость FEM по сетке и внешней границе, независимая проверка
позы, оценка неопределённости сегментации и контакта, разделение ошибок
точечных и конечных электродов, а также перенос на другую анатомию и сессию.
Конечные секущие по дискретным площадкам не являются подтверждёнными
производными непрерывного положения. Совпадение базового импеданса не
устанавливает точность тканевых или пульсовых оценок.

Эти вопросы распределены между `20.11`, `20.12` и `34`. Полноценный численный
прогон отдельной чувствительности и выбор упрощения остаются предстоящими
этапами. Число материалов в будущих сравнениях фиксируется до расчёта;
двухтканная классификация этого примера не переносится молча на исходную
трактовку мягкотканного слоя без костей в `30.01`.

## Канонические файлы и воспроизведение

Полный ноутбук и его читательский HTML находятся в `Colab Notebooks`.
Этот Markdown является паспортом этапа и не дублирует расчёт.
Генератор, текст-источник и MATLAB/Python-ядро остаются в
`MATLAB_TRKG4_real_subjects`; точка входа поля — `run_pipeline.m`.
Команды описаны в [документации расчётных стадий](../MATLAB_TRKG4_real_subjects/docs/PIPELINE_FOR_NOTEBOOKS.md).

При переносе сохранены вычисленные выходы; изменены только поиск каталога,
навигация и проверка происхождения версии скрипта сравнения. Старый путь
ноутбука содержит ссылку на 20.10 без вычислительных ячеек. Нового FEM или
поиска параметров при интеграции не выполнялось.

Полная локальная версия содержит индивидуальную анатомию и остаётся вне Git.
Её присутствие в основной рабочей папке не является разрешением на публикацию.
В Git сохраняются паспорт, генератор и безопасное описание последовательности.
''', encoding='utf-8')

route = f'''## Маршрут КТ-проверки и упрощения боковых моделей

1. `20.01–20.04` — происхождение КТ, регистрация и сценарии свойств;
   `10.01 → 11.01 → 12.01` — отдельная цепочка проверки электрических входов.
2. {LINK} — индивидуальная геометрия, кандидат позы и сравнение моделей;
   аналитическая основа читается в `30.01` и `30.04`.
3. [20.11](20.11_КТ_FEM_чувствительность_боковых_сборок.ipynb) — отдельная
   проверка чувствительности, нелинейности и численной сходимости.
4. [20.12](20.12_Критерий_Lmax_плоскослоистой_модели.ipynb) — выбор и проверка
   упрощения по ошибке импеданса, тканевых оценок и вычислительной стоимости;
   для плоской модели — допустимое множество размеров и, если применимо, `Lmax`.
5. `31–32 → 33 → 34` — анализ устойчивости, выбор размеров, оценка параметров
   и независимая проверка перед передачей тканевых вкладов в ТТРКГ.

Раздел 11.3.1 отчёта 20.10 содержит выполненный двухтканный пример; разделы
18–19 задают программу следующих проверок. Они не заменяют завершения 20.11
и 20.12. Полный отчёт имеет одного владельца, а соседние этапы ссылаются на
его результаты и не копируют вычисления. Это порядок исследования, а не
разрешение автоматически присвоить входам статус `accepted`.

'''

p=COLAB/'README.md'
s=p.read_text(encoding='utf-8')
old=next(line for line in s.splitlines() if line.startswith('| `20` |'))
new=f'| `20` | DICOM, Inobitec, регистрация электродов и проверка пространственных моделей | `20.01–20.04` задают исходные КТ-контракты; `20.07` сохраняет кандидатную автоматическую сегментацию. {LINK} содержит выполненный индивидуальный расчёт с исправленным размерным рядом, контролем контактов и сравнением двухтканных моделей. `20.11` остаётся контрактом отдельной чувствительности; `20.12` — контрактом применимости и выбора упрощения. Оптимальное семейство и численный $L_{{max}}$ не установлены |'
s=s.replace(old,new)
anchor='## Данные, конфигурация и производные артефакты'
assert s.count(anchor)==1;s=s.replace(anchor,route+anchor)
p.write_text(s,encoding='utf-8')

p=COLAB/'00.00_Карта_проекта.md'
s=p.read_text(encoding='utf-8');anchor='## Статусы научных утверждений';assert s.count(anchor)==1
s=s.replace(anchor,route+anchor)
p.write_text(s,encoding='utf-8')

p=COLAB/'TODO.md';s=p.read_text(encoding='utf-8')
anchor='## P1 — КТ, боковые сборки и FEM\n'
items=f'''
- [x] Включить {LINK} в основную последовательность с сохранением результатов,
  единым владельцем, обновлёнными путями генерации и обратными ссылками.
- [x] Зафиксировать исправленный размерный ряд и выполненное сравнение двух
  материалов; конечные секущие хранить как диагностику, без CRLB положения.
- [ ] В `20.11` отдельно проверить чувствительности, нелинейность, контакты,
  сходимость сетки и влияние удалённой границы двухтканного референса.
- [ ] В `20.12` согласовать контактные модели, сравнить слоистые, криволинейные
  и вычислительно сокращённые варианты по протоколу раздела 19 отчёта 20.10.
  Определить ошибку параметров на проверочных случаях и стоимость; после
  выбора допустимых ошибок установить область применимости.
- [ ] Передать в серию `32` проверенный оператор и допустимые размеры;
  выполненное сравнение одного случая не считать выбором оптимальной пары.
'''
assert s.count(anchor)==1;s=s.replace(anchor,anchor+items)
p.write_text(s,encoding='utf-8')

p=COLAB/'00.02_Реестр_доказательств_и_ограничений.md';s=p.read_text(encoding='utf-8')
anchor=next(line for line in s.splitlines() if line.startswith('| Двуслойная модель описывает'))
rows=f'''| Полный индивидуальный отчёт зарегистрирован как {LINK} | Факт о структуре и выполненной внутренней проверке | Сохранённые результаты, контракт происхождения и проверка переноса | Один пример эксперимента 2; физическая валидация не завершена | Продолжить 20.11 и 20.12; не присваивать позе статус независимо измеренной геометрии |
| Двухтканное сравнение 20.10 объединяет все нелёгочные области, включая поддиафрагмальные | Авторское допущение модели | Раздел 11.3.1 отчёта 20.10 | Два сопротивления при двух, трёх или нескольких пространственных слоях | Согласовывать материальную классификацию перед переносом в 30–34 |
| Добавление конечного лёгочного интервала через проверенное усреднение не улучшило согласие с двухтканным FEM | Отрицательный результат математического сравнения | Десять прямых вариантов при фиксированных сопротивлениях | Один набор геометрии и контактов; RMS содержит различия контакта и пространства | В 20.12 разделить факторы и проверить иные упрощения; оптимум не установлен |'''
s=s.replace(anchor,anchor+'\n'+rows);p.write_text(s,encoding='utf-8')

p=COLAB/'34.03_КТ_как_референс_разработки.md';s=p.read_text(encoding='utf-8')
s=s.replace('> результаты по действующим входам ещё не собраны в воспроизводимый артефакт.',
            f'> вычислительный пример зарегистрирован в {LINK}; независимая анатомическая\n> и электрическая валидация этого примера не завершена.')
s=s.replace('До его\nпоявления КТ/FEM-ветка имеет статус `not_tested`, а исторические совпадения\nчисел — `legacy_unvalidated`.',
            'Выполненный пример `20.10` имеет статус численного кандидата: его внутренние\nпроверки и происхождение зарегистрированы, но критерии внешней валидации\nнастоящего контракта не выполнены. Старые совпадения сохраняют свой\nисторический статус и не подменяют этот пример.')
s+='''\n## Связь с проверкой упрощённых моделей\n\nЭтап 20.10 владеет полным отчётом и индивидуальным сравнением. Этап 20.11\nпроверяет чувствительности; 20.12 определяет пригодное упрощение и диапазон\nразмеров. В 34.03 поступают их версионированные результаты для независимой\nпроверки. Двухтканное объединение всех нелёгочных областей в 20.10 необходимо\nуказывать явно; оно отличается от анатомической трактовки мягкотканного\nслоя без костей в исходной аналитической постановке 30.01.\n'''
p.write_text(s,encoding='utf-8')

p=COLAB/'ЖУРНАЛ_РЕШЕНИЙ.md'
with p.open('a',encoding='utf-8') as f:f.write(f'''\n\n## 09.09.2026 — полный отчёт о боковых сборках включён в 20.10\n\n- По запросу автора полная научная редакция включена как {LINK}. Номер выбран\n  по владельцу КТ-геометрии и обратной локализации. Старый путь оставлен\n  указателем, без второй вычислительной версии.\n- Сохранены результаты всех 25 вычислительных ячеек. Изменения кода ограничены\n  разрешением путей и проверкой сохранённой версии производителя сравнения;\n  нового FEM или поиска параметров не выполнялось.\n- Обновлены паспорт 20.10, README, карта, реестр, TODO и связи 20.11/20.12/34.03.\n  Сравнение раздела 11.3.1 является исходным примером для будущей проверки\n  упрощений. Оптимальная модель, Lmax и пара размеров не объявлены найденными.\n- Устаревшие утверждения о неверной атрибуции 90/100 мм и выдаче CRLB\n  заменены текущим статусом исправлений. Сеточная сходимость и независимая\n  валидация остаются открытыми. Полная локальная анатомическая версия\n  исключена из Git; исходные файлы сохранены в локальном снимке с хешами.\n''')

# Ignore only the local full anatomical reader artifacts; the safe passport
# and generator remain versionable. No index or commit operation is performed.
p=PROJECT/'.gitignore'
with p.open('a',encoding='utf-8') as f:f.write(f'\n# Local full anatomical report, canonical reading position 20.10\n/Colab Notebooks/{NAME}.ipynb\n/Colab Notebooks/{NAME}.html\n')

print('Core passport, sequence, evidence registry, TODO and decision journal updated.')
