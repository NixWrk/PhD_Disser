# 21.00. 4D-сердце: сегментация, просмотр, R–R и проверка масок

Общая точка входа серии `21` в единой папке
`Colab Notebooks/cardiac_4d_segmentation/`. Структура сверена с файлами, ячейками
ноутбуков, генераторами и манифестами 09.09.2026. Это техническая сборка
существующей ветки; её научный статус остаётся **разработкой методики**.

[Карта всего проекта](../00.00_Карта_проекта.md) ·
[Инструкции вычислительного блока](README.md)

## Что открывать

1. [21.01 — интерактивное 4D-сердце](21.01_4D_сердце_интерактивно.ipynb)
   ([HTML](21.01_4D_сердце_интерактивно.html)): общий обзор всего сердца,
   крови четырёх камер и модельного миокарда.
2. [21.03 — камеры, геометрический остаток и R–R](21.03_Вычитание_крови_и_объёмы_камер_RR.ipynb)
   ([HTML](21.03_Вычитание_крови_и_объёмы_камер_RR.html)): совместный просмотр
   3D, временных меток КТ, растровых полос GE и объёмов камер. В начале —
   условные оценки УО/ФВ ЛЖ и ПЖ, выбранные фазы, сравнение вариантов масок
   и КТ для проверки наблюдаемых экстремумов. Добавлен отдельный
   [Пилот Георга: объёмы и срезы](heart_rr_analysis/function_analysis/georg_pilot_20260910/index.html); улучшение масок не подтверждено.
3. [21.04 — научное описание и ограничения RR-разбора](21.04_heart_subtraction_and_RR_report.md).
4. [21.05 — независимая валидация масок](21.05_Валидация_анатомической_сегментации_сердца_4D_КТ.md),
   [21.06 — покамерный QC крови](21.06_Точная_сегментация_крови_четырёх_камер_4D_КТ.md),
   [21.07 — сравнение кандидатов](21.07_Бенчмарк_автоматизированной_сегментации_крови_4D_КТ.md).

**Подтверждённый факт о реализации:** `21.01` и `21.03` — генерируемые
ноутбуки с сохранёнными представлениями. На момент аудита все их кодовые
ячейки содержат комментарии, а результаты создаются внешними Python-скриптами.
Команда «выполнить все ячейки» не пересчитывает эту ветку. Для сохранения
вывода и внесённых правок использовать генераторы, описанные ниже.

## Текущий диагностический разбор

[Совместная динамика ЛЖ/ПЖ](heart_rr_analysis/function_analysis/joint_dynamics/index.html)
встроена в 21.03: 33 парных межкадровых интервала и 18 пространственных
сравнений на девяти выбранных интервалах. Новые ручные маски не ожидаются;
расчёт продолжается на исходных масках TotalSegmentator. Изменения границ
не названы измеренной ошибкой сегментации, численные поправки не введены.

## К каким ноутбукам относятся папки

| Папка | Владелец и назначение | Что находится внутри |
|---|---|---|
| [cardiac_4d_segmentation](README.md) | Общий вычислительный блок `20.05–20.07` и `21.01–21.07` | DICOM/NIfTI, сегментация v1/v2, анатомия, QC, построение представлений, RR-анализ, подготовка ручной проверки и бенчмарк; также локальные окружения и кэши |
| [4D_сердце_интерактивно](4D_сердце_интерактивно/README.md) | `21.01`, пояснение — [21.02](21.02_Интерактивная_4D_визуализация_и_анатомия_TotalSegmentator.md) | Три HTML-анимации, `manifest.json`; отдельно — таблицы и манифест QC окружающей анатомии на `phase_00` |
| [heart_rr_analysis](heart_rr_analysis/README.md) | `21.03`, пояснение — `21.04` | Три синхронных 3D-представления, три графика объёмов, шесть CSV, RR-сводка и обезличенные растровые полосы GE в `ecg_traces` |
| Внешний `<DERIVED_ROOT>` | Входы и производные маски вычислительной ветки | `<subject>/phases`, `automatic_masks_v2`, `products_v2`, `heart_minus_blood`; временные метаданные в `_cohort_qc_v2` |

Эти папки выполняют разные роли и не являются тремя копиями одного расчёта.
Локальный `derived` содержит каталог `inventory`;
его нельзя автоматически принимать за полный внешний `<DERIVED_ROOT>`.
Наличие внешних каталогов фаз и двух временных манифестов проверено;
полнота каждого NIfTI и его содержимое этим аудитом не проверялись.

## Единая последовательность и владельцы результатов

| Этап | Документ или ноутбук | Исполняемый код | Передаваемый результат |
|---|---|---|---|
| Историческая сегментация v1 | [20.05](20.05_4D_КТ_сегментация_сердца.md) | `cardiac4d.py`, `segment_totalseg_v1.py` | Исторический baseline; не подставлять вместо v2 |
| Фазы КТ и сегментация v2 | [20.06](20.06_4D_КТ_сегментация_v2_полное_сердце.md) | `cardiac4d.py inventory/convert`, `segment_totalseg_v2.py`, `build_v2_cardiac_products.py`, `summarize_v2_cohort_qc.py` | Фазовые NIfTI, raw-маски, `products_v2`, QC |
| Обзор 4D-геометрии | `21.01`, `21.02` | `build_interactive_cardiac4d.py` | HTML, notebook и `4D_сердце_интерактивно/manifest.json` |
| Окружающая анатомия на первой фазе | `21.02` | `segment_totalseg_anatomy.py`, `summarize_anatomy_qc.py` | Внешние маски; `anatomy_*` в папке просмотра |
| Временные метаданные КТ и GE ECG Report | `21.03`, `21.04` | `summarize_cardiac_phase_tags.py`, `extract_ecg_report_images.py`, `summarize_ecg_report_ocr.py`, `extract_deidentified_ecg_trace.py` | Два внешних JSON временных доказательств и `ecg_traces` |
| Остаток, объёмы и R–R | `21.03`, `21.04` | `analyze_subtraction_and_rr.py`; модули `cardiac_synchronized_view.py`, `cardiac_volume_time_plots.py` | `heart_rr_analysis`, notebook/HTML и внешние маски `heart_minus_blood` |
| УО/ФВ и диагностика масок | `21.03`, `21.04` | `cardiac_function_analysis.py`, `build_cardiac_function_report.py`, `build_cardiac_ct_review.py` | Локальные таблицы и КТ-просмотр в `heart_rr_analysis/function_analysis`; условные результаты без независимой валидации |
| Пилот уточнения ЛЖ/ПЖ Георга | 21.03, 21.06 | georg_ventricular_mask_pilot.py, prepare_georg_prompt_pilot.py, build_georg_pilot_review.py, summarize_georg_mask_pilot.py | Два способа, четыре фазы, 16 кандидатов; ни один не принят для основного расчёта |
| Независимая ручная проверка | `21.05` | `build_cardiac_validation_candidates.py`, `validate_manual_cardiac_masks.py` | Отдельные кандидаты, метрики и отчёт валидации при наличии независимого референса |
| Покамерный просмотр крови | `21.06` | `prepare_blood_chamber_review.py` | Очередь ручной проверки, исходные кандидаты и технический QC |
| Сравнение способов сегментации | `21.07` | `pilot_blood_segmentation_candidates.py`, `benchmark_blood_segmentation_candidates.py` | Пилотные кандидаты; accuracy только при независимой ручной маске |
| Статическая КТ | [20.07](20.07_Статическая_КТ_полная_сегментация_TotalSegmentator.md) | `spj_static_series_to_nifti.py`, общий сегментационный блок, `summarize_static_segmentation_qc.py` | Отдельный статический прогон, без динамики R–R |

Порядок зависимостей основной ветки:

```text
DICOM → phases → automatic_masks_v2 → products_v2 → 21.01 (обзор)
  │                    │                   │
  └→ метки GE / ECG ────┴───────────────────┴→ 21.03 (R–R и объёмы)
                       │
                       └→ 21.06 (покамерный QC)
                          + 21.07 (кандидаты)
                          + независимый ручной референс
                            → 21.05 (оценка точности)
```

`21.05` задаёт протокол оценки, поэтому его следует читать до подготовки
кандидатов `21.06–21.07`. Повышение доказательности зависит от независимого
референса, а не от номера документа. Передача принятых масок обратно в
RR-расчёт пока требует отдельного явного интерфейса: текущий `21.03` читает
фиксированные `automatic_masks_v2` и `products_v2`.

## Определения, которые необходимо сохранить при пересборке

| Объект | Наблюдаемое определение в текущем коде | Статус |
|---|---|---|
| Объём отдельной камеры в `21.03` | Воксели raw `heartchambers_highres` на исходной сетке, мл | Результат обработки автоматической маски |
| Поверхность камеры в синхронном 3D | `raw_chamber` (согласовано с объёмами 09.09.2026); сетка дополнительно облегчена для отображения | Визуальное представление; не источник табличного объёма |
| `blood_pool` в `products_v2` | Объединённый и обработанный продукт камер | Производная маска, не сумма четырёх независимых табличных объёмов по определению |
| `heart_minus_blood` | `whole_heart & ~blood_pool` | Геометрический остаток независимых моделей; не валидированный полный миокард |
| Временная координата `21.03` | DICOM-фазы и интервалы из GE `RpeakTimeStamps` | Поддержана метаданными реконструкции; не цифровая ЭКГ |
| Композитный цикл Adam/Nix | Периодическая модель после рабочего порога согласованности ЧСС 10% | Модельный результат, не непосредственно снятый полный цикл |
| Georg | Два отдельных интервала; композит отклонён | Причина коротко-длинной пары остаётся открытой |

Ссылки на реализацию: [загрузка масок и расчёт](analyze_subtraction_and_rr.py),
[постобработка v2](build_v2_cardiac_products.py),
[сохранённая RR-сводка](heart_rr_analysis/rr_and_subtraction_summary.json).
ЭКГ/гейтинг КТ серии `21` не является разметкой ЭКГ реокардиомониторов серии
`11` и не устанавливает синхронность КТ с экспериментами 2–3.

## Как пересобирать имеющиеся представления

Команды ниже описывают существующий интерфейс. Выполнять из
`Colab Notebooks/cardiac_4d_segmentation`, подставляя локальные пути;
требуются зависимости из `requirements-base.txt` и
`requirements-visualization.txt`. Полная сегментация и GPU-окружение описаны
в [README вычислительного блока](README.md).
Перезапуск генераторов перезапишет соответствующие notebook/HTML/CSV.

Просмотр `21.01` строится из готовых масок и не запускает TotalSegmentator:

```powershell
python build_interactive_cardiac4d.py `
  --products-root '<DERIVED_ROOT>' `
  --output-dir './4D_сердце_интерактивно' --subjects adam nix georg
```

Расчёт `21.03` требует одновременно raw-маски, `products_v2`, conversion
manifest и временные манифесты. Он также записывает производные маски во
внешний `<DERIVED_ROOT>`. Все пути задаются явно, поскольку значения по
умолчанию для внешнего хранилища отражают прежнюю локальную структуру.
Выходные папки по умолчанию теперь определяются относительно скриптов:

```powershell
python analyze_subtraction_and_rr.py `
  --derived-root '<DERIVED_ROOT>' `
  --cardiac-metadata-summary '<DERIVED_ROOT>/_cohort_qc_v2/cardiac_phase_metadata_summary.json' `
  --ecg-report-evidence '<DERIVED_ROOT>/_cohort_qc_v2/ecg_report_evidence_summary.json' `
  --output-dir './heart_rr_analysis' --subjects adam nix georg
```

Каталог `heart_rr_analysis/ecg_traces` должен уже содержать подготовленные
полосы и выделенные интервалы GE: RR-генератор читает их, но не извлекает
самостоятельно. Нельзя переименовать только папку результатов: относительные
ссылки `heart_rr_analysis/...` также записаны в генераторе и notebook/HTML.

Чтобы восстановить только читательский HTML из сохранённого ноутбука, без
пересчёта, использовать экспорт без исходного кода:

```powershell
python -m jupyter nbconvert --to html `
  --TemplateExporter.exclude_input=True `
  --TemplateExporter.exclude_input_prompt=True `
  --TemplateExporter.exclude_output_prompt=True `
  '21.01_4D_сердце_интерактивно.ipynb'
```

## Что уже объединено и что остаётся сделать

**Подтверждённый результат структурного аудита:** установлены владельцы трёх
папок, общий порядок чтения и генераторы двух ноутбуков. По запросу автора
от 09.09.2026 документы, ноутбуки и две папки представлений физически
перенесены сюда; имена файлов сохранены. Таблица переходов и SHA-256 —
в [манифесте](MIGRATION_4D_RR_2026-09-09.json). Общая точка входа связана
с картой проекта; относительные ссылки и выходные пути генераторов обновлены.

**Нерешённые задачи дальнейшей вычислительной сборки:**

- Зафиксировать единый манифест прогона: входы, версия/hash масок, геометрия,
  исходные фазы, вариант обработки, временные доказательства и решение QC.
  Наличие нынешних раздельных JSON ещё не обеспечивает согласованность версий.
- Передавать в `21.03` явно выбранный набор покамерных масок из `21.06–21.07`;
  сохранить различия raw, обработанной и отображаемой геометрии, отдельно
  сравнить с прежним v2. Сейчас такого переключателя в RR-генераторе нет.
- Сохранить ручные изменения научного текста в источнике генерации: полная
  пересборка `21.01/21.03` заменяет их текст содержимым Python-генераторов.
- Разобрать отдельно `temporal_registration.py`: скрипт существует, но ссылки
  из документов серии и включение в генераторы `21.01/21.03` не найдены.
  Это не подтверждённая обязательная стадия; автоматически её не подключать.
- После принятия масок сравнить результаты пересборки с текущими таблицами
  и только затем решать, какие старые продукты архивировать. УО/ФВ этим
  техническим объединением не валидируются.

**Изменение описания состояния:** прежняя строка серии `21` в общем README
называла шкалу R–R неподтверждённой. Более поздние `21.03–21.04` и RR-манифест
от 07.09.2026 документируют опору на метки GE; общая навигация приведена к этому
состоянию. Неподтверждёнными остаются цифровая ЭКГ, причина пары Georg и
физиологическая правомерность композита. Ранние отчёты сохранены как описание
соответствующих прогонов, а не переписаны задним числом.


[Заметки по выписке диалога GPT от 09.09.2026](ОБСУЖДЕНИЕ_GPT_2026-09-09.md) —
сопоставление с текущим `21.03` и вопросы к обсуждению; предложения
коррекции пока не приняты и не реализованы.
