# План доработок

Несрочные улучшения для `Kandidadskaya.ipynb`. Критические ошибки уже исправлены — здесь то, что стоит сделать, когда дойдут руки.

## 1. Сделать `Zbase` и `dZ_total` чистыми функциями (cell 32)

Сейчас функции принимают параметры, но в той же ячейке заведены глобалы с похожими именами (`a0`, `b10`, `br0`, `N`, `x0`, `y0`, `rho1`, `rho2`, `r1`, `h1`) и переменная `dz_total`. Именно из-за этого и возникла критическая бага: `return term + dz_total` использовало глобал вместо локального `dz`.

**Что сделать:** убрать обращение к глобалам внутри функций; параметры передавать только через аргументы. Альтернатива — собрать геометрию в `@dataclass GeometryParams` и передавать одним объектом.

## 2. Вынести линейную регрессию в функцию (cells 49, 50)

В cell 49 и cell 50 почти полностью дублируется блок `LinearRegression().fit(...)` + вычисление наклона/угла/R² + отрисовка. В cell 50 этот же блок ещё и крутится в цикле по `h1_values`.

**Что сделать:** функция вида

```python
def fit_Z_vs_r1(r1_array_m, Z_per_point, point_labels):
    # возвращает DataFrame с колонками: label, slope, intercept, angle_deg, r2
```

— и отдельная вспомогательная для отрисовки на переданной `Axes`.

## 3. Убрать неявные cross-cell зависимости

Многие ячейки опираются на переменные из предыдущих: `r1_values` (cell 45), `Z_target_1`/`Z_target_2`/`r1_value`/`r2_value` (cell 59), `rho1`/`rho2`/`heart_a`/`heart_b`, а также `n_values`, `coefs`, `P` (если они заведены где-то выше). Запуск «через одну» даёт `NameError`.

**Что сделать:** в начале каждой крупной ячейки явно перечислять, что она ожидает; либо вынести все инициализации констант в одну bootstrap-ячейку в самом начале notebook.

## 4. Производительность поиска `r1` на сетке (cells 61, 62)

Сейчас вложенные циклы по сетке `X × Y` с `scipy.optimize.brentq` в каждой точке. Для сетки 100×100 — десятки тысяч решений уравнения, минуты времени.

**Что сделать:**
- Векторизовать `compute_Z_at_point` так, чтобы за один вызов считался массив shape `(Ny, Nx, Nr1)`, и брэкетинг находился через табличную интерполяцию `Z(r1)`.
- Либо заменить `brentq` на одну итерацию Ньютона с аналитической производной `dZ/dr1`.

## 5. Прибраться

- Удалить закомментированные `#print(f"dz_am = …")` в cell 32 и другие отладочные следы.
- Добавить docstring к `extract_number`, `get_signal_with_comment`, `compute_Z_at_point`, `dZ_total`, `Zbase`.
- Захардкоженные пути и параметры файлов вынести в одну ячейку-конфиг в начале.

## 6. Резервная копия

`Kandidadskaya.ipynb.bak` — снимок до правок критических ошибок. Лежит в корне, в git не отслеживается. После того как убедитесь, что notebook отрабатывает корректно от начала до конца, файл можно удалить. В `.gitignore` стоит дописать `*.bak`, чтобы случайно не закоммитить будущие бэкапы.
