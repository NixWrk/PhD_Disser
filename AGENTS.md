# Контекст кандидатской диссертации

Этот файл фиксирует подтверждённый автором контекст проекта. Информация пока
неполная; не заполнять пробелы догадками и не объявлять исследовательскую ветку
успешной только по наличию кода или результатов расчётов.

## Глобальная цель и стратегия разработки методики

Главная цель проекта — разработать методику, позволяющую получать количественно
интерпретируемые и независимо валидированные значения функций сердца при
минимально необходимом наборе входных данных. Это целевое состояние, а не уже
достигнутый результат; до завершения проверок всю работу считать разработкой
методики.

Исследование строится как последовательное сокращение информации:

1. **Разработка при максимально полных данных.** На добровольцах собираются все
   доступные референсные данные: КТ и при наличии другие методы визуализации,
   ручная сегментация анатомии, геометрия электродов, ЭКГ, ТТРКГ/РЕО и другие
   записанные сигналы, сведения о приборе и протоколе. Эти данные нужны для
   построения, идентификации и проверки физических и математических моделей.
2. **Модельные и экспериментальные проверки.** Каждое звено методики проверять
   внутренними математическими тестами, вычислительными экспериментами,
   независимыми измерениями и сопоставлением с реальной анатомией. Внутренняя
   согласованность кода не заменяет физическую или экспериментальную
   валидацию.
3. **Последовательное исключение входов.** После проверки полной постановки
   по одному сокращать набор требуемых данных. Исключать референсный вход можно
   только после того, как способ его замены проверен относительно полной
   постановки и для него определены погрешность и границы применимости.
4. **Целевая практическая методика.** Для реальных пациентов стремиться к
   минимальному набору доступных входов без обязательной индивидуальной КТ,
   сохраняя заранее заданную точность и доказанную применимость оценок функций
   сердца.

КТ добровольцев на этапе разработки является необходимым источником геометрии
и референсом для проверки моделей, но не предполагается обязательным входом
конечной методики. В частности, эффективная толщина мягких тканей $h$ сначала
может задаваться или проверяться по КТ-сегментации, а затем должна оцениваться
по другим доступным показателям. Такой переход считать отдельной обратной
задачей: до проверки идентифицируемости и сравнения с КТ не объявлять
расчётную $h$ эквивалентной анатомической или достаточно точной.

Для каждого этапа, ноутбука и научного вывода обязательно фиксировать:

- какие входы измерены напрямую, получены ручной сегментацией, рассчитаны,
  перенесены из другого эксперимента или приняты как допущения;
- какие допущения введены и зачем, какие из них проверены, а какие остаются
  рабочими гипотезами;
- применимый эксперимент, прибор, расположение электродов, состояние дыхания,
  единицы, пути и происхождение данных;
- модель шума, неопределённости параметров, ошибки установки и расхождение
  модели с реальной анатомией; неизвестные компоненты не заменять одним числом
  без обоснования;
- чем именно выполнена проверка: самоконтролем кода, синтетическим расчётом,
  экспериментом на добровольцах, независимым методом, КТ/FEM или внешними
  данными;
- отрицательные результаты, неидентифицируемость, ограничения переноса и
  условия, при которых вход можно исключить из конечной методики.

На каждом переходе от полной постановки к сокращённой сохранять связь с
референсным расчётом и явно показывать, какая информация потеряна, чем она
заменена и как это изменило точность результата.

Каждое существенное утверждение помечать как подтверждённый факт, допущение,
результат математической модели, экспериментальное наблюдение, валидированный
результат или нерешённый вопрос. Результат полной постановки не переносить
автоматически на сокращённый протокол без отдельной проверки.

## Каноническая рабочая область

- `Colab Notebooks/` — основная папка текущей работы и итоговых материалов.
- Локальные правила основной папки находятся в `Colab Notebooks/AGENTS.md`.
- При расхождении копий, rebuilt-каталогов, snapshots и исторических веток не
  выбирать каноническую версию автоматически. Сначала сверять её с
  `Colab Notebooks/` и уточнять происхождение изменений.
- Предпочтительный локальный корень внешних исследовательских данных —
  `D:\Аспа\Bitrix\ЭИТЛ`. Он дублирует диск `Z:`, но для анализа и построения
  локальных конфигураций использовать именно копию на `D:`. Не смешивать
  файлы из двух зеркал в одном расчёте и не переключаться на `Z:` молча; при
  проверке расхождений сопоставлять относительный путь, размер, время и хэш.
- Результаты экспериментов искать в
  `D:\Аспа\Bitrix\ЭИТЛ\01 Project\01 Experiments`.
- КТ, МРТ и STL искать в `D:\Аспа\Bitrix\ЭИТЛ\02 Big_data\3D`.
- Эти абсолютные корни предназначены для агентских инструкций и локальных
  исключённых из Git конфигураций. Не встраивать их в переносимый код,
  notebook-файлы или отслеживаемые примеры конфигурации.
- Исходные данные включают КТ, МРТ и записанные физиологические сигналы, в том
  числе ЭКГ и РЕО. Они могут храниться вне репозитория и содержать медицинские
  данные; не перемещать, не публиковать и не коммитить их без явного указания.

## Подтверждённая последовательность исследования

1. В первую очередь была проведена сегментация КТ добровольцев, снятых на
   вдохе.
2. Итоговая сегментация выполнялась автором вручную в Inobitec.
3. Затем у тех же добровольцев записывались различные физиологические сигналы
   в соответствии с протоколами исследования.
4. Подтверждённые расчёты моделей выполнялись MATLAB/EIDORS/Gmsh и
   Python-скриптами. COMSOL рассматривался и сохраняется в репозитории как
   историческая проба, но не является подтверждённым звеном действующего
   расчётного pipeline. Наличие моделей не считать доказательством завершённой
   валидации без проверки конкретного расчёта, входных данных и итогового
   документа.

## Подтверждённая экспериментальная база

В основном аналитическом корпусе рассматриваются материалы трёх экспериментов.

1. Первая серия (`kandidatskaya`) проведена в МГТУ на приборе РЕО32. В
   `Colab Notebooks/70.01_Паспорт_и_QC_эксперимента_1_РЕО32.ipynb` используются записи `23_01_25`.
   Все описанные в ноутбуке каналы были физически подключены и действительно
   записаны: каналы 5 и 9 — крестообразное расположение электродов над областью
   сердца, канал 7 — мягкие ткани, канал 13 — правое лёгкое, канал 16 — ЭКГ,
   канал 32 — стандартная ТРКГ.
2. Вторая серия проведена в МГТУ на реокардиомониторе МГТУ. Регистрировались
   ЭКГ, ТТРКГ и сигналы нескольких электродных сборок для мягких тканей.
3. Третья серия проведена в РНЦХ на реокардиомониторе РНЦХ. Расположение
   электродов соответствует использованной в МГТУ схеме; подтверждённый
   номинальный размер боковой сборки — 140 мм.

РЕО32 эксперимента 1 — отдельный прибор и не является ревизией
реокардиомонитора. Сравнение аппаратных ревизий относится только к
реокардиомонитору МГТУ из эксперимента 2 и реокардиомонитору РНЦХ из
эксперимента 3. Не переносить между РЕО32 и этими реокардиомониторами каналы,
шкалы, калибровки, аппаратные эффекты или модели сигнала.

Текущий этап всей работы — разработка методики; наличие расчётов и сохранённых
результатов не означает завершённую экспериментальную или клиническую валидацию.
Приоритет текущего разбора — вторая и третья серии. Первую серию временно
держать в стороне: в ней использованы другие электроды и их расположение,
поэтому её расчётная модель не переносится на серии 2–3. Она в отдельных
аспектах ближе к методу из диссертации Тихомирова, но степень соответствия
остаётся предметом отдельной проверки.

ТТРКГ во второй и третьей сериях — не ТРКГ. Не объединять эти обозначения и не
переносить выводы между приборами без отдельной проверки аппаратной ревизии,
калибровки, межканального влияния и протокола регистрации. Вопросы к каждому
из трёх приборов считать открытыми до разбора соответствующей документации.

### Технический контекст экспериментов 2–3

- `Colab Notebooks/10.11_QC_эксперимента_3.ipynb` является первичным
  техническим паспортом и QC записей РНЦХ, а не расчётом функций сердца.
  Исходные CSV являются экспериментальными данными; их инвентаризация,
  дедупликация и численные сводки в ноутбуке — результаты обработки. Абсолютный
  путь к внешнему хранилищу и отсутствие сырья в Git являются ограничениями
  воспроизводимости, а не самостоятельными научными результатами.
- В относящемся к основной ветке сопоставлении экспериментов 2 и 3 участвовал
  один и тот же испытуемый, обозначаемый `Nix`. Поэтому утверждение о разных
  испытуемых в старой редакции ноутбука 17 и TODO неверно. Совпадение
  испытуемого не устраняет различия сессий, приборов, контактов и фактической
  установки электродов.
- Неправильная абсолютная шкала канала 1 реокардиомонитора МГТУ пока является
  рабочей гипотезой, основанной только на сравнении с реокардиомонитором РНЦХ
  и на MATLAB-моделировании, где получены кратно большие значения импеданса.
  Независимой калибровки или прямой метрологической проверки нет; не называть
  канал доказанно неисправным и не считать модель эталоном измерения.
- Неизменность электродов канала 1 в эксперименте 2 не делает этот канал
  чистым индикатором времени: показания могли зависеть от времени, состояния
  испытуемого и контакта, а также от одновременно подключённой сменной сборки
  канала 2. До разделяющего эксперимента использовать формулировку
  «межзаписная изменчивость канала 1», а не доказанный дрейф прибора.
- По подтверждению автора две дополнительные записи эксперимента 2 — по одной у
  Георга и Ника — являются полными записями дыхательного протокола: спокойное
  дыхание → задержка на вдохе → усиленное дыхание → задержка на выдохе. Их
  размер боковой сборки пока не установлен, поэтому они классифицированы, но
  не включены в размерный ряд и количественную подгонку по размерам.
- В тесте поочерёдного отключения каналов кабели отсоединялись от прибора без
  отклеивания электродов. Поэтому зависимость показаний одного канала от
  состояния соседнего считать установленным экспериментальным фактом для этой
  ревизии реокардиомонитора РНЦХ. Причина эффекта не исследуется; не объявлять
  доказанными шунтирование, перераспределение тока или иной механизм.
- Ни в одном из трёх экспериментов прибором не регистрировались временные
  метки команд дыхательного протокола. Порядок проб и границы режимов
  восстанавливаются ретроспективно по памяти автора и форме записанных
  сигналов, прежде всего `BASE`. Такие интервалы
  являются экспертной реконструкцией, а не непосредственно измеренными
  событиями. Для каждой принятой разметки фиксировать автора решения,
  использованные признаки, степень уверенности и неоднозначные границы.
- Последовательность дыхательных режимов в РНЦХ отличается от эксперимента 2
  и различается между записями РНЦХ. Ориентировочные интервалы из ноутбука 16
  не использовать в количественном анализе без повторного просмотра и явного
  принятия автором. Требуется отдельная реконструкция по каждой записи с
  логикой и QC, аналогичными эксперименту 2, но без переноса его порядка
  режимов.
- Повторная проверка четырёх основных записей эксперимента 3 подтвердила
  назначенные роли и фактические состояния каналов по `BASE > 5 Ом`. Это не
  подтверждает качество сигналов. Во всех активных `RHEO` присутствуют
  продолжительные повторения дискретных крайних уровней; насыщение остаётся
  только гипотезой. Автоматические кандидаты R-зубцов текущей серии `11.12`
  имеют длинные пропуски во всех четырёх записях, а в тесте переключения четыре
  найденных события совпадают по времени с границами состояний каналов. До
  принятой ручной ЭКГ-разметки и аппаратной проверки не использовать старые
  пульсовые амплитуды и выводы о сердечном происхождении из архивного `40.90`.
  Подтверждённой `mode_sequence` для записей РНЦХ пока нет; пустая
  последовательность должна блокировать `11.11`.
- Файловый QC выявил три дополнительные уникальные записи эксперимента 3, в
  которых по техническому правилу активен только канал 1: `BASE_2` ни в одной
  из них не превышает порог 5 Ом. Это факт о записанных полях. Отключение
  второго канала является рабочей гипотезой автора; назначение записей пока не
  подтверждено первичным протоколом. При неактивном `BASE_2` числа `RHEO_2` не
  интерпретировать как физиологический сигнал. В `RHEO_1` присутствуют участки
  достижения повторяющегося предельного уровня около ±584 мОм. Считать это
  насыщением прибора можно только как гипотезу до проверки тракта.
- `PULMOVISTA` в имени внешней папки относится к другому, не одновременному
  исследованию на другом приборе, не связанному с этой диссертацией. Не
  включать эти данные в эксперимент 3 и не синхронизировать их с ТТРКГ/ЭКГ.
- Описания протоколов, схемы, фотографии и другие дополнительные сведения в
  будущем предполагается связать с проектом через базу Obsidian. До её
  подключения не считать такие материалы доступными или проверенными. После
  подключения хранить в Git только безопасные ссылки и обезличенные описания,
  не копируя медицинские или персональные материалы автоматически.

### Роль боковых электродных сборок в общей методике

- Сборки, ранее называвшиеся «над лёгким» и «для мягких тканей», — один и тот
  же боковой монтаж на боковой поверхности грудной клетки.
- Рабочее физическое допущение для экспериментов 2–3: существенный объём
  чувствительности боковых сборок охватывает мягкие ткани грудной стенки
  (кожно-жировой слой и мышцы), рёбра/кости и лёгкое. Сердце и другие
  центральные структуры расположены значительно дальше, поэтому их прямой
  вклад принимается малым. Это анатомически мотивированное допущение, а не
  результат одной только ЭКГ-синхронности; его количественная проверка требует
  индивидуальной карты чувствительности КТ/FEM. Кости могут изменять
  распределение тока и базовый импеданс, но не рассматриваются как
  самостоятельный источник пульсового изменения удельного сопротивления.
- КТ в этой ветке имеет две самостоятельные роли: (1) межмодальная проверка
  адекватности порядка получаемых электрических параметров; (2) источник
  индивидуальной реальной анатомии и тканевых масок для построения и проверки
  конечноэлементной модели на конкретном добровольце.
- Цель боковых сборок на этапе разработки — по нескольким реально записанным
  размерам определить информативную пару либо рабочий диапазон размеров и
  критерии их выбора. В целевой сокращённой постановке не менее двух
  различающихся по глубине чувствительности сборок должны позволять оценить
  эффективные $\rho_1,\rho_2$, вентиляционные
  $\Delta\rho_1,\Delta\rho_2$ между вдохом и выдохом и пульсовые
  $\Delta\rho_1,\Delta\rho_2$. Использование ровно двух сборок является
  целевой постановкой; пригодность и оптимальность пары ещё требуется доказать
  на полном многоразмерном наборе.
- Для одного добровольца (псевдоним Ник) записи 90 и 100 мм совпадают.
  Проверка времени файлов и содержимого в `10.01` установила, что запись
  100 мм является поздней перезаписанной копией, а независимой — запись
  90 мм. Для этого добровольца исключается 100 мм. У второго добровольца
  (псевдоним Георг) 90 и 100 мм являются самостоятельными записями и
  проходят раздельный QC; глобально исключать размер 100 мм нельзя. Поэтому
  текст исторического ноутбука 08 об исключении 90 мм устарел, а решения
  включения принимаются по субъектному манифесту `10.01`.
- Эти параметры бокового монтажа не являются непосредственной оценкой функции
  сердца. Их предполагается использовать как поправочные факторы для отделения
  вкладов мягких тканей и лёгкого в сигнал ТТРКГ. После такой коррекции рабочая
  гипотеза основной ветки состоит в восстановлении по ТТРКГ изменения координат
  и объёма эквивалентной сердцу сферы, а затем в переходе к ударному объёму и
  фракции выброса. Эта цепочка пока является программой разработки методики, а
  не валидированным способом расчёта УО/ФВ.
- Пульсовый сигнал боковых сборок в рабочей двуслойной постановке содержит как
  вклад $\Delta\rho_1$ мягких тканей, так и вклад $\Delta\rho_2$ лёгкого.
  Одна лишь синхронность с ЭКГ задаёт время события, но не разделяет эти два
  источника. Оценка уровня $\Delta\rho_1$ нужна, в частности, чтобы
  экстраполировать возможный вклад мягких тканей в итоговый сигнал ТТРКГ.

### Допущения и источники погрешности бокового эксперимента

- **Подтверждённое допущение:** записи сборок разных размеров, сделанные
  последовательно, считаются сопоставимыми как измерения одного физиологического
  состояния. Это не свойство данных, а условие текущего объединённого расчёта.
- Возможные источники погрешности перечислять раздельно, не объединяя заранее
  словом «шум»: вариабельность сердечных циклов и дыхания; изменение состояния
  испытуемого между записями; фиксированный порядок съёмки; дрейф прибора;
  неизвестные gain, знак и калибровка каналов; межканальное влияние; контактное
  сопротивление; ручная наклейка и переклейка одноразовых электродов; ошибка
  положения, ориентации и расстояний; различия конкретных сборок; ошибка и
  возможное изменение $h$; несоответствие плоской двуслойной модели реальной
  кривизне, рёбрам и неоднородной анатомии; несовпадение состояний дыхания КТ и
  электрической записи.
- До отдельного исследования не принимать для совокупной погрешности конкретное
  распределение, независимость или гауссовость. Решения, необходимые для
  расчёта при неполной информации, фиксировать как допущения и отдельно
  проверять чувствительность результата к ним.

### Статус межэкспериментальной постановки ноутбука 19

- В этой постановке различать вентиляционное изменение удельного сопротивления
  между вдохом и выдохом и пульсовое изменение, связанное с перемещением и
  перераспределением крови в тканях. Пульсовый отклик закономерно имеет
  физиологическую задержку относительно зубца R. ЭКГ задаёт временную привязку,
  но сама по себе не локализует источник изменения внутри сердца, лёгкого или
  мягких тканей.
- Ноутбук 19 переносит между экспериментами прежде всего характеристики
  записанных и затем модельно обработанных сигналов боковых сборок, а не
  независимо измеренные универсальные свойства тканей. Использовать такой
  перенос только как предварительную оценку: испытуемый один, но сессии не
  одновременны, приборы и поля чувствительности различаются, а дыхательные
  состояния пока не согласованы.
- Для тетраполярного передаточного импеданса локальная чувствительность может
  быть отрицательной: знак определяется взаимной ориентацией токового и
  измерительного полей. Поэтому даже условное тождество
  $\sum_i S_i=1$ не влечёт $0\leq S_i\leq1$ и не даёт использованной в
  ноутбуке универсальной верхней границы отдельного тканевого вклада. Основание:
  [E. Naydenova, S. Cavendish, A. J. Wilson, «Measurement and modelling the
  sensitivity of tetrapolar transfer impedance measurements»,
  2016](https://doi.org/10.1016/j.medengphy.2016.07.005).
- Амплитуда `max - min` не сохраняет знак и фазу компоненты. Перенесённые между
  сессиями амплитуды нельзя непосредственно вычитать из ТТРКГ как синхронные
  знаковые сигналы без отдельного оператора переноса и оценки неопределённости.
- Изменение канала 1 при переключении соседнего канала считать установленным
  экспериментальным фактом. Не приписывать ему конкретный механизм или
  увеличение чувствительности именно к сердцу без разделяющего эксперимента.
- Малый по сравнению с полным сигналом вклад мягких тканей нельзя объявлять
  пренебрежимым, если его условная оценка сопоставима с допустимой ошибкой
  конечной коррекции. Проверять его относительно заранее заданного бюджета
  погрешности, а не только относительно полной амплитуды ТТРКГ.
- Требование к точности $S_{\mathrm{lung}}$, выведенное как доля полного
  измеренного сигнала, не является автоматически такой же относительной
  точностью остаточного сердечного сигнала после вычитания. Если остаток мал,
  та же абсолютная ошибка составляет существенно большую его долю.
- Корректную работу детектора R-зубцов, ансамблевого усреднения и кода пока
  считать явным рабочим допущением. Оно требует последующей ручной разметки,
  контроля циклов и оценки неопределённости, но не блокирует предварительный
  методический расчёт.
- Если FEM-модель построена по полному набору индивидуальных КТ-сегментаций и
  воспроизводит базовый импеданс при соответствующих монтаже и состоянии, это
  считать предварительной проверкой её пригодности для интегрального базового
  импеданса. Такое совпадение не проверяет автоматически региональные поля
  чувствительности, их единственность и производную по объёму сердца.
- Сначала довести цепочку «измеренный сигнал → отделение тканевых вкладов →
  эквивалентная сфера». Эллипсоид и другие усложнения формы сохранять как
  гипотезы следующего этапа.

## Статус отдельных направлений

- `breath geometry/` — исследовательская попытка оценить изменение толщины и
  формы мягких тканей на вдохе и выдохе. Направление оказалось чрезмерно
  сложным и математически неоправданным для основной задачи: литературный
  обзор по УЗИ указывает на минимальное изменение толщин. Не считать эту папку
  активным ядром диссертации и не возобновлять направление без новой научной
  мотивации.
- `3D_Slicer/` — неудачная попытка автоматизировать сегментацию. Финальная
  сегментация выполнялась вручную через Inobitec; результаты из `3D_Slicer/`
  не считать каноническими без отдельного подтверждения.
- `Литературный обзор/` — пока неудачная предварительная попытка собрать данные
  для начального этапа кандидатской. Не считать её завершённым систематическим
  обзором или канонической доказательной базой.

## Дополнительно подтверждённый статус папок

- `COMSOL_investigation_NIX/` и COMSOL-файлы в других папках были пробами.
  Сохранять их как историю; не считать активной FEM-веткой или подтверждённым
  результатом.
- Единственная активная папка MATLAB/Python-моделирования —
  `MATLAB_TRKG4_real_subjects/`. Исторический `MATLAB_investigation_NIX/`
  перенесён в `MATLAB_TRKG4_real_subjects/archive/` с сохранением Git-истории
  и не является вторым способом получать актуальные результаты. Каноническая
  точка входа для артефактов notebook-файлов —
  `MATLAB_TRKG4_real_subjects/run_pipeline.m`.

### КТ/FEM-поиск положения боковых электродных матриц

- Для многоразмерных записей боковых сборок целевая задача реализованной
  КТ/FEM-ветки — не оптимизация набора размеров. Все реально исследованные
  размеры используются совместно, чтобы по индивидуальной КТ/STL-геометрии и
  экспериментальной кривой $Z(L)$ восстановить общую позу матриц во время
  исследования: центр на коже, два смещения в касательной плоскости и угол
  ориентации. Выбор информативной пары размеров остаётся отдельной
  методической задачей и не подменяет пространственную локализацию.
- Реализованный расчёт находится не в одном
  `30.04_Вычислительное_ядро_двуслойной_модели.ipynb`, а в
  `MATLAB_TRKG4_real_subjects/`: `run_trkg4_inverse_inhale` совместно
  подбирает `rho_soft`, `rho_lung` и позу;
  `run_trkg4_fast_pem_scan` выполняет плотный предварительный поиск по
  поверхности; `run_trkg4_fast_cem_refine` уточняет кандидатов моделью
  конечных электродов CEM/EIDORS. Все жёсткие матрицы имеют общий центр и ось,
  их электроды проецируются на реальную поверхность тела, а профиль расстояния
  от кожи до лёгкого $h(s)$ выводится из КТ/STL-геометрии. Не объявлять этот
  расчёт отсутствующим только потому, что его нет среди ячеек ноутбука `05`.
- В аналитической двуслойной модели $h$ — эффективная толщина мягких тканей
  между кожей и лёгким: кожно-жировые ткани и мышцы; кости в $h$ не входят. В
  КТ/FEM-модели кости представлены отдельной тканевой маской и отдельной
  проводимостью, а прочие мягкие ткани образуют фон.
- Расчёт доведён до документированного регуляризованного локального кандидата
  положения матриц. Это результат этапа разработки методики, но не
  доказательство глобальной единственности позы или клинической валидации:
  ограничения зафиксированы в
  `MATLAB_TRKG4_real_subjects/docs/INVERSE_IDENTIFIABILITY_AND_BREATHING_STATES.md`.
  Численные outputs и крупные FEM-входы исключены из Git, поэтому их отсутствие
  в чистом checkout не означает отсутствия выполненного расчёта; для полного
  воспроизведения нужно восстановить внешние STL, сетку и промежуточные файлы.
- Найденная поза матриц и геометрический предел $L_{\max}$ — разные
  результаты. Пространственная локализация реализована; универсальная верхняя
  граница размера, после которой нарушается плоскослоистое приближение,
  остаётся отдельной задачей.
- Анализ влияния параметров анатомической модели на сигналы боковых сборок
  считать отдельной прямой задачей чувствительности. Выполнять его на
  индивидуальных моделях, восстановленных по КТ, в MATLAB/FEM с явным
  изменением параметров. Не смешивать эту задачу с аналитической обратной
  задачей для ТРКГ или ТТРКГ.
- Исходный замысел `archive/legacy/50.90_Прототип_идентифицируемости_источников.ipynb` — предварительный
  аналитический go/no-go перед обратной задачей: проверить разделимость вкладов
  сердца, крупных сосудов, лёгкого и грудной стенки. Текущая реализация
  смешивает размеры боковых сборок с моделью источников ТРКГ и не соответствует
  реальному монтажу ни одной серии; она требует переработки. ТРКГ эксперимента
  1 и ТТРКГ экспериментов 2–3 ставить как разные обратные задачи.

### Эквивалентная сфера эксперимента 1

- В ветке каналов 5 и 9 РЕО32 целевая физическая величина эквивалентной сферы —
  суммарный объём крови в сердце. Рабочее допущение: эффективные удельные
  сопротивления кровенаполненной сердечной мышцы и крови достаточно близки,
  чтобы в первом приближении представить их одной проводящей областью. Такая
  модель электрически не разделяет кровь и миокард; переход от изменения её
  эффективного объёма к УО или ФВ требует независимой валидации.
- Параметр `h` задаёт глубину передней поверхности эквивалентной сферы.
  Передняя поверхность принимается фиксированной из-за анатомической привязки
  зоны сердечной тупости и сердца к окружающим органам. Поэтому при изменении
  радиуса центр сферы закономерно перемещается как `z_c = h + R`. Это
  осознанное кинематическое допущение, а не утверждение о доказанной
  неподвижности реальной передней стенки; его следует проверить по
  визуализации.
- Каналы 5 («поперёк сердца», 180×90 мм) и 9 («вдоль сердца», 180×90 мм)
  рассматриваются как два наблюдения одной физической эквивалентной области,
  но должны иметь разные операторы чувствительности, фактическую геометрию и
  индивидуальную калибровку. Не заменять различия каналов простым offset.
- Под каналами 5 и 9 находится различная анатомия. Использование одной
  идеализированной симметричной геометрии для обоих каналов допустимо только
  как первый тест математического расчёта с явной оговоркой. Для физической
  интерпретации и обратной задачи нужны отдельные операторы чувствительности,
  построенные по фактической геометрии каждого монтажа и индивидуальной
  анатомии.
- Числа 12.703, 54–54.4 и 93–101 Ом, перенесённые в §1 ноутбука 18 из
  ноутбука 17, относятся к каналу 1 ТТРКГ реокардиомониторов МГТУ и РНЦХ, а
  не к боковой сборке канала 2. Историческое имя Z1_TRKG не означает, что
  это стандартная ТРКГ канала 32 РЕО32. Значение 100+ Ом относится к
  историческому модельному расчёту трансторакального монтажа; пока не
  установлено, каким именно MATLAB/Python-скриптом он получен, был ли это
  расчёт полной модели тела при заданном rho или более простая референсная
  постановка.
- В сравнении, использованном в ноутбуке 18, записи МГТУ и РНЦХ относятся к
  одному испытуемому Nix, но выполнены не одновременно, на разных приборах и
  при разных установках электродов. Поэтому межсессионное сравнение допустимо
  как исследовательская проверка согласованности и источник гипотезы о
  масштабе, но не как прямая калибровка РЕО32 или доказательство переносимости
  абсолютного импеданса между экспериментами.
- Значение rho2 = 1.35 Ом·м в сферической модели считать рабочим допущением,
  а не точно известной константой. Сфера описывает эффективную проводящую
  область крови и кровенаполненного миокарда, поэтому значение и
  неопределённость эффективного rho2 требуют отдельного обоснования.
- Проверка масштабирования в ноутбуке 18 пока не устанавливает ни
  идентифицируемость приборного коэффициента усиления, ни возможность
  обнаружить ошибку калибровки. Считать этот вопрос открытым до явной модели
  наблюдения с gain, геометрией, rho1, rho2, R, h и их неопределённостями.
- Рассчитанные в ноутбуке 18 доли дипольного, квадрупольного и последующих
  членов относятся только к выбранной идеальной сфере в однородном
  полупространстве. Они показывают необходимость проверки усечения ряда, но не
  являются разложением сигнала реального сердца.
- Эллипсоид в ноутбуке 18 — гипотеза возможного усложнения сферической модели,
  а не выбранная рабочая модель и не количественная оценка ошибки метода.
- Ноутбук 18 требует отдельного повторного аудита логики и математики. До него
  не считать установленным, что в §1 полный измеренный импеданс повторно
  учитывает сердце: сначала нужно восстановить точный смысл каждого входа,
  постановку исторического MATLAB/Python-расчёта и связь заданного там rho с
  формулами ноутбука.
- FEM-проверку критерия эквивалентной сферы для эксперимента 1 ставить как
  отдельную ветку: ручная сегментация Inobitec, фактическая геометрия каналов 5
  и 9 РЕО32 и явно заданная траектория изменения сердечного объёма.
  3D_Slicer не является каноническим источником сегментации, а
  MATLAB_TRKG4_real_subjects относится к другой геометрии экспериментов 2–3 и
  не переносится автоматически.
- `HnL/` — старый код, который пока остаётся отдельным проектом. Не включать
  его автоматически в доказательную цепочку основной диссертации.
- `Tikhomirov/` — важная история с возможным повторным использованием.
  Ничего не переносить из неё в активный код до проверки происхождения,
  лицензий, данных и соответствия текущей методологии.
- Необходимость и связи `Old_code/`, `research/` и `tools/` автором пока
  не подтверждены. Описывать только наблюдаемое содержимое и результаты аудита;
  не придумывать им роль и не запускать автоматизацию без отдельного решения.

## Zotero

- Связанная библиографическая библиотека/профиль Zotero: `Heart_Lung`
  (локальный профиль наблюдался как `Zotero_Heart_n_Lung_Data`). Отдельной
  коллекции с буквальным именем `Heart_Lung` в текущем дереве нет.
- При поиске литературы по проекту сначала проверять подходящую тематическую
  подколлекцию из карты ниже, затем всю библиотеку `Heart_Lung` и только после
  этого внешние источники. Явно отличать записи из Zotero от найденных вне неё.
- Текущая архитектура коллекций зафиксирована по read-only просмотру локальной
  библиотеки 27.08.2026 как наблюдаемое состояние, а не как утверждение о
  полноте или качестве доказательной базы. Содержимое коллекций дополнительно
  сверено по согласованному снимку метаданных
  `Zotero_Heart_n_Lung_Data/zotero.sqlite.bak` от 26.08.2026 23:06,
  SHA-256 `E707BB2DCCE4F89DAB3EF9F3FE698588CA4026CE1FD4ED05F0A99EA552080FC0`:
  в нём 306
  неудалённых библиографических записей, из них 2 не включены ни в одну
  коллекцию; в дереве `Калмыков` — 269 уникальных записей. Числа членства
  не складывать между коллекциями: одна публикация может входить в несколько
  разделов. Две записи вне коллекций требуют отдельного решения, но не
  автоматического перемещения: `X6T36MNA` — ещё одно представление диссертации
  Кузьмина с названием, совпадающим с `4ZB7JPPC`; `T5PNSXI8` — статья о
  возможностях и ограничениях ICG при оптимизации устройств сердечной
  ресинхронизирующей терапии. В обозначениях ниже `direct` — записи,
  непосредственно включённые в коллекцию, `tree` — уникальные записи коллекции
  вместе со всеми дочерними разделами:
  - `Знания прошлых поколений` (`SXEYTF4A`; direct/tree 20/20).
  - `Калмыков` (`WJZ3KI2U`; direct/tree 5/269):
    - `ААР` (`HE2FL89L`; direct/tree 8/8);
    - `База` (`GS5WLFZF`; direct/tree 1/1);
    - `диагностическая информативность ICG` (`8FF2ZWNM`; direct/tree 2/2);
    - `Доклады/презентации` (`FAXGMWY4`; direct/tree 4/4);
    - `Другое РЕО` (`2QIKI9U6`; direct/tree 2/2);
    - `Изменение анатомии во время дыхания` (`HV6A6BTH`; direct/tree 0/28):
      - `Изменение анатомии лёгких во время дыхания` (`8J7H7CMQ`; direct/tree 19/19);
      - `УЗИ межрёберных мышц и расстояния плевра-кожа` (`TE9MV9FY`; direct/tree 9/9);
    - `Корейский институр Рео` (`NXAQERHU`; direct/tree 4/4; имя коллекции приведено буквально);
    - `Найденные статьи` (`YRZN8MA3`; direct/tree 79/79);
    - `Обзор аналогов и существующих методик мониторинга гемодинамики`
      (`7X4U4E6U`; direct/tree 60/60);
    - `Обработка сигналов ICG` (`3FWZN6T9`; direct/tree 7/7);
    - `Основы метода и расчёт ударного объёма` (`V4ZINMRI`; direct/tree 9/9);
    - `Предсердия-компонент импедансного сигнала` (`WYLX9VHJ`; direct/tree 2/2);
    - `Раздельная функция желудочков и реконструированная ИКГ` (`MDL9A4T9`; direct/tree 2/2);
    - `Разработка импедансного кардиографа` (`39V42WW8`; direct/tree 3/3);
    - `Расположение электродов` (`68ZNTRCQ`; direct/tree 0/49):
      - `Картирование поля и конечно-элементные модели` (`43BPJB4B`; direct/tree 10/10);
      - `Классические шейно-грудные конфигурации` (`IRCDT6XH`; direct/tree 22/22);
      - `Другие конфигурации и носимые датчики` (`2SXADNXD`; direct/tree 14/14);
      - `Электрическая импедансная томография сердца` (`JGSPGJHT`; direct/tree 3/3);
    - `Фракция выброса и систолическая функция левого желудочка` (`L5SKRRXU`; direct/tree 5/5);
    - `Электрическая импедансная томография сердца` (`V5TFWJAD`; direct/tree 3/3) — отдельная
      одноимённая коллекция непосредственно внутри `Калмыков`.
  - `Математика` (`LT82IW33`; direct/tree 2/2).
  - `Написание кандидатской` (`IZDY8AJR`; direct/tree 14/14).
  - `Плыткевич` (`JLS6IMSY`; direct/tree 0/0).
- Только в резервном снимке присутствуют пустые служебные коллекции `test`
  (`4QCI2GJD`) и `тест` (`6X3UN5DZ`); в текущем дереве локального API их нет. Не
  считать их каноническими и не использовать как источник доказательств.
- Для моделей электродов и токораспределения сначала обращаться к
  `Расположение электродов`, особенно к `Картирование поля и
  конечно-элементные модели`; для обработки сигналов — к `Обработка сигналов
  ICG`; для формул ударного объёма — к `Основы метода и расчёт ударного
  объёма`; для исторических работ и диссертаций, включая материалы Тихомирова,
  — к `Знания прошлых поколений`.
- `Найденные статьи` (79 записей в снимке) и `Обзор аналогов и
  существующих методик мониторинга гемодинамики` (60) — крупные накопительные
  подборки, а не замороженные доказательные корпуса. Перед синтезом выделять
  версионный набор по конкретному вопросу, устранять дубли и проверять тип
  исследования, объект, геометрию электродов, прибор и способ валидации.
- `Расположение электродов` объединяет 49 уникальных записей в четырёх
  тематических подколлекциях: классические шейно-грудные конфигурации (22),
  другие и носимые конфигурации (14), картирование/FEM (10) и сердечную EIT
  (3). Одноимённая коллекция сердечной EIT непосредственно в `Калмыков`
  содержит другой набор из 3 записей; эти два раздела не смешивать молча.
- `ААР`, `База` и `Доклады/презентации` содержат смешанные публикации,
  веб-обзор и внутренние документы. Их принадлежность к коллекции не является
  признаком рецензирования или достаточности доказательств. `Написание
  кандидатской` использовать для нормативных и оформительских требований, а
  не для обоснования физической модели.
- Для дыхательной геометрии, эффективной толщины $h$ и согласования
  дыхательных состояний КТ/электрической записи/FEM сначала проверять
  `Изменение анатомии во время дыхания` и обе дочерние коллекции.
- Для целевых показателей сердца отдельно проверять `Фракция выброса и
  систолическая функция левого желудочка`, `Предсердия-компонент импедансного
  сигнала` и `Раздельная функция желудочков и реконструированная ИКГ`; для
  аппаратуры и метрологии — `Разработка импедансного кардиографа`.
- Дочерний EIT-раздел `JGSPGJHT` использовать прежде всего для вопросов
  электродной геометрии и происхождения сердечного сигнала; самостоятельный
  `V5TFWJAD` — для оценки сердечных объёмов/функции средствами EIT. Перенос
  публикаций и выводов между ними делать только явно.
- Для аналитической двуслойной постановки дополнительно проверять `Математика`
  и `Знания прошлых поколений`. Автореферат Кризского (`MGPBNPPU`, дубликатное
  представление `59SUEWA9`) подтверждает общий аппарат функций Грина и прямых
  задач в горизонтально-слоистом полупространстве, но просмотренный текст не
  является первичным источником конкретной тетраполярной формулы серии `30`.
- Автореферат Тихомирова (`BUPQP59M`) является существенной предшествующей
  работой для гипотез о пульсовом кровенаполнении мягких тканей, влиянии
  кожно-жирового слоя, размерах боковой электродной системы и эквивалентной
  сфере. Он использует другую модель и экспериментальную постановку, поэтому
  не считать его валидацией двуслойной модели экспериментов 2–3.
- Для прямого FEM-разложения тканевых вкладов проверять `Другое РЕО`, включая
  `Decomposition of Surface Thoracic Impedance to Partial Impedances of
  Internal Tissues` (`QF5XKU75`); для многоканальных trans-impedance/EIT
  подходов — `Корейский институр Рео`; для клинической применимости ICG —
  `диагностическая информативность ICG` и обзор аналогов.
- В библиотеке есть повторные записи одной публикации в разных коллекциях.
  Read-only аудит 27.08.2026 выявил 17 групп одинаковых DOI и 18 групп
  одинаковых нормализованных названий среди неудалённых библиографических
  записей. Перед подсчётом литературы и экспортом библиографии дедуплицировать
  по DOI/названию, не удаляя записи Zotero без отдельного решения автора.
- Не добавлять, не изменять и не удалять записи Zotero без явного запроса
  автора. При цитировании сохранять Zotero item key и BibTeX citation key как
  разные идентификаторы.

## Git и сохранение истории

- Канонический удалённый репозиторий: `https://github.com/NixWrk/PhD_Disser`.
- Основная ветка: `Калмыков`. Не переписывать её опубликованную историю и не
  выполнять force-push без отдельного явного решения автора.
- Исходная история из `D:\Git_Code\Kardio_respiratory_dudes` сохранена через
  локальный remote `legacy-old`. Неперенесённые коммиты закреплены веткой
  `legacy/old-kalmykov-2026-08-17` на SHA `07c52ad32a65`.
- Повреждённые метаданные, найденные до восстановления репозитория, сохранены в
  `.git-recovery/2026-08-23-pre-restore/`. Каталог не коммитить и не удалять до
  отдельной проверки опубликованной legacy-ветки и резервной копии.
- Не выполнять `filter-repo`, rebase опубликованных веток, reset --hard,
  очистку объектов или замену `.git` без отдельного аудита и плана отката.
